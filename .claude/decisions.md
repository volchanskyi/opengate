# Architecture Decision Records

<!-- Last updated: 2026-07-02 -->
<!-- Compact index. Full ADR text lives in /docs: -->
<!--   - ADR-001 through ADR-012: docs/Architecture-Decision-Records.md (combined log) -->
<!--   - ADR-013 onward:          docs/adr/ADR-NNN-title.md (one file per decision) -->
<!-- All ADRs are mutable — edit in place to keep them current (ADR-036), including the 001–012 log. -->
<!-- Supersede with a new ADR only for a genuine decision change. -->
<!-- Consolidated (ADR-036): former amendment ADRs folded into their parents' Amendments sections — -->
<!--   ADR-028 + ADR-032 → ADR-019; ADR-026 → ADR-020; ADR-031 + ADR-033 → ADR-023. -->
<!-- See docs/README.md for the full convention. -->

| ADR | Decision | Phase | Status |
|-----|----------|-------|--------|
| 001 | MessagePack wire protocol with internally tagged enums, `[type][len][payload]` framing | 1 | Accepted |
| 002 | Golden file tests — Rust generates, Go verifies | 1 | Accepted |
| 003 | SQLite WAL via `modernc.org/sqlite` (pure Go, no CGo), `MaxOpenConns(1)` | 2 | Superseded by ADR-014 |
| 004 | ECDSA P-256 self-signed CA, CSR enrollment via `/api/v1/enroll/{token}`, TLS 1.3 | 2 | Accepted |
| 005 | QUIC mTLS via quic-go — agent opens the control stream and writes first | 4 | Accepted; stream-ownership rationale superseded by ADR-037 |
| 006 | Platform traits with null impls for headless/CI environments | 5 | Accepted |
| 007 | VAPID Web Push, keypair persisted to `{dataDir}/vapid.json` | 10 | Accepted |
| 008 | `aarch64-unknown-linux-musl` cross-compilation via `cross` | CD-C | Accepted |
| 009 | Cosign keyless signing for container images (GitHub OIDC) | CD-E | Accepted |
| 010 | Separate `device_hardware` table for on-demand hardware inventory; `RestartAgent`/`RequestHardwareReport`/`HardwareReport` control messages | 12+ | Accepted |
| 011 | On-demand device logs via control path (`RequestDeviceLogs`/`DeviceLogsResponse`/`DeviceLogsError`), individual rows in `device_logs` table with 5-min cache TTL, SQL-level filtering | — | Accepted |
| 012 | SonarCloud quality gate as hard merge block — Clean-as-You-Code model, coverage/ratings/hotspots enforced on new code | — | Accepted |
| 013 | Docs live in-repo under `/docs`; wiki deprecated; link-over-paraphrase; original ADR immutability rule superseded by ADR-036 | — | Accepted; amended by ADR-036 |
| 014 | PostgreSQL 17 via `pgx/v5/stdlib`, native types (`TIMESTAMPTZ`/`UUID`/`JSONB`), app Helm chart StatefulSet, production `oci-bv` PVC, off-cluster `pg_dump` backups via Object Storage PAR, `postgres_exporter` metrics | 13a | Accepted (supersedes ADR-003) |
| 015 | IaC defense-in-depth: Checkov (terraform/dockerfile/github_actions) + Hadolint + Trivy + gitleaks all run; `.checkov.baseline` is the single suppression surface, no inline `checkov:skip` permitted | S2 | Accepted |
| 016 | Bidirectional goldens (Go→Rust reverse fixtures alongside existing Rust→Go) + `.meta.json` sidecars carrying `protocol_version`/`format` per `.bin` | C1 | Accepted (extends ADR-002) |
| 017 | CI gates consolidation: inline IaC gate into ci.yml with hard-block on direct-push destroys; drop in-repo `docs/mutation-history.jsonl` (workflow artifact retained for audit; trend-store clause superseded by ADR-038); remove `environment: staging` from `load-test.yml` | — | Accepted; trend-store clause superseded by ADR-038 |
| 018 | OCI Bastion (STANDARD, target = OKE worker-node subnet, IAM-gated, 3h session TTL) is the human node-SSH path; `make tunnel` is Kubernetes port-forward to Grafana; CI/CD uses `oci-kube-setup` and the OKE API, not Bastion/SSH | — | Accepted |
| 019 | PMAT (`pmat@3.17.0` exact pin) as augment-only quality overlay at three separately-togglable points: MCP server with 7-tool read-only allow-list, precommit `pmat tdg --threshold B+` step, nightly full-repo workflow → VictoriaMetrics/Grafana with ≥3-pt repo-score + single-file TDG-slip Telegram alerts; no existing gate replaced; Kaizen `--dry-run` only. **Amendments** (folded in, formerly ADR-028/032): the verified `pmat@3.17.0` CLI/MCP mapping (`check-quality --min-grade B+`, the 7-tool MCP allow-list, 0–100 repo-score, baseline 64.5/C on `d3f0373`) + the TDG gate excluding gofmt-only Go test files | — | Accepted (incl. amendments; trend store amended by ADR-038) |
| 020 | Modular monolith — full hexagonal across all modules (overrides plan's layered-hybrid recommendation); earned-port rule; 12 Go modules with named inbound + outbound ports; AMT keeps `internal/mps` as `internal/amt/transport` (no merge); CI gates `go-arch-lint` + `cargo-deny`/`cargo-modules` + `eslint-plugin-boundaries`/`dependency-cruiser` start in warn, flip to error auto at zero-violations (no soak); opportunistic-trigger rollout; ADR-021–024 follow. **Amendment** (folded in, formerly ADR-026): cargo-deny strict policy — HTTP-stack inventory (direct `reqwest`+`tokio-tungstenite`), 28-entry transitive-duplicate `[bans.skip]` allowlist, `publish=false`+`allow-wildcard-paths`, and the §5.4 `warn→deny` flip executed | — | Accepted (incl. amendment) |
| 021 | Go per-aggregate repositories extracted from monolithic 56-method `db.Store` (target <30 residual); 12 per-domain `*Repository` outbound ports owned by consuming modules; transactions owned by use-case layer, threaded via `context.Context` through `dbtx` helper; per-domain `query.go` for cross-aggregate read models; opportunistic migration order with `cert` as the lowest-fan-in pilot | — | Accepted |
| 022 | Web per-feature state — 11 of 12 Zustand stores move into `features/<x>/state/` with `index.ts` barrel exports; only `useAuthStore` stays global (hydration-gated); `useToastStore` → `lib/feedback/`; `eslint-plugin-boundaries` (app/feature/lib element groups) + `dependency-cruiser` snapshot enforce cross-feature access via barrels only; starts in warn, auto-flips at zero violations | — | Accepted |
| 023 | Relay session-registry seam — retain the slim `SessionRegistry` (`SaveSession`/`DeleteSession`/`Ping`) with `InProcessRegistry` as the only adapter; live connection pairs remain local and registry errors stay non-fatal. The Redis/Sentinel adapter and cross-server proxy amendments (formerly ADR-031/033) are reverted and removed; their rebuild requirements live in [`docs/Multiscale-Readiness.md`](../docs/Multiscale-Readiness.md) | — | Accepted; distributed amendments reverted |
| 024 | Rust agent `ControlMessageHandler` trait around the inner `handle_control` fan-out (~10 methods grouped into MouseHandler / KeyboardHandler / FileHandler / WebRTCHandler / SwitchHandler / TerminalControlHandler); outer 4-branch `handle_frame` stays as thin multiplexer (earned-port rule not satisfied); staged carve-up with cargo-mutants per-PR gate preserving 89.5% baseline; corrects plan's wrong hotspot location (handler.rs:17-46, not session/mod.rs) | — | Accepted |
| 025 | CD pre-flight digest check inside `resolve-tag` (crane against GHCR + actions/cache staging state) short-circuits `deploy-staging-k8s` when target digest matches prior cached deploy AND `deploy/**` is unchanged; fail-open to full Helm rollout on cache miss/ambiguity; `workflow_dispatch` always proceeds; production deferred | — | Accepted |
| 027 | Adversarial pen-test gate (Semgrep) — fail-closed custom-rule gate over the diff at commit hook + gauntlet + blocking CI job (required `merge-to-main` check); 6 v1 rules (missing-admin-gate, path-traversal, token-in-log, plaintext-secret-column, IDOR-advisory, OpenAPI spec-drift); `semgrep==1.108.0` pinned via `scripts/install-semgrep.sh`; diff-only via `--baseline-commit` grandfathers pre-existing findings; HIGH blocks / MEDIUM advisory; no inline suppressions (path-glob `.semgrepignore` or per-rule `paths.exclude` only); no LLM in v1 | — | Accepted |
| 029 | Test determinism — no silent skips. Postgres-backed tests auto-provision a throwaway `postgres:17-alpine` via testcontainers-go (`server/internal/testpg`, leaf pkg; `URL()` for `TestMain`, `BaseURL(t)` fails loudly) when `POSTGRES_TEST_URL` is unset — CI/dev external DB still honored; `max_connections=400` matches `make postgres-test-up`. Cross-language skip-guard hook (`pretooluse-test-skip-guard.sh`, PreToolUse, no bypass) blocks new skip/focus markers in test files: Go `t.Skip*`; web `it/test/describe.skip/skipIf/only/todo/fixme` + `xit/xdescribe/xtest/fit/fdescribe(`; Rust `#[ignore]`. Golden generators refactored to always-run (write committed fixtures only under `GENERATE_GOLDEN`). Rule: `.claude/rules/tests-determinism.md` | 13b | Accepted |
| 030 | Kubernetes adoption — OKE BASIC with the [`deploy/helm/opengate`](../deploy/helm/opengate/) chart; ingress-nginx + cert-manager HTTP edge; in-cluster PostgreSQL; direct node `hostPort` exposure for QUIC/MPS; external Secret; rendered-manifest validation through `make lint-k8s`. Multi-node L4 templates and distributed relay dependencies are not part of the current chart | 13b | Accepted |
| 034 | Shared server keys via the existing Kubernetes Secret — production mounts the enrollment CA, VAPID, and update-signing files read-only into an `emptyDir` `/data`, skips the server-data PVC, and preserves identity across redeploys. The KEDA `ScaledObject` and PodDisruptionBudget portions are reverted and removed; session-aware autoscaling remains a rebuild requirement in [`docs/Multiscale-Readiness.md`](../docs/Multiscale-Readiness.md) | 13b | Accepted; autoscaling/PDB reverted |
| 035 | OKE free-tier block-volume remediation (450→200 GB, at the cap) — count, not size, is the lever (50 GB OCI minimum). **Keep 3 block PVCs:** prod Postgres + VictoriaMetrics + Loki. **Drop 5:** (1) `uptime-kuma` removed entirely → external uptime SaaS (also catches whole-node outages), monitoring chart now exposes no ingress; (2) Grafana `/var/lib/grafana` → `emptyDir` (all config provisioned from ConfigMaps, only annotations/alert-history lost); (3+4) staging Postgres + server `/data` → `emptyDir` via new `postgres.storage.persistent` / `server.dataVolume.persistent` flags (default true; staging false, **production unchanged**); (5) backups → OCI Object Storage via a **write-only PAR** (`BACKUP_PAR_URL` Secret), CronJob split into a `pg_dump` init container + a `curlimages/curl` PUT container sharing an emptyDir (postgres image has no curl), retention via an OS lifecycle policy. Prod data/storage untouched; off-cluster backups survive cluster loss. Manual follow-ups (SaaS monitors, bucket/PAR/lifecycle, delete 5 freed PVCs, Cloudflare `status.*`) in chart NOTES.txt. Working plan: `.claude/plans/archive/oke-block-volume-free-tier.md` | 13b | Accepted |
| 036 | All ADRs are mutable current-state records — per-file (013+) and the combined 001–012 log alike; supersede only for decision changes; ADRs may link archived plans but not active plans | — | Accepted (supersedes ADR-013 immutability clause) |
| 037 | Client-first QUIC handshake + fast-path reconnect: agent opens and writes first, auth is mTLS-only, `0x14` saves a round-trip, 1-RTT session resumption adopted while 0-RTT is deferred; ADR-005's server-opens rationale is superseded | 4 | Accepted |
| 038 | VictoriaMetrics is the canonical numeric CI-trend store through the shared kubectl transport; Loki is logs-only; branch-hosted benchmark/browser-performance history and Loki trend pushes are retired; global VM retention applies | — | Accepted (supersedes ADR-017/019 trend-store clauses) |
| 039 | Diagrams as Code Part 2 (extends DD-E): native Mermaid **C4** (`C4Context`/`C4Container` in Architecture.md) gated by a **mandatory GitHub render-verification** with a documented `flowchart`/`sequenceDiagram` fallback; **CI-only** Mermaid syntax validation via the official parser pinned in `tools/mermaid-validate/` (`docs-validate.yml`, path-filtered, isolated from `web/` + the local gauntlet); **drift-guard hardening** — `docs-diagrams.test.sh` pins every diagram-bearing doc + a total-fence floor, plus a CODEOWNERS/PR-template nudge; a **diagram coverage standard** in README + new deploy/CI-CD/session-lifecycle diagrams. Working plans: `diagrams-as-code-part-2.md` + `diagrams-d1..d5.md` | — | Accepted (extends DD-E) |
| 040 | Service-extraction decision lens (Balanced Coupling) — score each module on integration strength (proven contract-level only for the constrained leaves `go-arch-lint` lists, not the `other` catch-all candidates), fan-in (distinct production importers), and volatility (120-day churn); extract only when strength is contract-level AND fan-in is low AND a concrete scale/availability/isolation driver exists, else decouple-in-place / leave alone. Advisory, not a CI gate. **Current verdict: no extraction** (single-node free-tier, no driver); `db` decouples in-place via ADR-021, `api`/`agentapi` stay (latency, low fan-in), relay stay (multi-server removed, churn self-inflicted). Replaces the former `service-extraction-roadmap.md` plan note | — | Accepted |
| 041 | Postgres RLS multi-tenancy — default tenant backfill, `tenant_id` on tenant tables, forced RLS policies, tenant context from JWT `tenant` through `dbtx.Scoped`, policy-based admin bypass, dedicated non-superuser/non-`BYPASSRLS` Helm runtime role; per-repo deny tests + automated dump/restore/down migration rehearsal | Edge-Sentinel WS-0 | Accepted |
| 042 | Control protocol forward compatibility — unknown control types tolerated both directions and new server→agent variants capability-gated through `AgentRegister` (`HardwareInventory`, `DeviceLogs`); bidirectional unknown-type goldens | Edge-Sentinel WS-1 | Accepted |
| 043 | Edge Sentinel local ML sampler — clean-room pure-Rust k=2 ensemble/window/sampler/redaction kernel, process top-N by rank, no full cmdline by default, always-on agent background task; allocation/RSS guards + Criterion ARM-footprint bench harness | Edge-Sentinel WS-2 | Accepted |
| 044 | Edge Sentinel server telemetry ingest — numeric samples go to VictoriaMetrics through a scoped server client that injects `tenant_id`; process snapshots go to forced-RLS Postgres `device_processes`; agent writes are scoped by the server-resolved device tenant, not payload `tenant_id`; telemetry has cap/interval/drop arbitration and VM stream aggregation | Edge-Sentinel WS-4 | Accepted |
| 045 | Load-test regression gate from VictoriaMetrics read-back — supersedes ADR-038's load-test visibility-only consequence: `load-test.yml` now splits run/publish jobs, checks per-series latency/rps/error-rate regressions through `scripts/lib/vm-query.sh`, keeps p99 advisory-only, pushes current rows before Telegram/fail-red, and leaves normal merge CI ungated | — | Accepted |
| 046 | Edge Sentinel raw-log broker — retire the central `device_logs` cache (dropped in `004_retire_device_logs`) and broker every raw pull transiently: `GET /devices/{id}/logs` blocks on a per-connection single-flight waiter for the agent's bounded `DeviceLogsResponse`, redacts server-side, and streams it back with nothing persisted; elevated (admin) gate + `device.logs.read` audit on every pull; `409` busy / `504` timeout; log-rate dims still go to VM scoped. Working plan: [edge-sentinel-ws-11-log-server.md](plans/archive/edge-sentinel-ws-11-log-server.md) | Edge-Sentinel WS-11 | Accepted |
| 047 | Web telemetry chart engine — thin uPlot (canvas-2D) adapter is the only module importing uPlot (typed-array `setData`, `ResizeObserver`, engine-agnostic swap seam), code-split into a budgeted `charts` chunk; device-detail anomaly panel + per-family timelines + drag-to-correlate drill-down; scalar-only health badges on the grid/dashboard (no per-device series); honest `min_max_source` band provenance (central VM avg-only); logs explorer over the WS-11 broker with level/time/search filters + page facets and a metrics↔logs correlation jump. [ADR-047](../docs/adr/ADR-047-web-telemetry-chart-engine.md); working plans: [edge-sentinel-ws-6-web-ui.md](plans/archive/edge-sentinel-ws-6-web-ui.md), [edge-sentinel-ws-12-log-web.md](plans/archive/edge-sentinel-ws-12-log-web.md) | Edge-Sentinel WS-6/WS-12 | Accepted |
| 048 | Edge Sentinel endpoint-log model (edge-stored, server-proxied) — raw log lines are the signal: they stay at the edge as the durable store of record and are read only on-demand through the transient broker, surfaced in the browser as two panes (Agent Logs = the agent's own files; System Logs = the platform host log via `source=host`, with severity/time/search + unit filters). A central Loki log lake is rejected (secret density + 200 GB cap + weak duplicate of the host's own log store). The numeric channel carries live host metrics ([057]), not a log-derived signal. [ADR-048](../docs/adr/ADR-048-edge-sentinel-endpoint-log-model.md); working plan: [edge-sentinel-ws-13-log-privacy-ops.md](plans/archive/edge-sentinel-ws-13-log-privacy-ops.md) | Edge-Sentinel WS-13 | Accepted |
| 049 | Edge Sentinel raw-log privacy — layered controls with redaction as **defense-in-depth**, not the primary boundary: no central persistence + admin-elevated + `device.logs.read` audit + bounded exposure are structural, while **two independent redaction guards** (agent-edge and server-backstop) scrub a shared secret corpus (bearer/basic, key=value/key: value creds, JWT, AWS/GCP keys, credentialed connection strings, PEM keys) kept in sync as paired tests; redaction is explicitly best-effort. [ADR-049](../docs/adr/ADR-049-edge-sentinel-raw-log-privacy.md); working plan: [edge-sentinel-ws-13-log-privacy-ops.md](plans/archive/edge-sentinel-ws-13-log-privacy-ops.md) | Edge-Sentinel WS-13 | Accepted |
| 050 | Edge Sentinel host log-reader sourcing (no-GPL) — read host log sources via first-party CLIs parsing structured output (`journalctl -o json`, PowerShell `Get-WinEvent \| ConvertTo-Json`, agent self-logs direct) instead of linking `libsystemd` (LGPL/GPL, incompatible with the workspace Apache-2); collectors are bounded, always-on, and no-op off-platform; output-format drift is a fixture-tested parser concern, not a linkage/ABI one. [ADR-050](../docs/adr/ADR-050-edge-sentinel-log-reader-sourcing.md); working plan: [edge-sentinel-ws-13-log-privacy-ops.md](plans/archive/edge-sentinel-ws-13-log-privacy-ops.md) | Edge-Sentinel WS-13 | Accepted |
| 051 | Edge Sentinel local TSDB substrate (WS-14a bake-off) — pick **`redb`** + the bespoke Gorilla tiering/compression layer as the offline store substrate: "simplest that clears the bar". Measured on one fixture corpus in [`agent/crates/edge-tsdb`](../agent/crates/edge-tsdb): bespoke append-only (A) is ~2.8× denser (2.6 vs 7.3 B/sample) but owns ~500 lines of crash/recovery code, whereas redb inherits 2-phase-commit/quick-repair/`check_integrity` with ~165 glue lines, no background threads, and zero transitive deps (MIT/Apache). `fjall`/`tsink` fail the dependency sub-gate empirically (both add a 3rd `hashbrown` major → `multiple-versions=deny`, plus compaction threads). A stays as density/fallback reference, C as the no-persist control. Working plan: [edge-sentinel-ws-14a-offline-tsdb-spike.md](plans/archive/edge-sentinel-ws-14a-offline-tsdb-spike.md) | Edge-Sentinel WS-14a | Accepted |
| 053 | Edge Sentinel declarative threshold alerts (WS-19) — per-tenant threshold rules evaluated locally beside the ML anomaly detector: a rule is a metric gauge (`cpu.total`/`mem.used`/`disk.used`) + comparator (`Gt`/`Lt`/`Gte`/`Lte`) + fire threshold + hysteresis `clear` + `sustain_secs`; a pure evaluator ([`alerts`](../agent/crates/mesh-agent-core/src/alerts/)) steps Clear→Pending→Firing so a breach must sustain before firing (rising-edge flap suppression) and stays firing until it recovers past `clear` (falling-edge). Rules are delivered as the connecting agent's authoritative-tenant set over a capability-gated (`ThresholdAlerts`) server→agent `PushAlertRules` (server config, no migration/API) so one tenant's rules never reach another; default minimal. A firing breach rides additively in `AgentHealthSummary` (`breaches`, no new stream), ingested as `opengate_edge_alert_breach` (metric label bounded, rule id sanitized) and charted on the Soak dashboard. Investigation-aid only (no auto-notify until the FPR soak). [ADR-053](../docs/adr/ADR-053-edge-sentinel-threshold-alerts.md); working plan: [edge-sentinel-ws-19-threshold-alerts.md](plans/archive/edge-sentinel-ws-19-threshold-alerts.md) | Edge-Sentinel WS-19 | Accepted |
| 054 | Edge Sentinel data lifecycle — right-to-be-forgotten erasure (WS-20): a device delete or tenant/fleet purge irreversibly erases the subject's telemetry across every store (VictoriaMetrics series, Postgres RLS rows, optional cold-tier objects, agent local store) and deprovisions its agents; no soft-delete/grace/undo. **Tombstone-first strict ordering** — record the subject in a persisted non-RLS `deleted_ids` deny-list, then delete VM series → cold objects → Postgres rows **last** (labels/FKs survive the drain; a crash marks-deleted not half-alive). **Every write path checks the deny-list** (agent server warms it at startup, rejects on connect + every write-path control msg; a tenant purge records a per-device tombstone so the check needs only the device id). Purge is a **persisted, resumable `purge_jobs`** whose completion gates on a post-delete VM emptiness verify; a periodic **reconciliation sweep** GCs orphaned series. **Audit events + the tenant row are retained** (erasure proof; audit FK would block tenant-row deletion). Server-side only (VM `-deleteAuthKey` + object creds never at the edge; selectors always pin `tenant_id`). Documented backups-expiry SLA (pg_dump copies age out via the bucket lifecycle policy). [ADR-054](../docs/adr/ADR-054-edge-sentinel-data-lifecycle-erasure.md); working plan: [edge-sentinel-ws-20-data-lifecycle-deletion.md](plans/archive/edge-sentinel-ws-20-data-lifecycle-deletion.md) | Edge-Sentinel WS-20 | Accepted |
| 052 | Edge Sentinel local TSDB build (WS-14b) — graduate the spike into `store::LocalTsdb` on **`redb`**: T0 1 s raw + inline anomaly bit / T1 60 s / T2 3600 s rollups in **separate tables written in one atomic transaction**; **fixed-point-per-metric** value codec (lossless to 1/scale, adaptive vs float32/int-DoD, blocks self-describe scale); rollups **sample-ts-keyed and merged incrementally** (sum+last_ts, no T0 re-read, NTP-safe); big-block T0 packing; durable **backfill cursor** (WS-15); **coarsest-first** logical-byte disk-cap eviction + host-free backoff; **format version + forward migration** + graceful open on corrupt/future files; **MVCC snapshot** reads; **deprovision purge** (WS-20); optional **cold-tier DEFLATE** on sealed T1/T2 only (pure-Rust `flate2`, **no zstd**, feature-gated default-on). Bake-off substrates + fault harness retained behind the default-on `bakeoff` feature; shipped agent depends `default-features=false` (only `redb`+`flate2` ship, clears `deny.toml`). Measured x86_64: ~1.87 logical B/sample (1.70 cold), ~1.1 ms crash-recovery open. [ADR-052](../docs/adr/ADR-052-edge-sentinel-local-tsdb-build.md); working plan: [edge-sentinel-ws-14b-offline-tsdb-build.md](plans/archive/edge-sentinel-ws-14b-offline-tsdb-build.md) | Edge-Sentinel WS-14b | Accepted |
| 055 | Fault-injection mechanism — **no fault code in the shipped binary**; prod and staging run the identical image. In-process app faults (panic recovery, typed port-error mapping, request timeout, ctx-cancel blocking, connection-close, tenant-context preservation) run in a **Go test harness** that substitutes a fault-decorating port around the real in-process server (the `store_failure_test.go` idiom + the FI0 `AgentControl` seam), `_test.go` only. Deployed faults (dependency latency/abort, QUIC/UDP agent path, **D1** packet loss/corrupt/partition, pod-kill, stress) run in **Chaos Mesh**, installed on-demand and `opengate-staging`-scoped; edge 5xx/timeout via ingress annotations. Rejected: a compiled-in injector, a build-tag fault binary, toxiproxy (TCP-only), and a **separate chaos cluster** (200 GB block cap infeasible, ADR-035). Accepted risk: a **privileged chaos-daemon on the shared production worker** under a hard guardrail contract — on-demand install/uninstall, namespace scope, mandatory `duration`, staging selectors + production-pod-exclusion guard, pinned arm64 digest, zero-residue uninstall. Both CI surfaces gate promotion at once (Go suite in normal CI + Chaos Mesh/ingress drills as a required post-E2E staging stage), no clean-run-history wait. [ADR-055](../docs/adr/ADR-055-fault-injection-mechanism.md); spec: [Fault-Injection](../docs/Fault-Injection.md). Working plan: `context-driven-fault-injection.md` | Fault-Injection FI1 | Accepted |
| 056 | Device maintenance mode + always-on edge collectors — **collectors are always-on**: the sampler/anomaly detector, threshold-alert evaluator, and discovery profiler all start with the agent, no opt-in flag/env/capability gate; `Discovery`+`ThresholdAlerts` advertised unconditionally, `Backfill` when the local store opens; store cap is the `EDGE_STORE_CAP_MB` sizing constant; per-entity caps keep in-code defaults (no measurement-gated flip, no sustained-soak/ARM-footprint gate). **Maintenance mode** is a per-device server-authoritative desired state (default Active) on the `devices` row (`maintenance_on`/`_since`/`_by`/`_reason`, migration `007_maintenance_mode`), pushed over the control channel via `SetMaintenanceMode`/`MaintenanceApplied`: while set the agent stops sampling/discovery + alert eval (D5/D6) and **keeps remote management + the control channel live** (not offline), then **re-baselines** anomaly detection on exit; manual-only, no TTL (D7); central web UI only (D8); tenant-scoped, audited toggle `POST /devices/{id}/maintenance` (200 even offline) + the fleet count of devices in maintenance as a field of the tenant rollup `GET /devices/summary` (D9, retargeted by ADR-062). Visibility (escalating badge + fleet count + day-counter) replaces auto-expiry. [ADR-056](../docs/adr/ADR-056-device-maintenance-mode.md) | — | Accepted |
| 057 | Live host-metric streaming to central VictoriaMetrics — the 1 s sampler folds its samples into a **10 s-average `AgentMetricWindow`** (dims `cpu.total`/`mem.used_percent`/`disk.used_percent`/`net.rx_bps`/`net.tx_bps`/`disk.mounts_critical`, the disk pair being the fullest mount and the count of mounts at or above the critical-usage threshold) emitted once per closed window over a bounded drop-on-full channel drained on the heartbeat, so a continuously-connected device charts **live** cpu/mem/disk/net (no reconnect needed) reusing the existing `opengate_edge_metric_avg` ingest + family charts unchanged. Averaging is **byte-identical** to reconnect-backfill's `roll_to_10s` (shared `floor(ts/10)*10` key + `sum/n`, invariant-tested over every dim), so live and gap-filled points for the same `(dim,ts)` land in one series; the partial window is discarded across maintenance and never emitted alone. The net dims are primary-interface throughput in bytes/second (rounded to whole bytes → lossless integer path). Host system logs are served on-demand (System Logs pane, `source=host`) with pushed-down severity/time/unit filters + enumerated `available_units`; the unit filter is argv-safe on journald and allowlist-guarded on the Windows PowerShell path (injection-fixtured). [ADR-057](../docs/adr/ADR-057-live-host-metric-streaming-and-system-logs.md); working plan: [system-logs-and-central-host-metrics.md](plans/archive/system-logs-and-central-host-metrics.md) | — | Accepted |
| 058 | Telemetry persist coalescing + fleet-health badge lookback — each connection buffers a heartbeat's telemetry burst (host-metric windows, the tail-ordered `AgentHealthSummary`, process numerics) on the single read-loop goroutine and flushes it as **one** `WriteSamples` via one persist slot (flush on the next heartbeat, a size cap, or teardown), so the low-rate anomaly summary no longer loses the four-slot persist race to the host-window firehose and is persisted, not dropped. `ProcessReport`'s row upsert keeps its own write; only its numerics join the buffer. The fleet-health badge query uses `last_over_time(<selector>[10m])` (defense-in-depth) so a brief gap never blanks the badge, and a Grafana drop-ratio alert on `edge_telemetry_drops_total`/`_ingested_total` catches regressions. Reserve-a-slot (minimal-diff) and bounded-queue-backpressure recorded as alternatives. [ADR-058](../docs/adr/ADR-058-telemetry-persist-coalescing-and-badge-lookback.md); working plan: [device-detail-ux-and-telemetry-reliability.md](plans/archive/device-detail-ux-and-telemetry-reliability.md) | — | Accepted |
| 059 | Agent session row lifecycle — three cleanup layers keep the database and relay aligned: (1) **bounded unpaired release** — the WebSocket handler defers `Relay.Unregister` and bounds `WaitForPeer`, so a browser-only or agent-only token drops from the relay even when an upgraded request context cannot observe socket closure; external cleanup runs before a potentially blocking graceful close; (2) **tenantless relay-end deletion** — `OnSessionEnd` uses the cross-tenant, self-scoped `DeleteRelaySession(token)` path because both request tenants are gone by teardown; (3) **relay-keyed reconciliation** — periodic `DeleteStale(cutoff, keep)` removes process-orphaned rows while sparing every token the relay still holds. Piped sessions delete immediately, unpaired sessions are bounded by peer wait, and predecessor-process rows are bounded by the sweep grace. [ADR-059](../docs/adr/ADR-059-agent-session-row-lifecycle.md) | — | Accepted |
| 060 | Hand-written msgpack encoder for `ControlMessage` — the union struct's 83 `omitempty` fields each cost a heap allocation per encode, because the reflection encoder decides emptiness by calling `reflect.Value.Interface()` on every field (boxing anything not pointer-shaped) to type-assert an interface that essentially never matches. Cost scaled with the fields the struct *declares*, not the ones a message *populates*: a two-field heartbeat paid the same 79 allocations as a full register, and the nightly trend measured 45→80 allocs/op and 1176→2072 B/op between 2026-06-27 and 2026-07-26 as the protocol grew — a real, compounding cost on every heartbeat and telemetry message, which is what the benchmark gate had been reporting red nightly since 2026-07-02. `ControlMessage` now implements `msgpack.CustomEncoder`, marking presence into a stack `[83]bool` via direct typed comparisons and emitting only marked fields: **80→4 allocs/op, 2072→264 B/op, 3879→569 ns/op**, below even the pre-drift figures. Output is **byte-identical** to the reflection encoder (same declaration order, keys, `omitempty` semantics, and width-preserving int encoders), so committed goldens, the Rust decoder, and deployed agents are unaffected — verified by regenerating the goldens to a zero diff. Hand-maintenance is guarded by a per-field reflection-driven differential test that byte-compares both encoders for every field, plus allocation budgets. Rejected: re-baselining the gate, splitting the union (correct end state, protocol-wide refactor, not foreclosed), pointer-shaped fields, and dropping `omitempty`. [ADR-060](../docs/adr/ADR-060-control-message-hand-written-encoder.md) | — | Accepted |
| 061 | Intel AMT as a device property — AMT stops being a parallel collection: a device's own payload carries an `amt` object (`available` from the agent's Management Engine reading, plus `status`/`uuid` once a CIRA connection is linked), built from two LEFT JOINs by primary key in the existing device read, so the detail page issues **no** AMT request and does no client-side hostname join. The link key is the **SMBIOS system UUID** — the agent reports it from DMI, the AMT firmware presents the same value over CIRA — which resolves the device **and its tenant**, closing a tenancy hole that made `GET /amt/devices` return `[]` in every deployment by construction (MPS upserted with the root app context; the Postgres adapter refused it; the MPS test substituted a writer that bypassed `dbtx.Scoped`). The lookup runs cross-tenant self-scoped (ADR-059 shape); an ambiguous key (cloned disk images) resolves to nothing; the key is **stored, never returned**. An unmatched connection **persists nothing** and is held in memory, re-resolved on a timer so a later agent registration adopts it. `device_hardware` gains five AMT columns written by **two writers on disjoint columns** (agent: `system_uuid`/`amt_available`/`amt_version`; WSMAN over CIRA: `amt_model`/`amt_firmware`), every upsert column-targeted; `amt_available` is a Go pointer so a *stated* false survives `omitempty` and version skew preserves rather than blanks. `amt_devices` reduces to connection state (migration `008_amt_device_link`, links by hostname then discards the unlinkable). Badge keys off capability so it never flickers; power buttons require linked **and** online; BIOS/MEBx setup instructions moved to `/setup`. [ADR-061](../docs/adr/ADR-061-amt-as-device-property.md); working plan: [amt-as-device-property.md](plans/archive/amt-as-device-property.md) | — | Accepted |
| 062 | Tenant-scoped reads, admin-gated configuration, and an O(1) fleet summary — **tenant is the visibility boundary and `is_admin` is the mutation boundary**: every member of a tenant sees the same fleet and may issue any device command (sessions, restart, maintenance, AMT power); only configuration (device delete/group move, group create/delete, enrollment, updates, users, security groups, purge) and secret-bearing reads (device logs, tokens, signing key, audit, user list) are admin-gated. Visibility is enforced in the **repository** (`app.current_tenant` inside every scoped transaction), so an out-of-tenant resource resolves to *not found* and a handler cannot forget the check; the three named guards `requireDeviceInScope` / `requireAMTDeviceInScope` / `requireSessionInScope` make it explicit and are the identifiers the ADR-027 pen-test gate accepts. `groups_.owner_id` is **dropped** (migration `009_drop_group_owner`; lossy, so the down path restores it nullable) — a group is a filing label, not an access boundary. **AMT power** moves from an admin gate to a tenancy check: the CIRA map is keyed by AMT UUID alone, so the handler resolves the managed device first, closing a hole where an admin of one tenant could power-cycle another's hardware. **`GET /devices/summary`** replaces the dashboard's O(fleet) list download plus the separate `maintenance-summary` trip with eight integers from one Postgres aggregate row + one VictoriaMetrics instant query that counts health bands **inside** the TSDB (`count()` over an empty set yields no sample, so a missing band maps to 0 — tested against a real VM; `unknown` = total − Σbands, floored); tenant-scoped for **every** caller including admins, the one deliberate exception to admin cross-tenant reads. Band thresholds live in Go **and** `health.ts`, held together by a paired sync test. Polling is gated on document visibility via `useVisibleInterval` at four sites (list/dashboard/detail/metrics) with a catch-up fire on re-show. Admin-only controls are **hidden, not disabled**. [ADR-062](../docs/adr/ADR-062-tenant-scoped-reads-and-fleet-summary.md); working plan: [dashboard-summary-and-visibility-gated-polling.md](plans/archive/dashboard-summary-and-visibility-gated-polling.md) | — | Accepted |
| 063 | Server→agent control-message completeness — Go encodes `ControlMessage` as a union whose non-`Type` fields are all `omitempty`, so a zero-valued field is dropped from the wire map, while Rust decodes an internally-tagged enum whose variant fields are required; the agent routes the resulting decode error to a catch-all that **breaks the control loop and forces a QUIC reconnect**. Verified reachable: `POST /devices/{id}/restart` with `{"reason": ""}` returned **200**, the device never restarted, and the agent dropped its control stream. The rule adopted is **a zero value may be dropped only where it is a legal value**, closed at a different end per half — informational `reason` on `RestartAgent`/`AgentDeregistered` gains `#[serde(default)]` **agent-side** (server-side `omitempty` removal would emit `reason: ""` on every message and break the byte-identity contract of the hand-written encoder, ADR-060), while load-bearing `token`/`relay_url`/`version`/`url`/`signature` keep the strict decoder and the **server refuses to construct the frame** (`ErrIncompleteControlMessage`) — that is what protects agents already in the field. No `Default` derive on `SessionToken`/`Permissions` (an empty-token session is worse than a decode error); an undeliverable manifest is a 400 on `PushUpdate`, not a silent zero-agent success; an empty restart reason is a 400 (`minLength: 1` in the schema **and** a runtime check, since OpenAPI is not runtime-enforced here). The regression guard is golden **completeness** — reflection over `*agentapi.AgentConn`'s `Send*` methods asserts each appears in the reverse-golden table (16 → 22 fixtures, `…_min` shapes pinning the smallest emittable map), because with the fix living in Rust a Go test cannot ask whether a Rust field is required. [ADR-063](../docs/adr/ADR-063-server-to-agent-control-message-completeness.md); working plan: [wire-contract-and-mutation-carveout-tests.md](plans/archive/wire-contract-and-mutation-carveout-tests.md) | — | Accepted |
| 064 | Four-level tenancy — **tenant → organization → site → device**, named the way the rest of the market names them: today's wall becomes the **tenant**, the freed word **organization** becomes one customer, and today's flat machine groups become **sites** reparented under a customer. The wall stays at the tenant and only there; organization and site are targeting levels carrying the tenant policy like any other tenant-scoped table, because a second enforced boundary would double the policy surface for a restriction nobody has requested. The rename shipped separately from the new levels — ~850 references, and a failure in a mixed diff could not be attributed to either half. **A device's site must be in the device's own customer**, which is a constraint over a *pair*, so it is a composite key (`devices (organization_id, site_id)` → `sites (organization_id, id)`) rather than an application check: the database refuses the mismatch, `ON DELETE SET NULL (site_id)` unfiles machines when an office closes instead of decommissioning them, and a customer move clears the site in the same statement so the old office never travels with the machine. Filing is server-side: a registering agent's site counts only when it belongs to the customer the device lands in, and a reconnect never refiles — otherwise a moved machine comes back naming an office its customer does not have and cannot reconnect at all. **Settings resolve device → site → organization → tenant → what shipped**, as one shared primitive holding the walk and the tie-break and nothing else; where a value is *stored* belongs to the feature it configures. Resolution is keyed on identity as well as level. One class reads the ladder the other way up — a customer-wide stop must not be undone by a value set on one machine — and is named (`BroadestWins`) rather than implied. Rejected: a general settings table owned by tenancy (scored against 26 operator situations; expresses nothing extra, fails six, and inverts the stop switch) and NinjaOne-style policy bundles (stronger for reading configuration back, weaker for labels crossing sites, and not foreclosed — bundles need the same walk). [ADR-064](../docs/adr/ADR-064-four-level-tenancy-and-the-settings-ladder.md); working plan: [tenancy-organizations-and-sites.md](plans/archive/tenancy-organizations-and-sites.md) | — | Accepted |
| 065 | The vitals contract — **cardinality, not sample rate, is what binds the central store**, so central telemetry is a fixed vocabulary at a fixed cost rather than a stream whose shape agents choose. Cadence moves to **60 s centrally** (local sampling stays at 1 Hz), and the four gauges where a within-minute spike *is* the signal ship a companion **`.max`** beside their average: inside a 60 s window at 1 Hz, five seconds pinned at 100 % move a 20 % average to 26.7 % — noise — while the maximum reads 100, so the freeze is unobservable centrally at *any* cadence without an extremum. `disk.used_percent` moves too slowly for a within-minute peak to mean anything and `disk.mounts_critical` is already a threshold count, so neither carries one: ten dims of `opengate_edge_metric_avg`, plus the node-wide anomaly rate and five per-family rates, is **16 series for a platform-neutral device against a cap of 24** — the other 8 belong to the stall (ADR-066) and disk-performance (ADR-067) vitals, which spend the cap in full, after which the next vital of any kind re-opens it. The `dim` label was **agent-controlled** on both write paths (live windows and reconnect backfill copied the wire name straight into a VM label with no allowlist and no bound), so a **server-side allowlist** now makes the count a compile-time constant — an unlisted name is dropped and counted under `opengate_edge_telemetry_drops_total{reason="unknown_dim"}`, and a window of a thousand invented dims creates zero series. Backfill rolls to the same 60 s grid and takes each bucket's maximum from the **stored** rollup, never recomputed from the averages it just read: a max-of-averages is a smaller number that hides exactly the stall the maximum exists to show. Band provenance is renamed `avg_of_60s` and the narrowest requestable bucket becomes 60 s. Rejected: keeping 10 s and adding extrema (six times the samples for a fleet view read at minute scale, and the extremum not the cadence is what recovers a stall), centralizing all four aggregates (the multiplication ADR-044 exists to avoid), bounding the dim label by length/count rather than by name (a bound on shape still lets an agent choose distinct names, and names are what create series), and percentiles (the local rollup holds min/max/sum/last/count and nothing percentile-shaped). [ADR-065](../docs/adr/ADR-065-vitals-contract-cadence-extrema-and-bounded-dims.md); working plan: [edge-first-b2-vitals-contract-and-cadence.md](plans/archive/edge-first-b2-vitals-contract-and-cadence.md) | 057, 044 | Accepted |
| 066 | Stall vitals from kernel pressure, **Linux only** — utilization answers how much of a resource is in use; the operator's question is how much time work spent **blocked** on it, and the two diverge exactly when it matters (a terminal server at 38 % memory whose users wait twenty seconds to save a file is spending a fifth of every minute in reclaim). Five vitals — `stall.cpu.some`, `stall.mem.some`, `stall.mem.full`, `stall.io.some`, `stall.io.full` — are read from the kernel's pressure `avg60`, whose window is exactly the vitals cadence, so each costs one file read and **zero cardinality**: the kernel already performed the reduction. CPU `full` is omitted (the kernel defines it as always zero). These five take a Linux device to **21 of the 24-series cap**, the last 3 going to the disk-performance vitals (ADR-067). **No analogue is synthesized for a platform without a time-in-stall primitive** — publishing counters that measure something else under a `stall.*` name would put two meanings behind one name, the same defect class as a pooled disk average called "disk used"; such a platform reports `stall.*` unsupported and ships nothing. **An absent vital is absent, never zero**: zero is the answer for a host that was measured and never stalled, which is a different fact from a host that cannot measure stalling. A **containerized agent reads its own cgroup's** `*.pressure` (host-wide `/proc/pressure` would report its neighbours' stalls as its own) with **no fallback** to the host when that cgroup publishes none. A stall vital's 60 s bucket carries its **latest reading, not the bucket mean** — the reading is already the kernel's own 60 s average, and a mean of sixty overlapping averages spreads the minute across two and damps the stall; the rule lives in one series mapping applied by the live windower and reconnect-backfill alike, and the hourly rollup (sixty kernel windows, not one) summarizes by mean like every other series. Every path resolves under an **injectable filesystem root**, so no-pressure hosts, containers and malformed kernel files are ordinary fixture directories and no test reads the reference host's `/proc`; the parser stays platform-neutral so nothing is invisible to coverage. A value outside `[0, 100]`, a non-numeric field, a missing line and a truncated file each yield no reading rather than a clamped or partial one. [ADR-066](../docs/adr/ADR-066-stall-vitals-from-kernel-pressure.md) | 065 | Accepted |
| 067 | Disk-performance vitals from per-device kernel counters, **Linux only** — capacity answers "is the disk full" and nothing answered "is it slow", which is a different question about a different entity (capacity belongs to a mount, service time to a physical device). Three vitals — `disk.await_ms`, `disk.await_ms.max`, `disk.queue_depth` — are derived from the kernel's per-device I/O counters, the ones `iostat` derives `await`/`avgqu-sz` from, taking a Linux device to **exactly the 24-series cap**: the reserved headroom is spent, so the next vital of any kind is a decision about the cap itself. **No busy-percentage vital** — SSD/NVMe service many I/Os in parallel, so it pins at 100 % with headroom remaining, while queue depth keeps scaling and separates "busy but healthy" from "overloaded" (recorded so nobody adds it later for symmetry with `cpu.total`). **Worst device, per vital, independently** — the slowest device and the deepest queue routinely differ, and a mean across an idle data disk and a struggling system disk describes neither; per-device detail rides alert evidence, so the reduction costs no cardinality. Device filter = the kernel's whole-block-device listing (excludes partitions by construction) minus `loop*`/`ram*`/`zram*`, with `dm-*`/`md*` **included deliberately** (LUKS and RAID overhead is latency the user experiences). Service time ships a `.max` — a 5 s freeze moves a 3 ms average to 69 ms and pins the maximum at 800, which is what turns "the desktop froze" into "the freeze was I/O-bound" — while queue depth, already a time-weighted average over the interval, ships none. A **VM needs no special handling** and guest-observed latency (host contention + volume throttling included) is the reading worth having; a **containerized agent reports unsupported** because the kernel's disk counters are not per-container and cgroup I/O accounting carries no service time to substitute — its I/O-stall signal comes from `stall.io.*` (ADR-066). **Never a wrong number:** counter wrap/reset, a device appearing or disappearing between readings, and a zero-length interval yield no reading; a device that completed **no I/O has no service time** (0 ms would read as "instantaneous"), while an empty queue is a real measurement. Readings publish at **milli resolution** (the centi scale would round a 0.125 ms NVMe service time away) and are quantized at the source so store, live window and alert evidence quote one number. Every path resolves under an **injectable filesystem root** — container, kernel without the counters, both kernel column layouts and every malformed line are fixture directories, and no test reads the reference host's `/proc` or `/sys`; the "am I containerized" question moves into one shared resolver so two collectors cannot disagree. [ADR-067](../docs/adr/ADR-067-disk-performance-vitals.md) | 065, 066 | Accepted |
| 068 | System-event rules over a **polled** log, and the bounded edge alert sink — every edge signal until now was a number crossing a line, and a whole class of failure never presents that way (a task blocked two minutes, memory reclaimed by killing a process, a disk that stopped answering, a processor throttling itself). Four curated Linux rules read them from the journal beside a rolling per-service error count. Rules match on **exclusions** as well as alternatives, because every subsystem reports its recovery in nearly the words it reports its failure. The reader is a bounded on-demand read, so polls overlap by design and a **cursor** decides what fires: newer than the cursor fires, at its instant fires only if not already answered for, older never fires — a record arriving late is **lost rather than duplicated**, because an alert delivered twice costs more trust than one delivered never. A saturated poll is counted as an event in itself and the records it lost are **not** invented as a number; unplaceable records and services turned away by the tracking cap are counted separately. A record a curated rule explained does not also feed the per-service count. Maintenance **suppresses** its window rather than deferring it. One bounded sink per device drops its **oldest** entry when full and admits **20 alerts per rolling hour**, both limits counting what they cost; redaction happens before an alert exists, not before it is sent. [ADR-068](../docs/adr/ADR-068-system-event-rules-and-the-edge-alert-sink.md) | — | Accepted |
| 069 | Correlation moves **to the device**, and `/api/v1/devices/{id}/correlate` is removed outright — an alert says what crossed a line, and the question after it (what else moved) is now answered where the detail lives: the agent ranks its own dimensions over the event window against the stretch before it, and the ranking rides inside the alert. Central telemetry is a 60 s average per dim (ADR-065), so a central ranking would rank exactly what averaging destroys — a ten-second I/O collapse barely moves the minute it sits in — and the endpoint was an on-demand telemetry pull at the moment a fleet is least able to serve several. Three signals blend into one score in `[0, 1]`: KS distribution shift, the share of readings outside the baseline's band, and the mean shift against the baseline's own scale; the third earns its place because the first two saturate on any clean separation, so without it 0.40 ms → 0.44 ms outranks 0.4 ms → 40 ms. Weights are **behaviour, not tuning knobs**. The port was proven against a **frozen reference captured before deletion** — order, four numbers to 1e-12, **and the tie-break** (score → shape change → label), since dimensions tie routinely and a primary-key-only check passes on a fixture and diverges in production; a blend of doubles is compared within tolerance, not bit-for-bit, because an FMA may round once where two operations round twice. Degenerate windows yield a number, never a NaN: under two readings is left out rather than scored from nothing, a flat baseline has no band so any different reading counts, an all-zero baseline has no scale so any nonzero mean is a full shift, and a non-finite reading is dropped where it enters. Every run is bounded in dimensions, readings **and** wall-clock (checked between dimensions, so the first is always scored and the run always terminates), because it runs exactly when the machine is already in trouble; the read is an MVCC snapshot so the sampler is never blocked. The drag-to-correlate UX retires with no replacement — the chart cursor reads values, presets choose the window. [ADR-069](../docs/adr/ADR-069-edge-correlation-ranking.md) | — | Accepted |
| 070 | The alert-rule grammar, metric aliasing and explicit coverage — a rule was four fields over three metric names, which names dimensions the vitals contract renamed (ADR-065) and states none of the failures the curated pack exists for: DAL-WS-012's NVMe drifts 2 ms → 40 ms over a fortnight with no second crossing a line anyone would set in advance, and FS01's 02:00 backup at 28 I/Os deep and 3 ms is indistinguishable from a failing device at 28 deep and 40 ms by any single number **or by two rules that fire independently**. Three grammar additions and no more — **rate of change, window `max`/`mean`, cross-dimension conjunction** — comparators, `sustain_secs` and hysteresis unchanged, so a rule declaring none of them is the rule already on the fleet. An RMM agent executing server-supplied code is a supply-chain weapon aimed at every customer estate, so the grammar grows but never into a language: **a predicate whose cost cannot be computed statically is one the grammar cannot express**, `rule_cost` answers from a rule's declared fields alone (monotone in the window, summed over conditions), and window and term count are bounded constants — an ill-formed shape (window past the bound, a windowed predicate with no window, an instant one carrying a window it would ignore, too many terms) is refused by name, never attempted. Rejected: an expression language, a scripting hook, WASM — each moves the program's highest-impact gate from a build-time computation to a runtime hope. **Conjunction is one rule, not two** (two rules page twice for one condition and neither alert knows the other exists); it clears as soon as **any** side recovers past its own boundary, which with one condition is exactly today's hysteresis. **A partial window is not-enough-data, not a fire** — a warming-up rule is `active`, and firing on a partial window would page for every agent restart. Vitals names become canonical with `mem.used`/`disk.used` as **aliases** so pushed rules keep firing; a breach always names the canonical metric and the server resolves inbound the same way, so pre- and post-rename agents write one series. Alias resolution lives in **one place per side**, pinned by `go_control_push_alert_rules.bin` — the server generates it from its own vocabulary, the agent asserts the resolved set is exactly its own — so a name either side adds alone fails a test. Coverage (I8): per rule every device is `active`, `unsupported` or `unknown`, always summing to the fleet, with **`unsupported` a first-class state, not an error path** (reading "no kernel pressure information" as a failed evaluation yields `unknown` and hides a permanent platform gap behind a transient-looking label). It rides `AgentHealthSummary` additively, omitted when empty — the same shape an agent that predates it sends, so the existing golden is byte-identical and doubles as the forward-compat fixture — and rides the periodic anomaly summary too, since a calm machine emits no breach summary. **The three states are stored by their nature, not together**: `active`/`unknown` are liveness and stay in memory with `unknown` derived as fleet − reported (correct across a restart by construction; a stored `active` would keep claiming a decommissioned file server is watched weeks after it was unplugged), while `unsupported` is a durable fact about the estate — a customer's containerized agents can never evaluate a kernel-pressure rule, and that standing monitoring hole must not evaporate on a deploy or no coverage report over it is reproducible — so it is persisted alongside the rules schema under two rules: only `unsupported` is ever written (reporting `active` deletes the row, so nothing stored can go stale and steady state costs zero writes) and deleting a device erases its rows. Rejected: persisting all three with `last_reported_at` + a staleness window (buys nothing the split does not, pays a write per summary per device, and reintroduces the decommissioned-server lie). [ADR-070](../docs/adr/ADR-070-rule-grammar-and-coverage.md); working plan: [edge-first-b8-rule-grammar-and-coverage.md](plans/archive/edge-first-b8-rule-grammar-and-coverage.md) | 053, 065 | Accepted |
| 071 | Where a rule lives, who may change what about it, and how coverage is stored — a rule has **three layers separated by how mutable each is**. **Definitions are versioned YAML `go:embed`-ed into the server** (predicate, grammar, evidence spec, `group_by`, `group_window`, shipped numbers): a YAML typo is a build failure rather than a runtime 500, there is deliberately **no load-from-disk fallback**, and loading refuses an unknown field, a missing/out-of-vocabulary `group_by`, a metric the fleet does not collect, a comparator or predicate outside the grammar, a duplicate `(rule_id, version)`, and a rule shipping a default its own declared bounds would refuse. **Immutable per `(rule_id, version)`**, enforced against digests committed in `catalogue.lock` — an alert raised last week must still mean what it meant then, so editing a published definition is refused at load and a change is a new version; the digest covers what a rule *means*, not its prose, so a clearer summary is not a version bump. That placement is what keeps the program's highest-impact gate in CI: **`rule_cost` is bounded at build time, per rule *and* across the whole pack** (a per-rule ceiling alone does not bound an endpoint — enough rules just inside it still sink one), where a bad rule costs one engineer's minute instead of 5 000 endpoints. Definitions in Postgres would move that gate into a runtime API path where a validator bug is a production incident; definitions with **no** DB layer could express no kill switch, canary state or coverage (the wall Netdata hit before DYNCFG). **Bindings live in Postgres, keyed down the tenancy ladder**, carrying only values the rule declares tunable and validated against that rule's own bounds **on write** — a number nobody would want is refused while an operator can still see why. Resolution is device → site → organization → tenant → shipped and the ordering is **not** redefined here (it is `internal/settings`, ADR-064); **each parameter resolves independently**, so a customer-wide sustain survives one machine's retuned threshold — resolving a binding as a unit silently drops the sustain, the loss nobody notices until an alert does not arrive. So the file-server-at-95 case needs no special machinery: put the file servers in a site. A binding may also carry a **bounded tag selector** with an operator-set `precedence`; across rungs the narrower always wins, a targeted binding beats the rung's blanket one, and **two selectors at one rung sharing a precedence cannot be stored** (partial unique index), so resolution never depends on row order. **Rollout state lives in Postgres** because stopping a rule cannot require a deploy; **a customer with no row has not configured the rule and gets it as shipped** — absence is never read as "switched off", or a fresh customer is silently unmonitored while looking healthy — and `kill` is deliberately separate from `enabled` (an ordinary choice vs an intervention, distinguishable afterwards), filed on the customer so no narrower rung can undo it. **An unreadable store is reported, never replaced with the shipped defaults**: substituting them pushes rules ignoring a kill switch at exactly the moment somebody reached for one, and the agent keeps the ruleset it already holds. **Coverage is stored by the nature of each state, not together**: `active`/`unknown` are liveness and stay in memory (FS01, decommissioned three weeks ago, is `unknown` — exactly true, where a stored `active` claims 500/500 watched when the real number is 499), while `unsupported` is durable and persisted under two rules — **only `unsupported` is ever written** (reporting `active` *deletes* the row, so nothing stored can go stale and **steady state costs zero writes**) and **deleting a device erases its rows** by cascade, or a decommissioned container inflates the count forever. Read: `unsupported` = persisted rows for devices still in the fleet, `active` = memory, `unknown` = fleet − active − unsupported; the identity holds. All three tables carry forced RLS on `tenant_id` through **one shared `app_tenant_visible` predicate** (stated once so an edit cannot reach two tables and miss the third) plus a composite `(tenant_id, organization_id)` FK, so a row naming another tenant's customer is refused by the database rather than an application check. Rejected: persisting all three states with `last_reported_at` + a staleness window (buys nothing the split does not, pays a write per summary per device, reintroduces the FS01 lie); a rule-authoring UI. [ADR-071](../docs/adr/ADR-071-rule-catalogue-bindings-and-durable-coverage.md); working plan: [edge-first-b9-rule-catalogue-and-bindings.md](plans/archive/edge-first-b9-rule-catalogue-and-bindings.md) | 070, 064 | Accepted |
| 072 | Re-running a rule over the device's own history, and the reach that makes it worth doing — a rule arriving on a machine answers "is this happening now"; the operator asks "has this been happening" in the same breath, and answering it turns a newly written rule from a guess into something with evidence behind it (Contoso installs `disk-slow` after DAL-WS-012's NVMe failed; the useful reply is "four of your machines have been drifting like that one was", not "no drives are slow this second"). Answering it **centrally** means a third storage path holding ~5.5 GB/48 h at fleet scale for only 48 h of reach; the device already holds the answer in its minute-by-minute rollups, so the scan runs there and nothing is shipped to make it possible. **History is only asked what it can answer**: a rule about a span shorter than one stored minute is reported as one history cannot answer rather than evaluated at the wrong resolution, and for the rest the reading taken from each minute follows the rule's own question — a **sustained** rule reads the minute's least favourable reading (so a finding means every second of that minute was past the line), a rule with **no sustain** reads the peak (which answers "was it ever crossed" exactly), a window **max** reads the peak and a window **mean** the mean (both exact reconstructions). The direction is deliberately the one that cannot invent an episode: a scan reporting something that did not happen costs more trust than one missing a marginal case, because the findings are the evidence. **A finding is stamped with the minute it happened**, never scan time — scan time destroys grouping and makes a three-week-old freeze look like now — and one scan's findings share one grouping key, so a scan folds into **one incident per `(rule, scope)`** instead of paging once per episode. **Scheduled around the machine**: chunks bounded in stored readings, a stand-down between them sized from a duty budget well inside the agent's own, and a full stop during maintenance, on a busy host, and while the host disk fills — the disk threshold derived from the store's own footprint policy so the scan always stands down **before** the store starts trading history for space. **Resumable from a durable cursor** that re-reads a bounded window before it (long enough to rebuild everything the rule's state machine carries) while suppressing findings from before it, so a stopped scan produces exactly what an uninterrupted one would. **Scanned once per rule *version*, not once per push**: the server pushes the whole ruleset on every reconnect, so triggering on the push would re-raise every historical finding whenever a flaky link came back — the device remembers each scanned definition **in full** rather than as a digest, since a digest that changes with a toolchain does the same thing after an agent upgrade. Findings spend the device's ordinary alert allowance through the one bounded, rate-limited sink (ADR-068): a scan matching thousands of minutes is exactly the flood the ceiling exists for, and "but they are old" is not a reason to let it past. **Q11 measured, not divided**: a store driven through the production write path until its cap is evicting, then asked for its oldest surviving minute, at three cap sizes across a fourfold range with the linearity between them as the evidence for extrapolating to the shipped cap — **about seven months** at the current vitals shape (13 series at 1 Hz on a busy machine), at a little over two bytes per stored reading. That is *months*, not the years the central-versus-edge comparison originally claimed, and still two orders of magnitude past a 48 h central recorder, so the comparison holds and the claim is restated as measured. Rejected: a central flight recorder; on-demand pulls (a customer endpoint on the critical path of a console interaction, answering only for the device someone thought to ask about); retro evaluation at the raw one-second tier (exact, but evicted alongside the minutes and reaching a fraction as far); reading the mean for every rule (damps the spikes a peak rule exists to catch and cannot support a sustained rule's claim). [ADR-072](../docs/adr/ADR-072-retroactive-rule-evaluation.md); working plan: [edge-first-b10-retroactive-evaluation.md](plans/archive/edge-first-b10-retroactive-evaluation.md) | 070, 071, 068, 051 | Accepted |
| 073 | Staged rule rollout, the kill switch, and the endpoint's own budget — one curated rule reaches every machine of every customer that has it, so a rule that is **valid, affordable and wrong** (Contoso's `disk-slow` tuned 2 ms too tight, all 380 machines raising it through the nightly backup window) is the one thing here that can degrade an estate at once, and neither the closed grammar (ADR-070) nor the CI cost gate (ADR-071) touches it. A rule now reaches an estate in **stages — max(5 devices, 1 %) → 10 % → 100 %**, held **1 h / 6 h**, where **the hold is a minimum and never a trigger**: what advances a rule is the estate having stayed quiet (no organization alert-ceiling breach, no agent budget throttle trip, no evaluation error), and any of the three sends it **back one stage and restarts that stage's clock**, so a flapping signal cannot ratchet a rule up. A rule already at the canary **halts** there rather than being pulled off the machines proving it — ending it is a **kill**, a person's decision, not a timer's. The gate reads its signals through an **injected port, never a guess** (a gate on an approximated signal is worse than none: it looks like protection). **Membership is computed per machine from (device, rule, reach)** — no roster, no stored canary list — so it is the same answer on every server and across a restart and growing a rollout only ever *adds* machines; the rule id is in the function so two trials do not land on one handful of endpoints. The **five-machine floor is bounded by the fleet and holds for every partial stage** (1 % of twelve machines is nobody; a rollout must not shrink as it moves forward), and the estate is counted **only for customers mid-rollout**. **`kill` is a row, not a release**: effective at the next rule push for a connected machine and at reconnect for an offline one — reconnecting re-resolves the ruleset — and it outranks the stage. The **agent enforces its own ceiling over readings actually touched** (same figure as the pack's CI budget, measured over a minute), stopping the rule **per rule** (one expensive rule must not silence the cheap ones), **hard** (no retry until a *different* rule arrives, so a flaky link cannot re-spend the allowance) and **reported** as a fourth coverage state, **`throttled`** — memory-resident liveness, deliberately not `unsupported` (a mistuned rule is not a permanent platform gap on a customer's remediation list). Rejected: advancing on the hold alone (an hour's patience, then the fleet); a stored canary roster (stale, migrated, server-dependent); reverting to zero (pulls the rule off its own evidence); notifying on a revert (trains people to ignore it); a budget relative to the declared cost (unfalsifiable — retention is bounded from the same declaration, so it could never fire). [ADR-073](../docs/adr/ADR-073-staged-rule-rollout-and-the-endpoint-budget.md); working plan: [edge-first-b11-rollout-safety.md](plans/archive/edge-first-b11-rollout-safety.md) | 070, 071, 072 | Accepted |
| 074 | The alert store, accounted ingest and the erasure cascade — an alert is the only carrier of the detail behind a signal and there is **no path for asking the endpoint again**, so evidence is a `bytea` column on the alert row written in the same statement (not a side table or an object key, both of which make "the alert is here and its evidence is not" reachable) and its cap plus the codec-always-named rule are **check constraints**, since an immutable unfetchable blob that slipped past an application check would sit there forever. Identity is `(device, rule_id, rule_version, window_start)` rather than the id the device chose — an agent that lost its local store picks a new one and would duplicate everything it still had to send — so a reconnect replay is a **no-op, not an error and not a second row**. The hourly ceiling is **500 per customer** measured over a **rolling** hour and evaluated **inside the insert**, because a count taken beforehand and an insert taken afterwards see different rows and a storm on several connections at once is exactly when that gap opens; per customer because at the tenant Contoso's bad night would spend Fabrikam's budget, and silencing an estate's detection is worse than the storm. Suppression is **counted twice and folded once**: a typed drop so the ingest ledger still balances, `opengate_alerts_suppressed_total` for the operator, and one storm incident carrying the count — a number on a dashboard is not a room in the triage queue. Alert timestamps are **refused, not clamped** (the window start *is* the identity, so clamping would make one alert land on a different row every reconnect); evidence is **inflated under a bound** before it is believed, since a codec check cannot tell a valid blob from a corrupt one. A device erasure **restates** `occurrences`/`device_count` from surviving rows **before** the cascade runs — recomputing not subtracting, because a purge is resumable — and closes an emptied room with a `resolution` event and **no cause code** (`false_positive` is a person's answer and the channel that retunes a rule). Rejected: evidence in an object store; the ceiling at the tenant; clamping alert clocks like telemetry's; trusting a codec name without decoding; subtracting the counts. [ADR-074](../docs/adr/ADR-074-alert-store-accounted-ingest-and-the-erasure-cascade.md); working plan: [edge-first-c2-alert-store-and-ingest.md](plans/archive/edge-first-c2-alert-store-and-ingest.md) | 068, 071, 054 | Accepted |
| 075 | Grouping, the incident lifecycle, and the hold that closes a quiet room — ADR-074 gave incidents a table and an index but nothing that fills them, so every ordinary alert landed with `incident_id` null and the storm room was the only room anything opened. That gap **is** the product question: Contoso's 02:41 driver rollout is 312 rows nobody on call reads, and "forty machines, since 02:41" is a thing a person acts on. **Grouping has two axes and only one is obvious** — how *wide* a room is (machine / office / customer) is the one everybody implements; how *long* firings stay one room is what carries WS-4471's month of daily freezes, where no single freeze is worth a callout and **the pattern is the diagnosis**, so the same thirty alerts under half an hour are thirty individually-dismissible rooms. Keyed on **`rule_id`, never `rule_version`** (retuning a rule must not fork the room somebody is working in); two *different* rules on one condition stay two rooms. **The customer is the widest a room may be** — at the tenant, Contoso's rollout and Fabrikam's outage share a room with no correct assignee — and a rule attempting to group above it is refused by test, since an unreachable-by-convention ceiling is how a ceiling comes back. **`mount`/`metric` do not decide the room**: they say which volume a firing was about, a property of the alert, and two rooms for one machine is a worse answer than one. The **scope key is derived server-side from the machine's own ladder**, never accepted from the endpoint (which could name another customer's room), and an unfiled machine narrows to itself rather than pooling with every other unfiled machine. **The window is measured against the room's own span, not the wall clock, and is two-sided** — a retro scan emits a month of findings in one second and newest-first, so a clock-judged or forward-only fold fragments it into thirty; a finding older than the room's whole span is **stored and filed under no room** (not part of a live room's story, and the room is not stale). **An alert past the window closes the lapsed room on its way past**, stamped at the instant it *became* closeable, which makes the sweep a matter of promptness rather than correctness. **`reopen_window` has no override** — ADR-074 anticipated one, and working it through, longer leaves a room open that an arriving alert may not join while shorter closes a room whose next occurrence is still due; the grouping window is the one number that makes auto-resolve and grouping agree by construction, and it is read **live** rather than frozen onto the row. **Counts are restated from the room's own alerts, never incremented** (two concurrent increments read one starting value; an erasure's rows just leave), which is also why a resumed purge is safe twice; `occurrences`=312 and `device_count`=40 are asserted in **one** test because a version conflating them passes every one-machine-one-alert fixture. **Concurrency is the database's**: `FOR UPDATE` plus an upsert whose conflict clause blocks, not a Go mutex that holds only while one process writes. **A low-severity observation opens nothing alone** — a fleet event no host breaches on is visible only through co-occurrence, so it waits, room-less, for a second *distinct* machine inside the window and then the waiting readings join the room they opened; at device scope co-occurrence is impossible, so such a rule never raises one. Lifecycle `new → acknowledged → investigating → resolved` with forward skips and backward moves stopping at the queue; **an unchanged status is not a transition** and is refused. **Resolution requires a cause code** from the closed set — `false_positive` is the only channel saying which curated rule needs retuning — and each refusal is its own typed error because each is a different mistake. **Reopen is a door, not a transition**: it withdraws the answer already given and yields with a named error when the condition has since recurred into a fresh room. The **sweep is the one statement naming no tenant** (it is asked about all of them; a stale room in an idle tenant still sits in that tenant's queue), runs admin-scoped, is **clock-injected never slept**, and its exception is pinned by test. **Maintenance keeps a room, checked inside the closing statement** — a device entering maintenance between check and close would have its incident closed by the very silence maintenance causes — and only for a room about *that* machine, or one parked device pins an estate's rooms open. A room whose rule this build no longer ships is left for a person; the storm room holds its own rolling hour. No notification on any transition. [ADR-075](../docs/adr/ADR-075-incident-grouping-lifecycle-and-auto-resolve.md); working plan: [edge-first-c3-incident-engine.md](plans/archive/edge-first-c3-incident-engine.md) | 074, 071, 072, 056 | Accepted |
| 076 | Aggregate platform metrics are **O(rules)**, and the alert rate becomes a **measured gate** — ADR-074 and ADR-075 built the machinery that turns a machine's report into a room a person works in, and neither gave any way to see the machinery itself behaving. Two problems sit inside that. A rule that is **valid, affordable and wrong** is the one thing that degrades every estate at once (Contoso's `disk-slow` tuned two milliseconds too tight is well-formed and cheap and raises on 380 machines through the nightly backup window); the staged rollout contains it, but a person watching a rollout had nothing to look at. And **0.2 alerts/device/day has never been measured** while three decisions rest on it: the yearly evidence projection, the 500/customer/hour ceiling (set as ~12x a rate nobody observed), and the trigger for reopening per-device alert series. Five series on the existing `/metrics`: `alerts_created_total{rule_id}`, `alerts_suppressed_total{reason}`, `alerts_open`, `incidents_open{status}`, `rule_coverage{rule_id,state}`. **Cardinality is the binding constraint, bounded by construction rather than by care** — no tenant, customer, machine or incident label anywhere: a rule pack is a handful of entries fixed for a release and a fleet is every machine every customer runs, so one entity label makes the platform's own monitoring the largest cardinality source in the system it watches, growing with exactly the dimension nobody watches it for. The `rule_id` label is bounded by the **shipped catalogue**, not the endpoint (ids travel to the agent and come back on alerts and coverage reports, so unbounded there means the device decides the server's cardinality); anything outside folds into one catch-all, the same bound the WS-19 breach path applies to its `metric` label. **Every value of every closed vocabulary is exported, zero-valued ones included** — a missing series reads as "no data", which is not "none open", and the two are indistinguishable exactly when somebody checks whether a rollout raised anything. The two gauges are **refreshed on a timer, never computed in the collector** (counts over tables that only grow, on an endpoint several things scrape): the open-work read is one statement joining open incidents to their alerts through the incident index so its cost tracks the queue not the table, and the coverage read counts fleet and blind spots in **one pass** because two reads a moment apart report a share nobody's estate was ever in. Both run admin-scoped over every tenant, like the stale-room sweep — a queue in a tenant nobody is serving is still a queue. **A failed read leaves the previous answer standing**: a database briefly down is not an empty triage queue nor a fleet where every machine is unknown, and both of those are real alarming states. **Only a stored alert counts as created** — a replay changed nothing and a refusal stored nothing, and counting either inflates the rate the ceilings are sized against. Q12 is `increase(alerts_created_total[24h])` over the fleet, the fleet read off the coverage gauge whose four states sum to it; the pack does not advance past canary until a **real population** produces the number, and a synthetic soak figure is reported as a harness figure naming its pack and fixture or not at all — publishing an assumed number as a measured one is the failure the gate exists to prevent. Rejected: per-rule series in VictoriaMetrics beside the per-device edge series (fleet aggregates about the platform, not readings about a machine); computing the gauges in the collector; a `tenant_id`/`organization_id` label so a customer's queue shows in Grafana (breaks the one property these series are built on — the customer's own view is the API, already scoped to them); publishing 0.2 as measured once the counters shipped. [ADR-076](../docs/adr/ADR-076-aggregate-platform-metrics-and-the-measured-alert-rate.md); working plan: [edge-first-c4-aggregate-metrics-and-q12.md](plans/archive/edge-first-c4-aggregate-metrics-and-q12.md) | 074, 075, 073, 071 | Accepted |
| 077 | **The investigations API: an operational boundary and a keyset triage queue** — ADR-074/075/076 gave alerts a home, folded them into rooms and put the machinery on a dashboard; nobody could **open** any of it, so Contoso's 02:41 rollout stayed a row with no reader. Three questions each had a wrong answer that passes every hand-written test. **Who may act:** reading the queue, moving an incident, assigning and commenting are *operational* work on the tenant's own resources — the same class as a restart or a maintenance window — so **tenant membership is the whole gate**; rule bindings and rollout stay admin-gated as configuration. `organization_id` **narrows, never permits**: every tenant member may look at any customer, but a narrowed read returning another customer's row refuses nothing and simply shows Contoso's estate to somebody looking at Fabrikam's — a quieter failure than a breached wall, tested route by route. Both boundaries answer **"no such incident"**, so neither is discoverable by probing ids, and `requireIncidentInScope` joins the ADR-027-recognised named guards so a future handler that forgets it is refused at commit time. **How it pages:** **keyset, never offset** — an offset over a queue being written to loses the row that shifts past the boundary, silently, and that row is an incident nobody ever sees; `(last_seen, id)` because two rooms can be last seen in the same microsecond, wrapped opaque so no client constructs a position no index answers. Migration `015` adds the two orders the queue is actually read in (customer-leading, tenant-leading) plus `alerts(incident_id, device_id)` for the device strip, which **subsumes and replaces** the incident-only index. The customer read and the whole-tenant read are **two statements**, because they are answered from two indexes and an optional predicate leaves the choice to a guess. **Q10 is a plan assertion, not a stopwatch**: at 10 000 open rooms every filter combination the UI can produce is `EXPLAIN`ed and refused a sequential scan — a timing test measures the laptop, and a seq scan passes at ten thousand and misses the budget on the estate this is sized for. **How much one read carries:** evidence is up to 64 KB per alert and a room folds hundreds, so neither the queue nor the detail embeds it; the detail states which alerts have evidence, under which codec and at what byte cost, and both its lists are bounded **with their totals**, so "312 alerts across 40 machines" never quietly becomes "200". Decoding is **server-side** (DEFLATE around msgpack; a browser has neither) and answers two ways only — this is what the machine sent, or this cannot be read and here is why; an unknown codec is its own `422` because it is the additive case working (newer agent, older server), and handing back bytes under a codec nobody claimed would put invented detail in front of whoever is deciding what happened to a customer's machine. Nothing is redacted here and nothing needs to be — the machine redacts before sending (ADR-049) — and a test pins the response's **field set** so a later change cannot fold in something never redacted. The device page's strip is the **same query** narrowed to the rooms holding an alert that machine raised, not a second list that drifts. `GET /rules` is a **coverage view, not an editor**: the four states always sum to the fleet size, counted once and passed in so the share and its total come from one moment, and the predicate/terms/clear are deliberately absent because a read-only surface carrying them invites the question of how to edit them — the one thing this product does not do. [ADR-077](../docs/adr/ADR-077-investigations-api-and-the-keyset-triage-queue.md); working plan: [edge-first-c5-investigations-api.md](plans/archive/edge-first-c5-investigations-api.md) | — | Accepted |
| 078 | **The triage workspace reads a snapshot** — ADR-077 gave the queue an API and nothing read it, so Contoso's 02:41 driver rollout was 312 alerts folded into one incident that existed only as JSON. Grafana has no ingress cluster-wide, so the surface is the web client. An incident in `new` **is** the queue — no promotion step, nothing to convert. The room's request boundary is **a test, not a convention**: every request it issues is recorded and checked to be under `/api/v1/investigations`, so the one convenience fetch an operator wants (the machine's *current* CPU, one call away) fails the suite rather than passing review. Evidence is frozen at write time and what is not in the snapshot is recorded nowhere, so the room **states an absence** — parts the machine recorded nothing for, a size cap that cost the blob something, a room whose alerts left with an erased machine — instead of a gap a reader takes for a pending load. The four-status lifecycle is mirrored client-side and drives which buttons exist, so an illegal move is never offered; the vocabularies are typed with `satisfies` against the generated API types, so a value the spec grows fails to compile rather than rendering as a wire string. All **four** coverage states are shown against the fleet size and a split that does not add up is itself the finding. Evidence series are inline SVG, not a charting engine — the lazy uPlot chunk stays on the device page and the initial bundle stays in budget. One latent bug closed on the way: every array query parameter in the spec is non-exploded and the client was exploding them, so a second value would have been dropped in silence. | [ADR-078](../docs/adr/ADR-078-the-triage-workspace-reads-a-snapshot.md) | 2026-08-17 |
