# Architecture Decision Records

<!-- Last updated: 2026-07-02 -->
<!-- Compact index. Full ADR text lives in /docs: -->
<!--   - ADR-001 through ADR-012: docs/Architecture-Decision-Records.md (frozen historical log) -->
<!--   - ADR-013 onward:          docs/adr/ADR-NNN-title.md (one mutable file per decision) -->
<!-- Per-file ADRs (013+) are mutable — edit in place to keep them current (ADR-036). -->
<!-- Supersede with a new ADR only for a genuine decision change. The 001–012 log stays frozen. -->
<!-- Consolidated (ADR-036): former amendment ADRs folded into their parents' Amendments sections — -->
<!--   ADR-028 + ADR-032 → ADR-019; ADR-026 → ADR-020; ADR-031 + ADR-033 → ADR-023. -->
<!-- See docs/README.md for the full convention. -->

| ADR | Decision | Phase | Status |
|-----|----------|-------|--------|
| 001 | MessagePack wire protocol with internally tagged enums, `[type][len][payload]` framing | 1 | Accepted |
| 002 | Golden file tests — Rust generates, Go verifies | 1 | Accepted |
| 003 | SQLite WAL via `modernc.org/sqlite` (pure Go, no CGo), `MaxOpenConns(1)` | 2 | Superseded by ADR-014 |
| 004 | ECDSA P-256 self-signed CA, CSR enrollment via `/api/v1/enroll/{token}`, TLS 1.3 | 2 | Accepted |
| 005 | QUIC mTLS via quic-go — server opens control stream (workaround) | 4 | Accepted; rationale superseded by ADR-037 |
| 006 | Platform traits with null impls for headless/CI environments | 5 | Accepted |
| 007 | VAPID Web Push, keypair persisted to `{dataDir}/vapid.json` | 10 | Accepted |
| 008 | `aarch64-unknown-linux-musl` cross-compilation via `cross` | CD-C | Accepted |
| 009 | Cosign keyless signing for container images (GitHub OIDC) | CD-E | Accepted |
| 010 | Separate `device_hardware` table for on-demand hardware inventory; `RestartAgent`/`RequestHardwareReport`/`HardwareReport` control messages | 12+ | Accepted |
| 011 | On-demand device logs via control path (`RequestDeviceLogs`/`DeviceLogsResponse`/`DeviceLogsError`), individual rows in `device_logs` table with 5-min cache TTL, SQL-level filtering | — | Accepted |
| 012 | SonarCloud quality gate as hard merge block — Clean-as-You-Code model, coverage/ratings/hotspots enforced on new code | — | Accepted |
| 013 | Docs live in-repo under `/docs`; wiki deprecated; link-over-paraphrase; original ADR immutability rule superseded by ADR-036 | — | Accepted; amended by ADR-036 |
| 014 | PostgreSQL 17 via `pgx/v5/stdlib`, native types (`TIMESTAMPTZ`/`UUID`/`JSONB`), app Helm chart StatefulSet, production `oci-bv` PVC, off-cluster `pg_dump` backups via Object Storage PAR, `postgres_exporter` metrics | 13a | Accepted (supersedes ADR-003) |
| 015 | IaC defense-in-depth: Checkov (terraform/dockerfile/github_actions) + Hadolint + Trivy + gitleaks all run; `.checkov.baseline` is the single suppression surface, no inline `checkov:skip` permitted | S2 | Accepted |
| 016 | Bidirectional goldens (Go→Rust reverse fixtures alongside existing Rust→Go) + `.meta.json` sidecars carrying `protocol_version`/`format` per `.bin` | C1 | Accepted (extends ADR-002) |
| 017 | CI gates consolidation: inline IaC gate into ci.yml with hard-block on direct-push destroys; drop in-repo `docs/mutation-history.jsonl` (workflow artifact retained for audit; trend-store clause superseded by ADR-038); remove `environment: staging` from `load-test.yml` | — | Accepted; trend-store clause superseded by ADR-038 |
| 018 | OCI Bastion (STANDARD, target = OKE worker-node subnet, IAM-gated, 3h session TTL) is the human node-SSH path; `make tunnel` is Kubernetes port-forward to Grafana; CI/CD uses `oci-kube-setup` and the OKE API, not Bastion/SSH | — | Accepted |
| 019 | PMAT (`pmat@3.17.0` exact pin) as augment-only quality overlay at three separately-togglable points: MCP server with 7-tool read-only allow-list, precommit `pmat tdg --threshold B+` step, nightly full-repo workflow → VictoriaMetrics/Grafana with ≥3-pt repo-score + single-file TDG-slip Telegram alerts; no existing gate replaced; Kaizen `--dry-run` only. **Amendments** (folded in, formerly ADR-028/032): the verified `pmat@3.17.0` CLI/MCP mapping (`check-quality --min-grade B+`, the 7-tool MCP allow-list, 0–100 repo-score, baseline 64.5/C on `d3f0373`) + the TDG gate excluding gofmt-only Go test files | — | Accepted (incl. amendments; trend store amended by ADR-038) |
| 020 | Modular monolith — full hexagonal across all modules (overrides plan's layered-hybrid recommendation); earned-port rule; 12 Go modules with named inbound + outbound ports; AMT keeps `internal/mps` as `internal/amt/transport` (no merge); CI gates `go-arch-lint` + `cargo-deny`/`cargo-modules` + `eslint-plugin-boundaries`/`dependency-cruiser` start in warn, flip to error auto at zero-violations (no soak); opportunistic-trigger rollout; ADR-021–024 follow. **Amendment** (folded in, formerly ADR-026): cargo-deny strict policy — HTTP-stack inventory (direct `reqwest`+`tokio-tungstenite`), 28-entry transitive-duplicate `[bans.skip]` allowlist, `publish=false`+`allow-wildcard-paths`, and the §5.4 `warn→deny` flip executed | — | Accepted (incl. amendment) |
| 021 | Go per-aggregate repositories extracted from monolithic 56-method `db.Store` (target <30 residual); 12 per-domain `*Repository` outbound ports owned by consuming modules; transactions owned by use-case layer, threaded via `context.Context` through `dbtx` helper; per-domain `query.go` for cross-aggregate read models; opportunistic migration order with `cert` as the lowest-fan-in pilot | — | Accepted |
| 022 | Web per-feature state — 11 of 12 Zustand stores move into `features/<x>/state/` with `index.ts` barrel exports; only `useAuthStore` stays global (hydration-gated); `useToastStore` → `lib/feedback/`; `eslint-plugin-boundaries` (app/feature/lib element groups) + `dependency-cruiser` snapshot enforce cross-feature access via barrels only; starts in warn, auto-flips at zero violations | — | Accepted |
| 023 | Relay session-registry seam — retain the slim `SessionRegistry` (`SaveSession`/`DeleteSession`/`Ping`) with `InProcessRegistry` as the only adapter; live connection pairs remain local and registry errors stay non-fatal. The Redis/Sentinel adapter and cross-server proxy amendments (formerly ADR-031/033) are reverted and removed; their rebuild requirements live in [`docs/Multiscale-Readiness.md`](../docs/Multiscale-Readiness.md) | — | Accepted; distributed amendments reverted |
| 024 | Rust agent `ControlMessageHandler` trait around the inner `handle_control` fan-out (~10 methods grouped into MouseHandler / KeyboardHandler / FileHandler / WebRTCHandler / SwitchHandler / TerminalControlHandler); outer 4-branch `handle_frame` stays as thin multiplexer (earned-port rule not satisfied); staged carve-up with cargo-mutants per-PR gate preserving 89.5% baseline; corrects plan's wrong hotspot location (handler.rs:17-46, not session/mod.rs) | — | Accepted |
| 025 | CD pre-flight digest check inside `resolve-tag` (crane against GHCR + actions/cache staging state) short-circuits `deploy-staging-k8s` when target digest matches prior cached deploy AND `deploy/**` is unchanged; fail-open to full Helm rollout on cache miss/ambiguity; `workflow_dispatch` always proceeds; production deferred | — | Accepted |
| 027 | Adversarial pen-test gate (Semgrep) — fail-closed custom-rule gate over the diff at commit hook + gauntlet + blocking CI job (required `merge-to-main` check); 6 v1 rules (missing-admin-gate, path-traversal, token-in-log, plaintext-secret-column, IDOR-advisory, OpenAPI spec-drift); `semgrep==1.108.0` pinned via `scripts/install-semgrep.sh`; diff-only via `--baseline-commit` grandfathers pre-existing findings; HIGH blocks / MEDIUM advisory; no inline suppressions (path-glob `.semgrepignore` or per-rule `paths.exclude` only); no LLM in v1 | — | Accepted |
| 029 | Test determinism — no silent skips. Postgres-backed tests auto-provision a throwaway `postgres:17-alpine` via testcontainers-go (`server/internal/testpg`, leaf pkg; `URL()` for `TestMain`, `BaseURL(t)` fails loudly) when `POSTGRES_TEST_URL` is unset — CI/dev external DB still honored; `max_connections=400` matches `make postgres-test-up`. Cross-language skip-guard hook (`pretooluse-test-skip-guard.sh`, PreToolUse, no bypass) blocks new skip/focus markers in test files: Go `t.Skip*`; web `it/test/describe.skip/skipIf/only/todo/fixme` + `xit/xdescribe/xtest/fit/fdescribe(`; Rust `#[ignore]`. Golden generators refactored to always-run (write committed fixtures only under `GENERATE_GOLDEN`). Rule: `.claude/rules/tests-determinism.md` | 13b | Accepted |
| 030 | Kubernetes adoption — OKE BASIC with the [`deploy/helm/opengate`](../deploy/helm/opengate/) chart; ingress-nginx + cert-manager HTTP edge; in-cluster PostgreSQL; direct node `hostPort` exposure for QUIC/MPS; external Secret; rendered-manifest validation through `make lint-k8s`. Multi-node L4 templates and distributed relay dependencies are not part of the current chart | 13b | Accepted |
| 034 | Shared server keys via the existing Kubernetes Secret — production mounts the enrollment CA, VAPID, and update-signing files read-only into an `emptyDir` `/data`, skips the server-data PVC, and preserves identity across redeploys. The KEDA `ScaledObject` and PodDisruptionBudget portions are reverted and removed; session-aware autoscaling remains a rebuild requirement in [`docs/Multiscale-Readiness.md`](../docs/Multiscale-Readiness.md) | 13b | Accepted; autoscaling/PDB reverted |
| 035 | OKE free-tier block-volume remediation (450→200 GB, at the cap) — count, not size, is the lever (50 GB OCI minimum). **Keep 3 block PVCs:** prod Postgres + VictoriaMetrics + Loki. **Drop 5:** (1) `uptime-kuma` removed entirely → external uptime SaaS (also catches whole-node outages), monitoring chart now exposes no ingress; (2) Grafana `/var/lib/grafana` → `emptyDir` (all config provisioned from ConfigMaps, only annotations/alert-history lost); (3+4) staging Postgres + server `/data` → `emptyDir` via new `postgres.storage.persistent` / `server.dataVolume.persistent` flags (default true; staging false, **production unchanged**); (5) backups → OCI Object Storage via a **write-only PAR** (`BACKUP_PAR_URL` Secret), CronJob split into a `pg_dump` init container + a `curlimages/curl` PUT container sharing an emptyDir (postgres image has no curl), retention via an OS lifecycle policy. Prod data/storage untouched; off-cluster backups survive cluster loss. Manual follow-ups (SaaS monitors, bucket/PAR/lifecycle, delete 5 freed PVCs, Cloudflare `status.*`) in chart NOTES.txt. Working plan: `.claude/plans/archive/oke-block-volume-free-tier.md` | 13b | Accepted |
| 036 | Per-file ADRs (013+) are mutable current-state records; frozen ADR-001–012 log remains immutable; supersede only for decision changes; ADRs may link archived plans but not active plans | — | Accepted (supersedes ADR-013 immutability clause) |
| 037 | Client-first QUIC handshake + fast-path reconnect: agent opens and writes first, auth is mTLS-only, `0x14` saves a round-trip, 1-RTT session resumption adopted while 0-RTT is deferred; ADR-005's server-opens rationale is superseded | 4 | Accepted |
| 038 | VictoriaMetrics is the canonical numeric CI-trend store through the shared kubectl transport; Loki is logs-only; branch-hosted benchmark/browser-performance history and Loki trend pushes are retired; global VM retention applies | — | Accepted (supersedes ADR-017/019 trend-store clauses) |
| 039 | Diagrams as Code Part 2 (extends DD-E): native Mermaid **C4** (`C4Context`/`C4Container` in Architecture.md) gated by a **mandatory GitHub render-verification** with a documented `flowchart`/`sequenceDiagram` fallback; **CI-only** Mermaid syntax validation via the official parser pinned in `tools/mermaid-validate/` (`docs-validate.yml`, path-filtered, isolated from `web/` + the local gauntlet); **drift-guard hardening** — `docs-diagrams.test.sh` pins every diagram-bearing doc + a total-fence floor, plus a CODEOWNERS/PR-template nudge; a **diagram coverage standard** in README + new deploy/CI-CD/session-lifecycle diagrams. Working plans: `diagrams-as-code-part-2.md` + `diagrams-d1..d5.md` | — | Accepted (extends DD-E) |
| 040 | Service-extraction decision lens (Balanced Coupling) — score each module on integration strength (proven contract-level only for the constrained leaves `go-arch-lint` lists, not the `other` catch-all candidates), fan-in (distinct production importers), and volatility (120-day churn); extract only when strength is contract-level AND fan-in is low AND a concrete scale/availability/isolation driver exists, else decouple-in-place / leave alone. Advisory, not a CI gate. **Current verdict: no extraction** (single-node free-tier, no driver); `db` decouples in-place via ADR-021, `api`/`agentapi` stay (latency, low fan-in), relay stay (multi-server removed, churn self-inflicted). Replaces the former `service-extraction-roadmap.md` plan note | — | Accepted |
| 041 | Postgres RLS multi-tenancy — default organization backfill, `org_id` on tenant tables, forced RLS policies, tenant context from JWT `org` through `dbtx.Scoped`, policy-based admin bypass, dedicated non-superuser/non-`BYPASSRLS` Helm runtime role; per-repo deny tests + automated dump/restore/down migration rehearsal | Edge-Sentinel WS-0 | Accepted |
| 042 | Control protocol forward compatibility — unknown control types tolerated both directions and new server→agent variants capability-gated through `AgentRegister` (`HardwareInventory`, `DeviceLogs`); bidirectional unknown-type goldens | Edge-Sentinel WS-1 | Accepted |
| 043 | Edge Sentinel local ML sampler — clean-room pure-Rust k=2 ensemble/window/sampler/redaction kernel, process top-N by rank, no full cmdline by default, always-on agent background task; allocation/RSS guards + Criterion ARM-footprint bench harness | Edge-Sentinel WS-2 | Accepted |
| 044 | Edge Sentinel server telemetry ingest — numeric samples go to VictoriaMetrics through a scoped server client that injects `org_id`; process snapshots go to forced-RLS Postgres `device_processes`; agent writes are scoped by the server-resolved device org, not payload `org_id`; telemetry has cap/interval/drop arbitration and VM stream aggregation | Edge-Sentinel WS-4 | Accepted |
| 045 | Load-test regression gate from VictoriaMetrics read-back — supersedes ADR-038's load-test visibility-only consequence: `load-test.yml` now splits run/publish jobs, checks per-series latency/rps/error-rate regressions through `scripts/lib/vm-query.sh`, keeps p99 advisory-only, pushes current rows before Telegram/fail-red, and leaves normal merge CI ungated | — | Accepted |
| 046 | Edge Sentinel raw-log broker — retire the central `device_logs` cache (dropped in `004_retire_device_logs`) and broker every raw pull transiently: `GET /devices/{id}/logs` blocks on a per-connection single-flight waiter for the agent's bounded `DeviceLogsResponse`, redacts server-side, and streams it back with nothing persisted; elevated (admin) gate + `device.logs.read` audit on every pull; `409` busy / `504` timeout; log-rate dims still go to VM scoped. Working plan: [edge-sentinel-ws-11-log-server.md](plans/archive/edge-sentinel-ws-11-log-server.md) | Edge-Sentinel WS-11 | Accepted |
| 047 | Web telemetry chart engine — thin uPlot (canvas-2D) adapter is the only module importing uPlot (typed-array `setData`, `ResizeObserver`, engine-agnostic swap seam), code-split into a budgeted `charts` chunk; device-detail anomaly panel + per-family timelines + drag-to-correlate drill-down; scalar-only health badges on the grid/dashboard (no per-device series); honest `min_max_source` band provenance (central VM avg-only); logs explorer over the WS-11 broker with level/time/search filters + page facets and a metrics↔logs correlation jump. [ADR-047](../docs/adr/ADR-047-web-telemetry-chart-engine.md); working plans: [edge-sentinel-ws-6-web-ui.md](plans/archive/edge-sentinel-ws-6-web-ui.md), [edge-sentinel-ws-12-log-web.md](plans/archive/edge-sentinel-ws-12-log-web.md) | Edge-Sentinel WS-6/WS-12 | Accepted |
| 048 | Edge Sentinel endpoint-log model (edge-stored, server-proxied) — log-*rate* signal is centralized in VictoriaMetrics as `opengate_edge_metric_avg{dim="log.rate.<source>.<field>"}` (bounded cardinality, no message text); raw lines stay at the edge as the durable store of record and are read only on-demand through the transient broker; a central Loki log lake is rejected (secret density + 200 GB cap + weak duplicate of the host's own log store). [ADR-048](../docs/adr/ADR-048-edge-sentinel-endpoint-log-model.md); working plan: [edge-sentinel-ws-13-log-privacy-ops.md](plans/archive/edge-sentinel-ws-13-log-privacy-ops.md) | Edge-Sentinel WS-13 | Accepted |
| 049 | Edge Sentinel raw-log privacy — layered controls with redaction as **defense-in-depth**, not the primary boundary: no central persistence + admin-elevated + `device.logs.read` audit + bounded exposure are structural, while **two independent redaction guards** (agent-edge and server-backstop) scrub a shared secret corpus (bearer/basic, key=value/key: value creds, JWT, AWS/GCP keys, credentialed connection strings, PEM keys) kept in sync as paired tests; redaction is explicitly best-effort. [ADR-049](../docs/adr/ADR-049-edge-sentinel-raw-log-privacy.md); working plan: [edge-sentinel-ws-13-log-privacy-ops.md](plans/archive/edge-sentinel-ws-13-log-privacy-ops.md) | Edge-Sentinel WS-13 | Accepted |
| 050 | Edge Sentinel host log-reader sourcing (no-GPL) — read host log sources via first-party CLIs parsing structured output (`journalctl -o json`, PowerShell `Get-WinEvent \| ConvertTo-Json`, agent self-logs direct) instead of linking `libsystemd` (LGPL/GPL, incompatible with the workspace Apache-2); collectors are bounded, always-on, and no-op off-platform; output-format drift is a fixture-tested parser concern, not a linkage/ABI one. [ADR-050](../docs/adr/ADR-050-edge-sentinel-log-reader-sourcing.md); working plan: [edge-sentinel-ws-13-log-privacy-ops.md](plans/archive/edge-sentinel-ws-13-log-privacy-ops.md) | Edge-Sentinel WS-13 | Accepted |
| 051 | Edge Sentinel local TSDB substrate (WS-14a bake-off) — pick **`redb`** + the bespoke Gorilla tiering/compression layer as the offline store substrate: "simplest that clears the bar". Measured on one fixture corpus in [`agent/crates/edge-tsdb`](../agent/crates/edge-tsdb): bespoke append-only (A) is ~2.8× denser (2.6 vs 7.3 B/sample) but owns ~500 lines of crash/recovery code, whereas redb inherits 2-phase-commit/quick-repair/`check_integrity` with ~165 glue lines, no background threads, and zero transitive deps (MIT/Apache). `fjall`/`tsink` fail the dependency sub-gate empirically (both add a 3rd `hashbrown` major → `multiple-versions=deny`, plus compaction threads). A stays as density/fallback reference, C as the no-persist control. Working plan: [edge-sentinel-ws-14a-offline-tsdb-spike.md](plans/archive/edge-sentinel-ws-14a-offline-tsdb-spike.md) | Edge-Sentinel WS-14a | Accepted |
| 053 | Edge Sentinel declarative threshold alerts (WS-19) — per-tenant threshold rules evaluated locally beside the ML anomaly detector: a rule is a metric gauge (`cpu.total`/`mem.used`/`disk.used`) + comparator (`Gt`/`Lt`/`Gte`/`Lte`) + fire threshold + hysteresis `clear` + `sustain_secs`; a pure evaluator ([`alerts`](../agent/crates/mesh-agent-core/src/alerts/)) steps Clear→Pending→Firing so a breach must sustain before firing (rising-edge flap suppression) and stays firing until it recovers past `clear` (falling-edge). Rules are delivered as the connecting agent's authoritative-org set over a capability-gated (`ThresholdAlerts`) server→agent `PushAlertRules` (server config, no migration/API) so one org's rules never reach another; default minimal. A firing breach rides additively in `AgentHealthSummary` (`breaches`, no new stream), ingested as `opengate_edge_alert_breach` (metric label bounded, rule id sanitized) and charted on the Soak dashboard. Investigation-aid only (no auto-notify until the FPR soak). [ADR-053](../docs/adr/ADR-053-edge-sentinel-threshold-alerts.md); working plan: [edge-sentinel-ws-19-threshold-alerts.md](plans/archive/edge-sentinel-ws-19-threshold-alerts.md) | Edge-Sentinel WS-19 | Accepted |
| 054 | Edge Sentinel data lifecycle — right-to-be-forgotten erasure (WS-20): a device delete or tenant/fleet purge irreversibly erases the subject's telemetry across every store (VictoriaMetrics series, Postgres RLS rows, optional cold-tier objects, agent local store) and deprovisions its agents; no soft-delete/grace/undo. **Tombstone-first strict ordering** — record the subject in a persisted non-RLS `deleted_ids` deny-list, then delete VM series → cold objects → Postgres rows **last** (labels/FKs survive the drain; a crash marks-deleted not half-alive). **Every write path checks the deny-list** (agent server warms it at startup, rejects on connect + every write-path control msg; an org purge records a per-device tombstone so the check needs only the device id). Purge is a **persisted, resumable `purge_jobs`** whose completion gates on a post-delete VM emptiness verify; a periodic **reconciliation sweep** GCs orphaned series. **Audit events + the org row are retained** (erasure proof; audit FK would block org-row deletion). Server-side only (VM `-deleteAuthKey` + object creds never at the edge; selectors always pin `org_id`). Documented backups-expiry SLA (pg_dump copies age out via the bucket lifecycle policy). [ADR-054](../docs/adr/ADR-054-edge-sentinel-data-lifecycle-erasure.md); working plan: [edge-sentinel-ws-20-data-lifecycle-deletion.md](plans/archive/edge-sentinel-ws-20-data-lifecycle-deletion.md) | Edge-Sentinel WS-20 | Accepted |
| 052 | Edge Sentinel local TSDB build (WS-14b) — graduate the spike into `store::LocalTsdb` on **`redb`**: T0 1 s raw + inline anomaly bit / T1 60 s / T2 3600 s rollups in **separate tables written in one atomic transaction**; **fixed-point-per-metric** value codec (lossless to 1/scale, adaptive vs float32/int-DoD, blocks self-describe scale); rollups **sample-ts-keyed and merged incrementally** (sum+last_ts, no T0 re-read, NTP-safe); big-block T0 packing; durable **backfill cursor** (WS-15); **coarsest-first** logical-byte disk-cap eviction + host-free backoff; **format version + forward migration** + graceful open on corrupt/future files; **MVCC snapshot** reads; **deprovision purge** (WS-20); optional **cold-tier DEFLATE** on sealed T1/T2 only (pure-Rust `flate2`, **no zstd**, feature-gated default-on). Bake-off substrates + fault harness retained behind the default-on `bakeoff` feature; shipped agent depends `default-features=false` (only `redb`+`flate2` ship, clears `deny.toml`). Measured x86_64: ~1.87 logical B/sample (1.70 cold), ~1.1 ms crash-recovery open. [ADR-052](../docs/adr/ADR-052-edge-sentinel-local-tsdb-build.md); working plan: [edge-sentinel-ws-14b-offline-tsdb-build.md](plans/archive/edge-sentinel-ws-14b-offline-tsdb-build.md) | Edge-Sentinel WS-14b | Accepted |
| 055 | Fault-injection mechanism — **no fault code in the shipped binary**; prod and staging run the identical image. In-process app faults (panic recovery, typed port-error mapping, request timeout, ctx-cancel blocking, connection-close, tenant-context preservation) run in a **Go test harness** that substitutes a fault-decorating port around the real in-process server (the `store_failure_test.go` idiom + the FI0 `AgentControl` seam), `_test.go` only. Deployed faults (dependency latency/abort, QUIC/UDP agent path, **D1** packet loss/corrupt/partition, pod-kill, stress) run in **Chaos Mesh**, installed on-demand and `opengate-staging`-scoped; edge 5xx/timeout via ingress annotations. Rejected: a compiled-in injector, a build-tag fault binary, toxiproxy (TCP-only), and a **separate chaos cluster** (200 GB block cap infeasible, ADR-035). Accepted risk: a **privileged chaos-daemon on the shared production worker** under a hard guardrail contract — on-demand install/uninstall, namespace scope, mandatory `duration`, staging selectors + production-pod-exclusion guard, pinned arm64 digest, zero-residue uninstall. Both CI surfaces gate promotion at once (Go suite in normal CI + Chaos Mesh/ingress drills as a required post-E2E staging stage), no clean-run-history wait. [ADR-055](../docs/adr/ADR-055-fault-injection-mechanism.md); spec: [Fault-Injection](../docs/Fault-Injection.md). Working plan: `context-driven-fault-injection.md` | Fault-Injection FI1 | Accepted |
| 056 | Device maintenance mode + always-on edge collectors — **collectors are always-on**: the sampler/anomaly detector, threshold-alert evaluator, discovery profiler, and log-rate readers all start with the agent, no opt-in flag/env/capability gate; `Discovery`+`ThresholdAlerts` advertised unconditionally, `Backfill` when the local store opens; store cap is the `EDGE_STORE_CAP_MB` sizing constant; per-entity caps keep in-code defaults (no measurement-gated flip, no sustained-soak/ARM-footprint gate). **Maintenance mode** is a per-device server-authoritative desired state (default Active) on the `devices` row (`maintenance_on`/`_since`/`_by`/`_reason`, migration `007_maintenance_mode`), pushed over the control channel via `SetMaintenanceMode`/`MaintenanceApplied`: while set the agent stops sampling/discovery/log collection + alert eval (D5/D6) and **keeps remote management + the control channel live** (not offline), then **re-baselines** anomaly detection on exit; manual-only, no TTL (D7); central web UI only (D8); group-owner authz + audited toggle `POST /devices/{id}/maintenance` (200 even offline) + tenant `GET /devices/maintenance-summary` fleet count (D9). Visibility (escalating badge + fleet count + day-counter) replaces auto-expiry. [ADR-056](../docs/adr/ADR-056-device-maintenance-mode.md) | — | Accepted |
