# Implementation Phases

<!-- Last updated: 2026-07-06 -->
<!-- Update this file after completing or starting any significant phase of work. -->

## Completed

| Phase | Summary | Version | Plan |
|-------|---------|---------|------|
| Edge-Sentinel WS-17 inventory server | Persisted the WS-16 `DiscoveryReport` into a tenant-scoped inventory store and exposed it over the API, so a device's discovered attack-surface footprint is queryable. **Store:** migration `005_inventory` adds a forced-RLS `device_inventory` table (`org_id`-leading index, `kind`∈{port,service,db_engine,container,package}, canonical `name`/`version`/`port` plus `proto`/`state`/`runtime`/`image`, `first_seen`/`last_seen`) mirroring the WS-4 `device_processes` RLS pattern; the new [`server/internal/inventory`](../server/internal/inventory/) repo runs every read/write through `dbtx.Scoped`. Each report is a full snapshot, so `Replace` **upserts present components (advancing `last_seen`, preserving `first_seen`) and prunes vanished ones** — the rows always equal the latest scan and stay bounded by the agent's per-category caps; an empty report is a no-op so a collector hiccup can't wipe state, and every text field is control-char-redacted + length-capped as defense-in-depth over WS-16's no-secrets guarantee. **Ingest:** [conn.go](../server/internal/agentapi/conn.go) now dispatches `DiscoveryReport` to a new [conn_discovery.go](../server/internal/agentapi/conn_discovery.go) handler that flattens the five categories into components and persists them **scoped to the connection's authoritative org (never the agent-supplied `org_id`)** through the bounded async telemetry-slot path, with a discovery-specific 1 MiB payload cap and 30 s interval floor (drops counted on the existing edge-telemetry drop metric). **API:** `GET /devices/{id}/inventory` (OpenAPI + Go/TS regen) returns the footprint as a flat `items[]` list to **any device viewer in the org** (group-owner gated, not admin-elevated), tenant-scoped via RLS. Migration up/down + RLS + dump/restore are re-verified in the `TestMultitenancyMigrationRehearsal` rehearsal, `device_inventory` is added to the scoped-SQL tenant-table gate, and the inventory module is constrained to `mayDependOn: [dbtx]` in `.go-arch-lint.yml` alongside its `telemetry` sibling. Docs: [Database](../docs/Database.md), [API-Reference](../docs/API-Reference.md). | — | [edge-sentinel-ws-17-inventory-server.md](plans/archive/edge-sentinel-ws-17-inventory-server.md) |
| Edge-Sentinel WS-16 discovery agent | Added zero-config, non-intrusive host auto-discovery at the edge. New `mesh-agent-core::discovery` collectors profile the host read-only — **ports** (`/proc/net/{tcp,tcp6,udp,udp6}` listeners/bound sockets on Linux, resolving the owning process basename via a bounded `/proc/[pid]/fd` inode walk; `netstat -ano` on Windows), **services** (`systemctl list-units` / `Get-Service`), **db_engines** (pure inference from listening ports: process-name match is authoritative, well-known port is a fallback only when the process is unknown or agrees — never a probe, so no credential leak), **containers** (read-only `docker`/`podman ps` JSON), and **packages** (`dpkg-query`/`rpm -qa` / Windows uninstall registry). Each collector no-ops (empty) where its source is absent, so one call is safe on every fleet host. The orchestrator caps each category (`MAX_PORTS`/`MAX_SERVICES`/`MAX_DB_ENGINES`/`MAX_CONTAINERS`/`MAX_PACKAGES`), sets `truncated` on any cap hit, and exposes a timestamp-independent `fingerprint` for change-triggered re-profiling. Additive wire: `DiscoveryReport { ts, org_id, ports[], services[], db_engines[], containers[], packages[], truncated }` (+ five sub-structs) with a Rust↔Go golden ([control_discovery_report.bin](../testdata/golden/control_discovery_report.bin)) and the new `Discovery` capability; the report carries engine/port/version and names only — never a bound address, connection string, or credential. `mesh-agent` spawns the task behind `--edge-discovery` / `OPENGATE_EDGE_DISCOVERY` (default-off): a long-interval `spawn_blocking` sweep that forwards a report over a bounded channel drained on heartbeat **only when the profile changed**, so a steady host is silent and a burst never backpressures control. Server storage/UI is WS-17/18; the server ignores the report until then. Docs: [Wire-Protocol](../docs/Wire-Protocol.md), [Architecture](../docs/Architecture.md). | — | [edge-sentinel-ws-16-discovery-agent.md](plans/archive/edge-sentinel-ws-16-discovery-agent.md) |
| Edge-Sentinel WS-15b ops + soak harness | Made the default telemetry path observable and built the sustained-soak driver that gates the default-on flip (the flip itself still awaits a real measured soak; telemetry stays **default-off**). **Metrics:** the WS-4 ingest path and WS-15 backfill scheduler now export Prometheus series ([metrics.go](../server/internal/metrics/metrics.go)) wired through `agentapi` — `opengate_edge_telemetry_ingested_total{type}` (accepted messages), `opengate_edge_telemetry_drops_total{reason}` (server-side drops; `persist_slots_full` is the queue-saturation signal), and the scheduler's `opengate_edge_backfill_active_slots` / `opengate_edge_backfill_decisions_total{decision}` / `opengate_edge_backfill_grant_rate_samples_per_second`. The nil sink is a safe no-op, so programmatic `AgentConn`s stay uninstrumented. **Load harness** ([loadtest](../server/tests/loadtest)): drives the full default telemetry shape per agent (`-default-telemetry`: health summary + host metric window + minimal process report), spreads agents across tenant cohorts (`-orgs`), and runs a fleet-wide reconnect storm (`-backfill-batches`) that advertises `Backfill`, requests an admission slot, and drains one acked batch at a time — deferrals and expired grants end the drain cleanly. Deterministic scenario builders are unit-tested (N-tenant × M-agent partition, default-telemetry shape, tiered backfill batch, storm round-trip). **Dashboard:** new `Edge-Sentinel Soak` Grafana JSON ([edge-sentinel-soak.json](../deploy/grafana/provisioning/dashboards/edge-sentinel-soak.json)) charts ingest/drop rates, anomaly rate, VM active-series cardinality + disk growth, control-plane and correlation query p99, and backfill scheduler state over the VM datasource. Alerts deferred (investigation-aid only). Docs: [Monitoring](../docs/Monitoring.md#sustained-soak-and-default-on-gate), [Multiscale-Readiness](../docs/Multiscale-Readiness.md), [ADR-044](../docs/adr/ADR-044-edge-sentinel-server-telemetry-ingest.md). | — | [edge-sentinel-ws-15b-ops-measurement.md](plans/archive/edge-sentinel-ws-15b-ops-measurement.md) |
| Edge-Sentinel WS-15 reconnect backfill | Completed the reconnect-backfill workstream by driving it from the agent binary. The **server surface** already landed: the additive wire contract, the pure agent-core replay engine ([backfill.rs](../agent/crates/mesh-agent-core/src/ml/backfill.rs)), the server admission scheduler ([backfill_scheduler.go](../server/internal/agentapi/backfill_scheduler.go)), the tiered VM ingest at true timestamps via the import API ([conn_backfill.go](../server/internal/agentapi/conn_backfill.go)), and the admin-gated deep-history broker (`GET /devices/{id}/history`). This slice **wires the `mesh-agent` control loop** ([backfill_loop.rs](../agent/crates/mesh-agent/src/backfill_loop.rs)): the sampler-owned `LocalTsdb` is shared (`Arc<Mutex<LocalStoreSink>>`) with the control loop; on each reconnect the agent advertises the `Backfill` capability, sends `RequestBackfillSlot` with a backlog hint, and — on `GrantBackfill` — drains **recent-first, tier-mapped (10 s → 1 min → 1 hr, 1 s never sent), throttled to the granted rate, one acked batch at a time**, advancing a durable per-tier watermark (reserved cursor-table keys) only on `MetricBackfillAck`; a `DeferBackfill` holds the durable data and retries with ±25 % jitter; an expired grant re-requests a slot rather than draining past it. It also answers server-brokered `RequestLocalHistory` from the local T0 raw tier (bounded, truncation-flagged). Gated on the same default-off `--edge-store` enablement as the store it drains. Docs: [Monitoring](../docs/Monitoring.md#reconnect-backfill-and-deep-history-pull), [Multiscale-Readiness](../docs/Multiscale-Readiness.md). | — | [edge-sentinel-ws-15-offline-backfill.md](plans/archive/edge-sentinel-ws-15-offline-backfill.md) |
| Edge-Sentinel WS-14b local-TSDB build | Graduated the WS-14a spike crate [`agent/crates/edge-tsdb`](../agent/crates/edge-tsdb) into the production agent-local multi-tier store `store::LocalTsdb` on **redb** — the sovereign copy of min/max/last + 1 s raw that central `avg`-only VM cannot serve ([ADR-052](../docs/adr/ADR-052-edge-sentinel-local-tsdb-build.md)). **Storage:** T0 1 s raw + inline anomaly bit / T1 60 s / T2 3600 s rollups in three separate redb tables written in **one atomic transaction**; a **fixed-point-per-metric** value codec ([compact.rs](../agent/crates/edge-tsdb/src/compact.rs) `encode_compact_scaled`) storing `round(value×scale)` delta-of-delta — **lossless to 1/scale**, adaptive against float32-XOR / lossless int-DoD, blocks self-describe their scale; rollups **sample-ts-keyed** and merged incrementally via sum+last_ts ([tier.rs](../agent/crates/edge-tsdb/src/tier.rs) `StoredTierPoint`) so a bucket split across commits or a late NTP sample folds exactly with **no T0 re-read**; big-block T0 packing amortises redb's per-key overhead. **Surface:** durable per-series **backfill cursor** (WS-15 resume), **coarsest-first** logical-byte disk-cap eviction (globally-oldest first, coarser tier on tie, newest raw always retained) + host-free backoff (never fills the host disk), **format version + forward migration** (+ graceful open on corrupt/future files, never a panic), **MVCC snapshot** reads (detection/backfill read a stable view while the sampler writes), **deprovision purge** (WS-20), and optional **cold-tier DEFLATE** on sealed T1/T2 only (pure-Rust `flate2`, **no zstd**, feature `cold-deflate` default-on). **Graduation:** the WS-14a bake-off substrates (A append-only / C no-persist / B,B+ redb references) + fault harness are retained behind the default-on `bakeoff` feature (off-ramp reference + `cargo bench` corpus); the shipped `mesh-agent` depends `default-features=false` so only `redb`+`flate2` ship (clears `deny.toml`). **Wiring:** `mesh-agent-core::ml::store_sink::LocalStoreSink` persists each sampled dimension with its ensemble anomaly bit; `mesh-agent` opens/recovers the store under `OPENGATE_DATA_DIR` behind `--edge-store` (default off, `--edge-store-cap-mb` cap), recreating a corrupt cache rather than crashing. Measured x86_64: **~1.87 logical B/sample** (1.70 cold), ~1.1 ms crash-recovery open, ~0.59 ms range query — below the ~2.4 target, so the append-structured off-ramp is not triggered. Default-off until the ARM64+Windows footprint gate + WS-15b soak. | — | [edge-sentinel-ws-14b-offline-tsdb-build.md](plans/archive/edge-sentinel-ws-14b-offline-tsdb-build.md) |
| Edge-Sentinel WS-14a local-TSDB spike | Evidence-first substrate bake-off for the agent-local offline store (load-bearing because central VM keeps `avg` only, so min/max/last + 1 s raw live only at the edge). New spike crate [`agent/crates/edge-tsdb`](../agent/crates/edge-tsdb) — nothing in the shipped agent depends on it — holds: a deterministic replayable **fixture corpus** (sticky gauges + monotonic counters), a shared **Gorilla** compression (delta-of-delta ts + XOR value) + **tier rollup** (T0/T1/T2 min/max/avg/last, sample-ts-keyed) + CRC **framing** layer, three substrates behind one trait (**A** bespoke append-only files, **B** redb, **C** no-persist control), and a **fault-injection harness** (torn-tail truncation, mid-file bit-flip quarantine, disk-full byte-cap eviction, NTP clock-jump). Gate thresholds are asserted as always-run tests (`tests/gates_test.rs`); the full table is reproduced by `cargo bench -p edge-tsdb`. **Decision: `redb`** — "simplest that clears the bar": A is ~2.8× denser (2.6 vs 7.3 B/sample) but owns ~500 lines of crash/recovery code, whereas redb inherits 2-phase-commit/quick-repair/`check_integrity` with ~165 glue lines, no background threads, and zero transitive deps; `fjall`/`tsink` fail the dependency sub-gate empirically (3rd `hashbrown` major → `multiple-versions=deny` + compaction threads). **Path-1 codec follow-up:** a Netdata/tsink-grade compact block codec ([compact.rs](../agent/crates/edge-tsdb/src/compact.rs): float32 + implicit fixed-step timestamps + adaptive XOR32/int-DoD + inline anomaly bit) measures **~2× denser** than f64 Gorilla (1.26 vs 2.47 B/sample; float32 error 5.7e-8 rel; counters bit-exact), and big-block packing in redb ([redb_compact.rs](../agent/crates/edge-tsdb/src/redb_compact.rs)) halves redb's steady-state footprint (~4.9→~2.4 B/sample), erasing A's density edge while keeping redb's crash-safety — so **WS-14b builds redb + the compact block format**, and custom-A's off-ramp sharpens to "only if ~2.4 B/sample still blows the fleet disk budget". ADR: [ADR-051](../docs/adr/ADR-051-edge-sentinel-local-tsdb-substrate.md). | — | [edge-sentinel-ws-14a-offline-tsdb-spike.md](plans/archive/edge-sentinel-ws-14a-offline-tsdb-spike.md) |
| Edge-Sentinel WS-13 log privacy/ops | Closed the cross-cutting endpoint-log concerns: a raw-log redaction corpus, the no-GPL reader-sourcing ADR, edge-reader benchmarks, and soak/dashboard wiring toward the WS-15b default-on gate. **Redaction (defense-in-depth):** two independent guards now scrub a shared secret corpus — the agent's `redact_log_line` ([redact.rs](../agent/crates/mesh-agent-core/src/ml/redact.rs)) runs on every raw line before it leaves the device (applied in `host_logs::redact_entries`), and the server's `redactSecrets` ([log_redact.go](../server/internal/api/log_redact.go)) runs again before lines reach the browser. Both cover bearer/basic, `key=value`/`key: value` creds, JWTs, AWS access-key ids, GCP API keys, credentialed connection strings, and PEM keys; the two corpora are pinned as paired tests. **Benchmark:** the Edge-Sentinel Criterion harness ([edge_sentinel_bench.rs](../agent/crates/mesh-agent-core/benches/edge_sentinel_bench.rs)) gains a log-rate window-fold and a per-line redaction bench, recorded through the existing benchmark→VM pipeline. **Ops:** new `opengate_device_log_pulls_total`/`opengate_device_log_pull_duration_seconds` broker metrics ([metrics.go](../server/internal/metrics/metrics.go)) instrument `GetDeviceLogs` (the ok series is the audited-read count); the load harness ([loadtest](../server/tests/loadtest/main.go)) emits log-rate windows and can answer raw pulls; a new `Edge-Sentinel Logs` Grafana dashboard ([edge-logs.json](../deploy/grafana/provisioning/dashboards/edge-logs.json)) charts log-rate ingest, pull rate/latency, and audited reads over the VM datasource. ADRs: [ADR-048](../docs/adr/ADR-048-edge-sentinel-endpoint-log-model.md), [ADR-049](../docs/adr/ADR-049-edge-sentinel-raw-log-privacy.md), [ADR-050](../docs/adr/ADR-050-edge-sentinel-log-reader-sourcing.md). | — | [edge-sentinel-ws-13-log-privacy-ops.md](plans/archive/edge-sentinel-ws-13-log-privacy-ops.md) |
| Edge-Sentinel WS-12 log web | Logs explorer + metrics↔logs correlation jump on the WS-6 chart engine. The device-detail logs explorer reads the WS-11 on-demand broker with level, time-range, and full-text filters plus level facets over the returned page and pagination, rendering only the redacted lines the broker returns. A compact log-rate sparkline reuses the WS-6 uPlot adapter and plots the numeric `log.rate.*` dims only — message text is never a chart label. The Netdata-style correlation jump ("View logs for this window") carries a device window from the metrics panel straight into the explorer, which pre-filters and fetches it. Also fixed the CD staging-reset step that still truncated the retired `device_logs` table (dropped by `004_retire_device_logs`). ADR: [ADR-047](../docs/adr/ADR-047-web-telemetry-chart-engine.md). | — | [edge-sentinel-ws-12-log-web.md](plans/archive/edge-sentinel-ws-12-log-web.md) |
| Edge-Sentinel WS-11 log server | Retired the central `device_logs` raw-log cache and rebuilt on-demand log retrieval as a transient broker, resolving the "nothing raw persisted centrally" precondition against real code. `GET /devices/{id}/logs` now resolves the connected agent, sends `RequestDeviceLogs`, and **blocks on a per-connection single-flight waiter** (`AgentConn.RequestLogsSync`) until the agent's bounded `DeviceLogsResponse` arrives; the lines are redacted and streamed straight back with **nothing persisted** — raw-log isolation is the connection scope, not an RLS row. The pull is **elevated (admin-gated)** and writes a `device.logs.read` **audit event** (who/device/window/filters, never content); it is **bounded** by line-count, per-line-byte, and blocking-time caps, and passes a **server-side redaction guard** (auth headers, key/value credentials, AWS keys, PEM private keys) that runs even when agent redaction is off. Busy → `409`, timeout → `504`; the `202`/`refresh` cache-poll surface is gone and the web viewer fetches directly. Migration `004_retire_device_logs` drops the table, RLS policy, and repository (rehearsed up/down). Log-rate dims continue to VM scoped by the server-resolved org. ADR: [ADR-046](../docs/adr/ADR-046-edge-sentinel-raw-log-broker.md). | — | [edge-sentinel-ws-11-log-server.md](plans/archive/edge-sentinel-ws-11-log-server.md) |
| Edge-Sentinel WS-10 log wire | Carried endpoint log-rate signals over the existing telemetry path and extended the on-demand log query to host sources — additive and golden-gated in both languages. The `RequestDeviceLogs` control message gained a `source` selector (`self`/`journald`/`windows`) and a structured `unit` filter, both default-empty so older servers and agents interoperate unchanged (a Go-sends-only-`type` decode test pins the Rust defaults). Log-rate windows reuse `AgentMetricWindow` (no new variant): the agent's default-off host log readers fold each window into nine dims named `log.rate.<source>.<field>` — five severity-level counts, three top-emitting-unit counts by rank, and total volume — carrying only counts and ranks, never a unit name or message text, so central cardinality stays bounded. The reader forwards windows to the control loop over a bounded `sync_channel` drained on each heartbeat; it drops under pressure so a log burst never backpressures control. New Rust→Go goldens (`control_agent_metric_window_log_rates.bin` + the extended `control_request_device_logs.bin`) pin the naming contract and the extended request; the request stays gated by the `DeviceLogs` capability. | — | [edge-sentinel-ws-10-log-wire.md](plans/archive/edge-sentinel-ws-10-log-wire.md) |
| Edge-Sentinel WS-9 log readers | Added default-off host log-rate readers at the edge. New `mesh-agent-core` `ml::log_rate` extractor folds a window of normalized entries into a fixed nine-wide feature vector — five per-level counts, the counts of the top three emitting units **by rank** (never unit name, so cardinality stays bounded), and total volume — allocation-free at `finish`; it rides the same WS-2 k-means ensemble as host metrics (an injected error burst trips the trained ensemble in-test). A new `host_logs` module (beside the self-log `logs.rs`) provides `journalctl -o json` and `Get-WinEvent | ConvertTo-Json` parsers that normalize to `LogEntry` (syslog-priority and Windows-level bands → level labels; `__REALTIME_TIMESTAMP` µs → RFC 3339 UTC), a `LogSource` selector, and bounded (`MAX_HOST_LINES`) subprocess collectors that no-op off-platform so one call is safe on every fleet host. `main.rs` spawns the reader task behind `--edge-log-readers` / `OPENGATE_EDGE_LOG_READERS` (default off); it sleeps between windows so bounded reads never starve control/session traffic. No message text ever becomes a feature or label. | — | [edge-sentinel-ws-9-log-readers.md](plans/archive/edge-sentinel-ws-9-log-readers.md) |
| Edge-Sentinel WS-7 cold tier | Aligned the deployed VictoriaMetrics stream-aggregation config with the locked central-`avg`-only cardinality decision: rollup `outputs` dropped from `[avg, min, max, last]` to `[avg]` on both the 1 m and 1 h intervals. The min/max/last rollup families were consumed by no read path — WS-6 chart bands derive min/max via `min_over_time`/`max_over_time` on the raw 10 s `avg` series, never a central `_min`/`_max` rollup — yet cost ~4× the avg-only active-series count, over the 50k budget ratified in [`spike_test.go`](../server/tests/vmcardinality/spike_test.go); `-streamAggr.keepInput` still preserves raw. New [`edge-sentinel-cold-tier.test.sh`](../scripts/tests/edge-sentinel-cold-tier.test.sh) pins the config to that budget (avg-only outputs, keepInput, retention wiring) — the cardinality spike is a pure model that never reads the live YAML. Documented the long-term (cold) tier honestly in Monitoring/Database: single-node OSS VictoriaMetrics has one global retention window (per-series retention + downsampling are Enterprise), so raw and `avg` rollups share it and rollups exist for query efficiency, not longer survival. | — | [edge-sentinel-ws-7-cold-tier-duckdb.md](plans/archive/edge-sentinel-ws-7-cold-tier-duckdb.md) |
| Edge-Sentinel WS-6 web UI | uPlot-based device telemetry view on a thin canvas-2D adapter (the only module importing uPlot; React owns chrome, the renderer owns pixels via typed arrays), code-split into a budgeted `charts` chunk that leaves first paint untouched. The device-detail panel shows the current anomaly rate, per-family metric timelines, and a Netdata-style drag-to-correlate drill-down over the WS-5 correlate endpoint; the virtualized grid and the dashboard carry scalar-only health badges (no per-device series on the grid). Chart bands carry a `min_max_source` provenance and honestly fall back to `avg_of_10s` (min/max across the 10 s averages, labelled as not host extrema) because central VM is `avg`-only; true host min/max is a later WS-15 enrichment. Server range endpoint bounds returned points via a `*_over_time` collapse of the raw 10 s `avg` series. ADR: [ADR-047](../docs/adr/ADR-047-web-telemetry-chart-engine.md). | — | [edge-sentinel-ws-6-web-ui.md](plans/archive/edge-sentinel-ws-6-web-ui.md) |
| Edge-Sentinel WS-5 correlation engine | Added the on-demand metric-correlation engine `server/internal/correlate`: it fetches a device's candidate numeric series from VictoriaMetrics through the WS-4 tenant-scoped read client (`Export`, which injects the `org_id` label matcher) and ranks the dimensions that broke pattern in an incident window versus a baseline. Ranking is server-side pure Go — a two-sample Kolmogorov–Smirnov distribution-shift statistic blended with an anomaly-rate (band-breach fraction) and a shift-magnitude (volume) signal — because MetricsQL has no native KS test or join. The engine is on-demand only (no background matrix) and bounded by a concurrency limiter, per-request timeout, and series/points caps; missing or thin history is skipped cold-start-safe. New `POST /api/v1/devices/{id}/correlate` endpoint (OpenAPI + Go/TS regen) derives the tenant org from the authenticated context (never the body), enforces device-group ownership, and reports 400/404/503 for invalid window / unknown device / unconfigured-or-at-capacity. | — | [edge-sentinel-ws-5-correlation-engine.md](plans/archive/edge-sentinel-ws-5-correlation-engine.md) |
| VM Readback: Retire Criterion HTML reports | Completed the VM read-back master effort. Benchmark trends live in VictoriaMetrics, so Criterion's HTML output is dead weight — the summarizer only reads `new/estimates.json`. Dropped the default `html_reports` feature (`default-features = false` + `cargo_bench_support` on the workspace dep; `mesh-protocol` now inherits `criterion.workspace = true`), which removes `plotters*` from `agent/Cargo.lock`. `benchmark.yml` now copies only the `<bench>/new/estimates.json` files into a renamed data-only `bench-rust-estimates` artifact (both upload and download). All five micro-plans (HTML retire, M0–M3) and the master plan are archived under `plans/archive/`. | — | [vm-readback-criterion-html-retire.md](plans/archive/vm-readback-criterion-html-retire.md) |
| VM Readback M3: Load-Test Regression Gate | Added the first load-test trend regression gate on top of VictoriaMetrics read-back. `loadtest-regression-check.sh` gates each `{source,scenario,phase}` independently: latency p50/p95 and rps compare against 14d per-series VM medians with frozen calibrated bands plus absolute ceilings/floors, `error_rate` has absolute/previous-sample checks, p99 is advisory-only, cold-start series use absolute-only, and VM/kubectl gaps fail open. `load-test.yml` now splits run vs publish: the run job uploads canonical summary rows without a GitHub environment; the `observability` publish job reads VM, pushes current rows to VM, sends Telegram on regression, and then fails red. ADR-045 supersedes ADR-038's load-test visibility-only consequence. Live calibration found 15 retained QUIC samples and no retained k6 samples, so k6 begins in cold-start mode. | — | [vm-readback-m3-loadtest-gate.md](plans/archive/vm-readback-m3-loadtest-gate.md) |
| VM Readback M0+M2: Shared Query Lib + Benchmark ns/op Hard Gate | Extracted `scripts/lib/vm-query.sh` (`vm_query_latest` newest-sample + `vm_query_window` per-series window aggregation, both fail-open, current-commit exclusion via `VM_EXCLUDE_COMMIT`) and repointed `pmat-vm-query.sh` at it behavior-preserving (M0). Promoted benchmark `ns/op` from never-firing 150% advisory to a hard red gate (M2): `benchmark-summarize.sh` reds when a run's `ns/op` exceeds either the 14d VictoriaMetrics window median × a frozen 50% band (relative, skipped on cold-start < 3 samples) or the committed baseline `ns_op` × 2 (absolute boiling-frog backstop); `allocs/op`/`bytes/op` keep the committed-baseline ±2% gate. Band/ceiling calibrated from the live VM series (measured run-to-run CV ≤ 12.4%, worst no-change excursion +28%). `benchmark.yml` moves OCI/kube setup before summarize and broadens the fail-red/Telegram wording to cover ns/op. Also hardened the mutation-baseline-fetch test's JSON comparison to be jq-1.7 number-literal stable. | — | — |
| VM Readback M1: Mutation Drop Gate + Pages Churn Fix | Restored the mutation workflow's previous-run drop gate by adding `scripts/mutation-baseline-fetch.sh`, which reconstructs a one-line `HISTORY_FILE` from prior `mutation_score` VictoriaMetrics samples while excluding the current commit and failing open on VM/transport gaps. `mutation.yml` now moves OCI/kube setup before summarize, restores that baseline before `mutation-summarize.sh`, and keeps the existing exit-2 incomplete-run split intact; alert headers use `GITHUB_REF_NAME` so scheduled `main` runs are no longer mislabeled as `dev`. The linked GitHub Pages timeout was traced to volatile `perf-publish` commits to `gh-pages`; that publisher is removed, ADR-038/CI docs now state browser performance evidence is per-run artifacts/summaries, and `ci-trend-retirement.test.sh` guards against reintroducing `gh-pages/dev/perf` history. | — | [vm-readback-m1-mutation-drop-gate.md](plans/archive/vm-readback-m1-mutation-drop-gate.md) |
| Edge-Sentinel WS-4 server telemetry ingest | Added the actual WS-4 ingest path: `server/internal/telemetry` writes numeric Edge Sentinel samples to VictoriaMetrics with server-owned `org_id`/`device_id` labels and scoped export reads, while `003_telemetry` adds a forced-RLS `device_processes` Postgres table for sanitized process snapshots. The agent control path now resolves the enrolled device's real organization after handshake and scopes agent-originated writes to that org rather than trusting payload `org_id` or falling back to the default tenant. Telemetry dispatch enforces a 64 KiB payload cap, a 10 s per-type interval floor, bounded non-blocking persistence slots, and drop accounting so telemetry cannot backpressure heartbeat/session/restart traffic. Helm wires the server to the in-cluster VictoriaMetrics service and enables VM stream aggregation for `opengate_edge_*` rollups while preserving raw input. ADR: [ADR-044](../docs/adr/ADR-044-edge-sentinel-server-telemetry-ingest.md). | — | [edge-sentinel-ws-4-server-ingest-vm.md](plans/archive/edge-sentinel-ws-4-server-ingest-vm.md) |
| Edge-Sentinel WS-3 wire contract | Added additive Edge Sentinel `ControlMessage` variants for `AgentHealthSummary`, `AgentMetricWindow`, `ProcessReport`, `RequestHealthWindow`, and `HealthWindowResponse` in the Rust protocol enum and Go flat control envelope. Rust-generated forward goldens and Go-generated reverse goldens now cover the new shapes, including `serde_bytes` binary encoding for `recent_bitmask`. `RequestHealthWindow` is gated by the new `HealthWindow` capability in the server agent connection API. Live agent emission remains default-off pending the ARM/soak gate; server ingest and write arbitration landed in WS-4. | — | — |
| Edge-Sentinel WS-0 deployment/RLS hardening | Closed the post-migration staging reset gap by reseeding the Administrators group with the default `org_id` after CD truncates tenant tables, guarded by a focused workflow test. Production/staging Helm now run application traffic through a dedicated `opengate_app` runtime role that is non-superuser/non-`BYPASSRLS`; fresh DBs create it during Postgres bootstrap and CD repairs/verifies existing clusters before rollout while leaving the original Postgres role available for maintenance/backups. | — | — |
| Edge-Sentinel Foundation WS-0/1/2 | Landed the first Edge-Sentinel foundation slice. **WS-0:** Postgres `002_multitenancy` migration adds organizations, `org_id`, org-leading indexes, forced RLS policies, JWT `org` claims, tenant-scoped `dbtx.Scoped` repository transactions, default-org login/enrollment paths, API/test fixtures, web auth-store `orgId` retention, per-repo cross-tenant-deny tests, a static tenant-table SQL scoped-helper gate, and an automated migration rehearsal (`001` seed → `002` backfill/RLS/idempotence → in-container `pg_dump`/restore → restored RLS proof → `.down.sql` rollback). **WS-1:** Control dispatch tolerates unknown future types both directions, hardware/log request variants are capability-gated, new agent capabilities advertise HardwareInventory/DeviceLogs, and bidirectional unknown-type golden fixtures are green. **WS-2:** Added default-off agent Edge Sentinel sampler (`--edge-sentinel` / `OPENGATE_EDGE_SENTINEL`) plus pure-Rust k=2 model, ensemble consensus, anomaly window, `sysinfo` sampler, fake sampler, command-line redaction tests, allocation-free detection guard, RSS footprint guard, and Criterion bench harness. ADRs: [ADR-041](../docs/adr/ADR-041-postgres-rls-multitenancy.md), [ADR-042](../docs/adr/ADR-042-control-forward-compat-capabilities.md), [ADR-043](../docs/adr/ADR-043-edge-sentinel-local-ml-sampler.md). | — | — |
| Diagrams as Code — Part 2 (D1–D5) | Extended the Part-1 Mermaid-only docs-as-code regime ([ADR-039](../docs/adr/ADR-039-diagrams-as-code-part-2.md)). **D1 (drift guard):** [`docs-diagrams.test.sh`](../scripts/tests/docs-diagrams.test.sh) now pins every diagram-bearing doc + a total-fence floor (was 2 of 6), closing the silent-deletion gap; added [`.github/CODEOWNERS`](../.github/CODEOWNERS) + a [PR-template](../.github/pull_request_template.md) diagram-drift nudge. **D3 (CI syntax validation):** new path-filtered [`docs-validate.yml`](../.github/workflows/docs-validate.yml) parses every `mermaid` fence with the official parser pinned in [`tools/mermaid-validate/`](../tools/mermaid-validate/) (no Puppeteer, isolated from `web/` + the local grep-only gauntlet). **D2 (C4):** Architecture.md gained render-verified `C4Context` + `C4Container` views; README documents the C4 convention + the mandatory GitHub-render-with-fallback rule. **D4 (coverage):** a README coverage standard + the deploy-topology (Kubernetes.md), CI/CD-flow (Continuous-Deployment.md), and session-lifecycle (Architecture.md) diagrams, each pinned. 12 Mermaid blocks total, all CI-validated. | — | [diagrams-as-code-part-2.md](plans/archive/diagrams-as-code-part-2.md) |
| Benchmark Trends B5: Legacy Trend Retirement | Retired both superseded CI-trend paths after verifying benchmark, mutation, PMAT, terraform-drift, and load-test series in live VictoriaMetrics. The legacy benchmark feeder/publisher jobs and action are gone; volatile browser-performance history is no longer pushed to `gh-pages`, leaving API docs as the low-churn branch publication path. Loki trend push/query wrappers and their transport test are removed, PMAT's day-over-day lookup now reads the newest VM sample, and focused shell guards prevent either trend path from returning. PromQL dashboards remain; Loki's runtime deployment and log pipeline are untouched. | — | [bench-trends-b5-retire-legacy.md](plans/archive/bench-trends-b5-retire-legacy.md) |
| Benchmark Trends B4: Load-Test Trend Persistence | Load tests now persist visibility-only CI trends to VictoriaMetrics without adding any latency alert or changing the k6/QUIC pass-fail gates. `load-test.yml` writes k6 `--summary-export` JSON for each scenario, tees the QUIC harness output while preserving failure status with `pipefail`, and runs an `if: always()` VM publish step. `scripts/loadtest-summarize.sh` extracts k6 + QUIC latency p50/p95/p99, throughput, and error-rate rows; `scripts/loadtest-vm-push.sh` maps them to `loadtest_*` Prometheus samples; `opengate-loadtest-trend` is the new PromQL/Grafana dashboard. Focused shell tests cover parsing, partial failed-run capture, duration conversion, and VM payload mapping. | — | [bench-trends-b4-loadtest-trends.md](plans/archive/bench-trends-b4-loadtest-trends.md) |
| Benchmark Trends B3: Family VM Migration | Mutation, PMAT, and terraform-drift now emit Prometheus samples to VictoriaMetrics through new `scripts/mutation-vm-push.sh`, `scripts/pmat-vm-push.sh`, and `scripts/terraform-drift-vm-push.sh` wrappers while retaining the legacy Loki mirror for B5 removal. The mutation and PMAT dashboards are PromQL/VictoriaMetrics-backed, terraform drift has a new `opengate-terraform-drift-trend` PromQL dashboard, and terraform-drift publishes zero-drift samples without changing its Telegram/fail-red drift detection. `scripts/tests/ci-trend-vm-push.test.sh` pins the canonical-row→Prometheus mapping for all three wrappers. | — | [bench-trends-b3-migrate-family.md](plans/archive/bench-trends-b3-migrate-family.md) |
| Benchmark Trends B2: Nightly Benchmark VM Pipeline | Added the standalone `benchmark.yml` nightly/dispatch workflow for Go + Rust micro-benchmarks, with a run/publish split, committed allocation baseline, deterministic `allocs/op` + `B/op` regression gate, Telegram alert path, VictoriaMetrics push through `scripts/benchmark-vm-push.sh`, and the `opengate-benchmark-trend` PromQL Grafana dashboard. `scripts/benchmark-summarize.sh` parses Go `-benchmem` plus Criterion output into canonical rows; focused shell tests pin parsing, baseline generation, hard allocation regressions, and `ns/op` advisory-only behavior. | — | [bench-trends-b2-benchmark-pipeline.md](plans/archive/bench-trends-b2-benchmark-pipeline.md) |
| Benchmark Trends B1: VictoriaMetrics Push Transport | Added the shared `scripts/lib/vm-push.sh` CI trend transport for Prometheus-text imports into in-cluster VictoriaMetrics via the existing authenticated kubectl/curl-pod channel. The transport enforces the CI trend label convention (`commit`, `env="ci"`) before reaching Kubernetes, supports file or stdin payloads, propagates VM/kubectl failures loudly, and is covered by the offline `vm-transport.test.sh` behavioral test. `docs/Monitoring.md` now records the metric naming/label convention for B2–B4. | — | [bench-trends-b1-vm-transport.md](plans/archive/bench-trends-b1-vm-transport.md) |
| Fast-Path Reconnect W5: Workaround Removal + Hardening | Removed the vestigial server-proof / agent-proof handshake surface (`0x12`/`0x13`) from Go and Rust decoders, with regression tests pinning those retired type bytes as rejected. The server's client-first `AcceptStream` path now has an explicit 10s bound, stale "server-initiated" wording is gone from the agent connection comments, and the Rust `AgentConnection` constructor no longer carries the unused `AgentConfig` field. `Allow0RTT` remains off per W3. | — | [fast-path-w5-remove-workaround.md](plans/archive/fast-path-w5-remove-workaround.md) |
| Fast-Path Reconnect W4: ADR-037 Client-First Reconnect Model | Added [ADR-037](../docs/adr/ADR-037-client-first-fast-path-reconnect.md) to supersede ADR-005's server-opens/quic-go-mTLS-bug rationale without editing the frozen ADR-001–012 log. The decision index now includes ADR-036 and ADR-037, and readiness/wire docs reflect the settled model: agent opens and writes first, auth is mTLS-only, `0x14` saves a round-trip, `0x12`/`0x13` are retired, 1-RTT resumption is adopted, and 0-RTT is deferred. | — | [fast-path-w4-adr.md](plans/archive/fast-path-w4-adr.md) |
| Docs Doctrine E: Diagram / Docs-as-Code | Architecture docs now use hand-curated Mermaid blocks for system topology, the client-first/`0x14` handshake, and browser↔agent relay flow; `docs/Wire-Protocol.md` matches the live agent-first handshake model. `docs/README.md` records the Mermaid-only convention (no D2/SVG/rendering pipeline), and `scripts/tests/docs-diagrams.test.sh` enforces the diagram policy plus the existing boundary-linter wiring in precommit/CI. | — | [docs-doctrine-e-diagrams-as-code.md](plans/archive/docs-doctrine-e-diagrams-as-code.md) |
| Fast-Path Reconnect W3: QUIC Session Resumption (evaluation + decision) | Spike ([`quic_resumption_test.go`](../server/internal/agentapi/quic_resumption_test.go) + paired benchmarks) proved, against quic-go v0.60.0 and the repo's mTLS config, that **1-RTT TLS session resumption** completes under `RequireAndVerifyClientCert` and **preserves the verified client identity** server-side (`DidResume==true`, `PeerCertificates` retained) while skipping the asymmetric handshake — measured **~23% / ~360µs** lower per-reconnect cost (~207 fewer allocs). 0-RTT also works with mTLS on this version but is **deferred** (early data is replayable; on top of 1-RTT it saves latency, not crypto). **Decision: adopt 1-RTT resumption, defer 0-RTT.** Server needs **no change** (tickets issued by default; `Allow0RTT` stays off); realizing the saving requires the quinn agent to persist a session-ticket cache — tracked in [techdebt.md](techdebt.md) and recorded in [ADR-037](../docs/adr/ADR-037-client-first-fast-path-reconnect.md). | — | [fast-path-w3-0rtt-eval.md](plans/archive/fast-path-w3-0rtt-eval.md) |
| Fast-Path Reconnect W2: `0x14` Fast-Path Reconnect | The QUIC handshake now branches on the agent's first message: `0x11` AgentHello → full handshake; `0x14` SkipAuth → fast-path reconnect that verifies the cached CA hash is current and skips the ServerHello/AgentHello round-trip (`HandshakeResult.Skipped` wired for real). The agent caches the CA hash from the cold-start ServerHello and replays it via `0x14` on reconnect, falling back to a full handshake if the server rejects a stale hash (CA rotation). Settled auth model recorded in [ADR-037](../docs/adr/ADR-037-client-first-fast-path-reconnect.md): **mTLS-only** — no app-layer signatures; `0x12`/`0x13` are retired; `0x14` saves a round-trip, not crypto, and the dominant TLS-handshake cost is W3's resumption target. Cross-language `handshake_skip_auth` golden added. mTLS still enforced on the fast path; coupled to W1's coordinated cutover (see [techdebt.md](techdebt.md)). | — | [fast-path-w2-0x14-fast-path.md](plans/archive/fast-path-w2-0x14-fast-path.md) |
| Fast-Path Reconnect W1: Client-First QUIC Handshake | Realigned the QUIC control-stream handshake to the designed agent-opens/agent-speaks-first model, replacing the Phase-4 server-opens workaround. `handshaker.go` now reads `0x11` AgentHello (`io.ReadFull`) before writing `0x10` ServerHello; `server.go`'s `openControlStream`→`acceptControlStream` calls `AcceptStream` instead of `OpenStreamSync`; the Rust agent (`main.rs`) calls `open_bi` and writes AgentHello first. Cross-language golden fixtures are unaffected (only message *order* changed, not wire encoding) — confirmed via `make golden`, zero `testdata/golden` diff. New integration assertion pins control-stream IDs as even (client-initiated, RFC 9000 §2.1). Rollout is a breaking wire change tracked in [techdebt.md](techdebt.md) (coordinated agent+server deploy required — prod has one agent on Ed25519-signed auto-update); W2–W5 are now complete. | — | [fast-path-w1-client-first-handshake.md](plans/archive/fast-path-w1-client-first-handshake.md) |
| Shell Quality Hardening and Enforcement | Pinned ShellCheck and shfmt now drive one offline runner across local edits, the precommit gauntlet, and CI. Every Bash file has an enforced standalone, aggregator, or library execution class; sourced libraries preserve caller state. Composite-action logic is extracted into tracked scripts, and offline behavioral tests cover the installer, Bastion lifecycle, deploy/rollback state, private Loki transport, and action setup paths. Bash remains the orchestration language; migration to Go is reserved for persistent state, concurrency, complex parsers, or multi-command APIs. | — | [shell-quality-hardening.md](plans/archive/shell-quality-hardening.md) |
| Dormant Multi-Replica Teardown | The server now carries only the production single-replica path: local relay pairing, the slim in-process `SessionRegistry`, shared keys, one-replica Helm deployment, and the standard E2E path. Redis/Sentinel, peer proxying, multiserver harnesses, KEDA, PDB, and multi-node L4 templates are removed. [`Multiscale-Readiness.md`](../docs/Multiscale-Readiness.md) is the rebuild specification. | — | [dormant-scale-out-teardown.md](plans/archive/dormant-scale-out-teardown.md) |
| Docker Hub Download SPOF Hardening | Centralized the Docker daemon mirror configuration in [`.github/actions/docker-hub-mirror/action.yml`](../.github/actions/docker-hub-mirror/action.yml), removing the inline mutation and Sonar fallback copies. All seven Docker Hub pull-capable jobs invoke the composite first and pass optional read-only credentials so direct fallback pulls can avoid anonymous limits. [`docker-hub-mirror.test.sh`](../scripts/tests/docker-hub-mirror.test.sh) enforces the single source, protected job inventory, credential wiring, gated login, and `--password-stdin` handling. | — | [download-spof-hardening.md](plans/archive/download-spof-hardening.md) |
| VM SSH CI Path Retirement | Removed the decommissioned compose-VM deployment jobs and SSH composite actions; OKE Helm jobs are the sole CD path. Load testing now runs k6 through a Service port-forward and the QUIC harness in a short-lived cluster pod. Loki trend scripts are kubectl-only. A gauntlet guard and shell regression suite reject reintroduction of the retired workflow, aliases, variables, and secrets. Continuous deployment and monitoring documentation now reflect the Kubernetes path. | — | [retire-vm-ssh-path.md](plans/archive/retire-vm-ssh-path.md) |
| OKE Free-Tier Block-Volume Remediation | Brought cluster block storage from **450 GB → 200 GB** (the OCI Always-Free cap, ~$6–8/mo overage eliminated) by cutting 5 of 8 PVCs — the lever is **count, not size** (50 GB OCI minimum). Chart changes only ([ADR-035](../docs/adr/ADR-035-oke-free-tier-block-volume-remediation.md)): **(1)** `uptime-kuma` removed entirely (`deploy/helm/monitoring` template + values + its `status.<domain>` ingress deleted) → external uptime SaaS (also catches whole-node outages); monitoring chart now exposes no ingress. **(2)** Grafana `/var/lib/grafana` → `emptyDir` (all config provisioned from ConfigMaps, admin pw re-seeds; only annotations/alert-history lost). **(3+4)** Staging Postgres + server `/data` → `emptyDir` via new `postgres.storage.persistent` / `server.dataVolume.persistent` flags (default **true**; `values-staging.yaml` false, **production unchanged**). **(5)** Backups → OCI Object Storage via a **write-only PAR** (`BACKUP_PAR_URL` Secret) — CronJob rewritten as a `pg_dump` init container + a `curlimages/curl` PUT container sharing an emptyDir (the postgres image has no curl), retention via an OS lifecycle policy. Final tally: prod Postgres + VictoriaMetrics + Loki = **3 block volumes (150 GB)** + node boot (50) = 200 GB; verified by rendering every overlay under `make lint-k8s` (all green). Monitoring docs now point at the OKE topology; residual external follow-ups remain in [techdebt.md](techdebt.md). | — | [oke-block-volume-free-tier.md](plans/archive/oke-block-volume-free-tier.md) |
| OKE Cutover Execution + Compose VM Decommission | Executed the Phase 13b compose→OKE cutover (runbook `docs/Kubernetes-Migration.md` §6) and decommissioned the compose VM `163.192.34.124`. `terraform apply` destroyed `module.compute.oci_core_instance.opengate` (instance + boot volume + public IP); the `compute` module is **kept** as a tested, reusable rollback path but no longer instantiated — root `main.tf`/`outputs.tf` drop the module block, its `moved` block, and the three `instance_*` outputs; `ci.yml` output-sensitivity loop + `integration.tftest.hcl` updated. **Bastion repointed** to the OKE worker-node subnet; [`bastion-session.sh`](../deploy/scripts/bastion-session.sh) resolves the node via `oci ce node-pool get` (`TARGET_USER` ubuntu→opc, dead `tunnel` subcommand removed); `make tunnel`→`kubectl port-forward` for Grafana. **`/observe`** rewritten from VM `docker exec` to cluster `kubectl exec` (VictoriaMetrics/Loki StatefulSets, pod-based Loki labels, ingress-nginx replacing Caddy). Pre-destroy cluster `pg_dump` captured; `ignore_changes` added to the CCM-managed `oke_lb` security list (Terraform was reverting the OCI Cloud Controller Manager's NodePort/health-check rules). Discovered + filed **High techdebt**: in-cluster metrics scraping broken (VictoriaMetrics pod missing SA token/RBAC for `kubernetes_sd` — separate follow-up commit). Open: OKE-node Bastion-plugin enablement (node has public-IP SSH fallback via the `node_ingress_ssh` NSG rule). | — | [decommission-vm.md](plans/archive/decommission-vm.md) |
| Test Determinism + Cross-Language Skip-Guard (ADR-029) | All tests always run deterministically — no silent skips. New leaf pkg [`server/internal/testpg`](../server/internal/testpg/testpg.go) auto-provisions a throwaway `postgres:17-alpine` via testcontainers-go when `POSTGRES_TEST_URL` is unset (`URL()` for `TestMain`, `BaseURL(t)` fails loudly); CI/`make postgres-test-up` external DB still honored; `max_connections=400` matches the existing concurrency budget. Removed **all 8 Go skip sites**: the `testutil.NewTestStore` chokepoint + 4 standalone DB sites (`db`/`amt/transport`/2×`api`) reroute through `testpg`; 3 `GENERATE_GOLDEN` golden generators refactored to always-run (write committed `testdata/golden` only under the flag, temp dir + assert otherwise); 1 `/etc/hostname` platform skip self-provisions in `t.TempDir()`. New hook [`pretooluse-test-skip-guard.sh`](../.claude/hooks/pretooluse-test-skip-guard.sh) (PreToolUse, no bypass) blocks new skip/focus markers across Go (`t.Skip*`), web (`it/test/describe.skip/skipIf/only/todo/fixme` + `xit/xdescribe/xtest/fit/fdescribe(`), Rust (`#[ignore]`); registered in `settings.json`, ruled in [`.claude/rules/tests-determinism.md`](../.claude/rules/tests-determinism.md), 16 new cases in `scripts/tests/hooks.test.sh` (70 total). Lifted the two touched test files back to B+ (Clean-as-You-Code) by extracting shared setup helpers — `relay_test.go` 70.9→82.4, `mps_test.go` 70.8→80.2. | — | — |
| PMAT Quality Overlay (ADR-019) | All 4 ADR-019 integration points, implemented against pinned `pmat@3.17.0` whose CLI/MCP surface differs from ADR-019's literal text (full mapping in [ADR-019 Amendment 1](../docs/adr/ADR-019-pmat-quality-overlay.md#amendments)). **C3 pre-decomposition baseline** (dev HEAD `d3f0373`): repo-score **64.5/100 (C)**; 8 production files + ~36 server tests below B+, web clean. **C4 MCP allow-list**: [`.claude/settings.json`](settings.json) gains `enabledMcpjsonServers:["pmat"]`, 7 read-only `analyze_*` tools allowed, 3 file-writers (`scaffold_project`/`generate_template`/`generate_enhanced_report`) denied — `pmat serve` has no server-side tool filter. **C5 precommit gate**: [`scripts/pmat-precommit.sh`](../scripts/pmat-precommit.sh) runs `pmat tdg check-quality --min-grade B+` on changed code (incl. tests, excl. generated — new `tdd-check.sh is-code` classifier), appended last in [`scripts/precommit-gauntlet.sh`](../scripts/precommit-gauntlet.sh); Clean-as-You-Code (a sub-B+ file blocks only when edited). **C6 nightly**: [`.github/workflows/pmat-trend.yml`](../.github/workflows/pmat-trend.yml) → [`scripts/pmat-summarize.sh`](../scripts/pmat-summarize.sh) → VictoriaMetrics through [`scripts/pmat-vm-push.sh`](../scripts/pmat-vm-push.sh), with previous values read by [`scripts/pmat-vm-query.sh`](../scripts/pmat-vm-query.sh); Grafana `opengate-pmat-trend`; Telegram on ≥3-pt repo-score drop OR below-B+ count rise. New shell tests: pmat-precommit (9), pmat-summarize (13), tdd-check is-code (+15). Kaizen not enabled (`--dry-run` policy). | — | [adr-020-024-pmat-phase-13b-rollout.md](plans/archive/adr-020-024-pmat-phase-13b-rollout.md) |
| ADR-027 Adversarial Pen-Test Gate | Semgrep custom-rule gate (6 rules) + OpenAPI spec-drift over the diff; enforced at commit hook + precommit gauntlet + blocking CI job (required `merge-to-main` check); `semgrep==1.108.0` pinned, diff-only via `--baseline-commit`; HIGH blocks / MEDIUM advisory; `/pentest-review` skill | — | [pentest-gate-semgrep-precommit.md](plans/archive/pentest-gate-semgrep-precommit.md) |
| Phase 0: Scaffolding | Rust workspace, Go module, React/Vite app, CI, Makefile | — | — |
| Phase 1: Shared Protocol | mesh-protocol crate (Rust) + internal/protocol (Go), golden file cross-lang tests | — | — |
| Phase 2: Server Infrastructure | SQLite WAL db (golang-migrate, 8 tables), ECDSA P-256 CA, mTLS, TLS 1.3 | — | — |
| Phase 3: HTTP API + Auth | chi v5 router, JWT+bcrypt, REST handlers, integration tests | — | — |
| Phase 4: Agent Connections | QUIC mTLS (quic-go v0.48), relay, agentapi handshake + conn lifecycle, Rust agent-core | — | — |
| Phase 5: Platform Traits | ScreenCapture/InputInjector/ServiceLifecycle traits, platform-linux, platform-windows stubs | — | [phase-4-platform-agents.md](plans/archive/phase-4-platform-agents.md) |
| Phase 6: Session & WS Relay | Session CRUD, WebSocket relay handler, side detection | — | [phase-6-session-relay.md](plans/archive/phase-6-session-relay.md) |
| Phase 7: Web Client UI | SessionView with tabs (desktop/terminal/files/chat), Zustand stores | — | — |
| Phase 8: Web Client Perf & DRY | Performance and DRY improvements across web client | — | [phase-8-refactoring.md](plans/archive/phase-8-refactoring.md) |
| Phase 9: Agent Session + WebRTC | Terminal forwarding, file ops, signaling state machine, WebRTC transport | — | [phase-9-session-webrtc.md](plans/archive/phase-9-session-webrtc.md) |
| Phase 10: Admin + Notifications | VAPID web push, admin dashboard, service worker | — | [phase-10-admin-notifications.md](plans/archive/phase-10-admin-notifications.md) |
| Phase 11: Intel AMT MPS | CIRA/APF over TLS, RSA 2048 certs, amt_devices table | — | [phase-11-mps-amt.md](plans/archive/phase-11-mps-amt.md) |
| Phase 12: Integration & E2E Tests | Playwright E2E (22 tests/5 files), Go integration expansion, k6 load tests, CI gating | — | [phase-12-e2e-tests.md](plans/archive/phase-12-e2e-tests.md) |
| Phase 14: Agent Auto-Update | Ed25519 signed manifests, QUIC push, rollback watchdog, SELinux restorecon | v0.11.0 | [phase-14-agent-auto-update.md](plans/archive/phase-14-agent-auto-update.md) |
| Broad Codebase Refactoring | 12 work items: DRY helpers, module splits, polish | v0.7.4 | [broad-refactoring-plan.md](plans/archive/broad-refactoring-plan.md) |
| Test Coverage Gaps | 17 cross-component integration tests covering 5 gaps | v0.14.4 | — |
| File Manager + Capability Tabs | File view/download, capability-based session tabs | v0.15.0 | [file-manager-capability-detection.md](plans/archive/file-manager-capability-detection.md) |
| Display Detection Fixes | Socket probing for systemd services, probe_socket helper extraction | v0.15.2–v0.15.6 | [linux-display-detection.md](plans/archive/linux-display-detection.md) |
| X11 Screen Capture + Chat Echo | JPEG capture via x11rb + chat message echo from agent | v0.16.0 | — |
| System Tray with IPC | Desktop agent tray, dedicated desktop CI job | v0.17.0 | — |
| CD Phase A–C: Oracle Cloud Deployment | ARM64 Always Free VPS, Docker Compose + Caddy, GitHub Actions CD, staging/prod | — | — |
| CD Phase D: Monitoring & Observability | VictoriaMetrics, Grafana, Loki, Promtail, Node Exporter, Uptime Kuma, Telegram alerting | v0.13.0 | [phase-d-monitoring-observability.md](plans/archive/phase-d-monitoring-observability.md) |
| CD Phase E (partial): Cosign + Trivy | Cosign keyless signing, SBOM attestation, Trivy image scanning | v0.14.0 | — |
| CD Phase F: Agent Release Pipeline | Auto-tag, changelog, agent binary builds, release pipeline | — | [agent-binary-release.md](plans/archive/agent-binary-release.md) |
| CI Auto-Tag + Changelog | Conventional commit bumping, Keep a Changelog, SYNC_TOKEN trigger | — | [agent-binary-release.md](plans/archive/agent-binary-release.md) |
| Linux Agent: No Tray | Remove tray/IPC crates, Linux = Terminal + FileManager only, delete display detection | — | [linux-agent-no-tray.md](plans/archive/linux-agent-no-tray.md) |
| Dev Workflow Phase 1 | `.claude/` dedup — compact ADR index with wiki cross-refs, archive 47 completed plan files | — | — |
| Linux Agent: Remove Display Detection | Strip X11 capture, input injection, and related deps from platform-linux; Linux = Terminal + FileManager only | — | — |
| Agent Restart + Hardware Inventory | Restart agent from web UI (exit code 42), on-demand hardware inventory (CPU/RAM/disk/NICs) | — | — |
| SonarCloud Quality Gates | SonarCloud scanning, 3-language coverage, SARIF export, merge gating | — | [sonarcloud-quality-gates.md](plans/archive/sonarcloud-quality-gates.md) |
| Dev Workflow Phase 3 | `/observe` skill — PromQL, LogQL, container health, WSL agent diagnostics, investigation playbooks | — | [developer-workflow-enhancement.md](plans/archive/developer-workflow-enhancement.md) |
| Device Logs | On-demand log retrieval via QUIC control path, DB caching, REST API, React UI with filtering/pagination | — | [device-logs-feature.md](plans/archive/device-logs-feature.md) |
| Coverage 80% + Quality Gates | Go/Web coverage to 80%, CI thresholds, precommit enforcement, 3-language coverage badges, SonarCloud hard-fail | — | [coverage-80-flip-gates.md](plans/archive/coverage-80-flip-gates.md) |
| Phase 13a: PostgreSQL Migration | Fresh-start cutover from SQLite to PostgreSQL 17 (pgx/v5 stdlib), native types (TIMESTAMPTZ/UUID/JSONB), now deployed as the app Helm chart StatefulSet with off-cluster `pg_dump` backups and `postgres_exporter` monitoring | v0.24.0 | [phase-13a-postgres-migration.md](plans/archive/phase-13a-postgres-migration.md) |
| Test Coverage Phase A: Targeted Gap-Closers | 10 cross-boundary ControlMessage goldens (session/file/chat/agent_update), 5 edge-case goldens (empty/UTF-8/>64KiB/forward-compat/LE-length negative), Device Logs and capability-tabs E2E specs | — | [tests-coverage-phase-a-targeted-gaps.md](plans/archive/tests-coverage-phase-a-targeted-gaps.md) |
| Structural Testing PR 1: Tooling install | Makefile targets (`mutate`, `taint-go/web`, `dead-code`), web devDeps (stryker, eslint-plugin-security/no-unsanitized, ts-prune), `web/stryker.config.json`, `web/eslint.security.config.js`, `server/.gosec.json`. Developer-facing only; no CI gates | — | [enhance-audit-skills-with-structural-testing.md](plans/archive/enhance-audit-skills-with-structural-testing.md) |
| Structural Testing PR 2: Skill SKILL.md patches | `precommit` (mutation-diff gate, taint, dead-code as new lint steps); `tests-audit` (Section 0.5 mutation analysis); `backend-audit` (taint-paths section + renumbering); `frontend-audit` (DOM taint-paths section + renumbering); `infra-audit` (sensitive-value flow trace); `refactor` (slice-before-you-cut + dead-code sweep); `admin-infra-oci` (post-deployment control-flow validation) | — | [enhance-audit-skills-with-structural-testing.md](plans/archive/enhance-audit-skills-with-structural-testing.md) |
| Structural Testing PR 3: Dead-code baseline cleanup | Rust 0 / Go 4 / TS 11 baseline → all clean. Removed unused `dbStore` interface (meshserver/main.go), `generateAgentCert` test helper (handshaker_test.go), `newTestServerWithAgents` (helpers_test.go), `result` shadow type (loadtest/main.go), and unused `src/test/test-utils.tsx`. Stripped `export` on module-private types (chat-store/connection-store/toast-store/webrtc-transport). Marked protocol-surface types with `// ts-prune-ignore-next-line`. Fixed `make dead-code` to scan via `tsconfig.app.json` and skip generated `src/types/api.d.ts` | — | [enhance-audit-skills-with-structural-testing.md](plans/archive/enhance-audit-skills-with-structural-testing.md) |
| Structural Testing PR 4: gosec baseline cleanup (Go server) | Baseline 56 findings → 0. Fixes: G104 (32) explicit `_ = ` discard or `// #nosec` on best-effort phase transitions / cleanup paths; G115 (8) bounds checks + `clampNonNegativeUint32` / `clampInt64` helpers + `maxAPFPayload` cap; G304 (5) `filepath.Clean` + `// #nosec` justification on operator-supplied paths; G117 (2) `// #nosec` justification on persisted VAPID/signing key files; G301 (2) MkdirAll perms 0755 → 0750; G306 (2) WriteFile perms 0644 → 0600; G401/G501 (2) `//nolint:gosec` → `// #nosec G401/G501` for RFC-7616 Digest auth MD5. Fixed `.gosec.json`: removed `audit: enabled` (was reporting suppressed findings) and `nosec: false` (was being misinterpreted) — minimal config now honors annotations | — | [enhance-audit-skills-with-structural-testing.md](plans/archive/enhance-audit-skills-with-structural-testing.md) |
| Structural Testing PR 5: ESLint security baseline cleanup (web) | Registered `eslint-plugin-security` (`detect-object-injection`, `detect-non-literal-fs-filename`, `detect-non-literal-regexp`, `detect-unsafe-regex`, etc.) and `eslint-plugin-no-unsanitized` in `eslint.config.js` at error severity. Cleared 8 baseline findings (all `security/detect-object-injection` false positives on bracket access): refactored typed Record lookups to switch statements (StatusBadge, SessionToolbar), `forEach` with `arr.at()` (Breadcrumbs), `Uint8Array.from` (NotificationCenter), `Object.fromEntries(filter)` instead of `delete obj[key]` (file-store), bounded array index with `Math.min` + `arr.at()` (DeviceDetail). Added `coverage/`, `reports/` to globalIgnores. `npm run lint` clean, 435 tests pass | — | [enhance-audit-skills-with-structural-testing.md](plans/archive/enhance-audit-skills-with-structural-testing.md) |
| Structural Testing PR 6: Rust mutation-test gap closure (agent) | cargo-mutants baseline 46% (152 caught / 177 missed / 26 unviable / 355 total) → 76.6% (252/76/26/355) by closing test gaps in 11 files. New tests: exhaustive `key_to_bytes` table for terminal_handle.rs (kills 64 match-arm mutants); codec.rs boundary tests (MAX_FRAME_SIZE, decode-incomplete `needed`, single-byte ping/pong); handshake.rs AgentProof short/min payload; logs.rs level_severity table + parse_log_line timestamp validation + matches_filter time boundary inclusivity + discover_log_files truncation; identity.rs partial-state regenerate; file_ops.rs absolute-size CHUNK_SIZE pin; connection.rs AsyncControlStream r/w/x reaches underlying stream + reconnect_backoff no-trailing-sleep + receive_control bounds check; terminal.rs pty_reader/stdin_writer with stub Read/Write; session/handler.rs match-arm dispatch coverage (mouse click, key press, terminal resize, file download, ICE, switch ack, file upload silent ack); session/relay.rs capture_loop error counting + ws_writer_loop forwarding (function generalized over `Sink<Message>` for testability); webrtc.rs store_channel_by_label routing. Carve-outs in `agent/.cargo/mutants.toml`: platform shims, `mesh-agent/src/main.rs` entry point, `restore_selinux_context`. Golden tests support `OPENGATE_GOLDEN_DIR` so cargo-mutants' temp tree resolves fixtures. `make mutate-rust` sets the env automatically | — | [enhance-audit-skills-with-structural-testing.md](plans/archive/enhance-audit-skills-with-structural-testing.md) |
| Structural Testing PR 7: Go mutation-test gap closure (server) | gremlins baseline (with Postgres test DB up + carve-outs for openapi_gen / cmd-meshserver / loadtest / testutil): 567 K / 68 L / 7 T / 140 NC / 782 total = 73.4% caught → **77.9% (602 K / 33 L / 7 T / 140 NC)**. 35 LIVED killed (efficacy 89.3% → 94.8%). New tests: cert validity-period pinning (kills all 10 ARITHMETIC_BASE on `5*time.Minute` / `365*24*time.Hour` / `10y` for CA/Agent/Server/MPS/AgentCSR); relay `msgs_copied` log assertion via captureHandler (kills `count++`); api server `MetricsRegistry`/`Metrics` wiring test (kills `!= nil` checks at api.go:131,139); agentapi `clampNonNegativeUint32` / `clampInt64` boundary tests; agentapi UpdateAck DB-state assertion (kills `msg.Success != nil` mutation that would silently mis-persist outcome); apf.go boundary battery (10 sub-tests for `strLen > maxAPFStringLen` at 6 read-points + ParseGlobalRequest off-boundary + ParseChannelOpen off+12 + ParseChannelData len-4 with error-message distinguishing + WriteChannelData maxAPFPayload + readChannelData 1MiB cap + writeStringMsg + ReadGlobalRequest/ChannelOpen name-dispatch); converters `Offset+Limit == total` no-more pin; install-script `len(prefix) > 0` empty-context test; protocol MaxFrameSize literal pin; vapid private-key 32-byte pin (kills `len(privBytes) < 32` padding loop). Carve-outs in `server/.gremlins.yaml`: openapi_gen, cmd-meshserver/main, tests/loadtest/main, internal/testutil. `make mutate-go` warns if `POSTGRES_TEST_URL` is unset (otherwise api/db tests skip and ~290 mutants drop to NOT COVERED) | — | [enhance-audit-skills-with-structural-testing.md](plans/archive/enhance-audit-skills-with-structural-testing.md) |
| Structural Testing PR 8: Web mutation-test gap closure | stryker baseline (3327 mutants across 62 files, no carve-outs): 1097 K / 110 T / 686 surv / 392 NC / 1042 errors = **52.82% total / 63.76% covered** → **70.96% total / 77.11% covered (1244 K / 90 T / 396 surv / 150 NC / 851 errors)**, 4 iterative test-and-config rounds, target ≥70% met. Lifted across 4 stryker rounds (52.82 → 61.67 → 66.22 → 67.76 → 70.96). Carve-outs in `web/stryker.config.json` (analogous to PR 6 platform shims): `src/main.tsx` (bootstrap), `src/router.tsx` (declarative route table — StringLiteral mutants on path strings aren't real bugs), `src/features/terminal/use-terminal.ts` (xterm hook), `src/features/remote-desktop/use-remote-desktop.ts` (WebRTC + Worker), `src/features/remote-desktop/input-handler.ts` (DOM event → wire-protocol shim), `src/state/connection-store.ts` (WebRTC signaling handlers — handleSignalingMessage WebRTCAnswer/IceCandidate/SwitchAck branches require RTCPeerConnection which jsdom doesn't simulate). `ignoreStatic: true` (Stryker recommended; 278 module-level mutants would consume ~76% of test time). New tests / strengthening across 19 files: new `src/state/api-action.test.ts` (pins `loading=true/false` paths + initial-state literals); state stores admin/session/push/amt/device/file/toast (assert exact `set()` call shapes, both branches of `if (res.ok)`, ring-buffer slice ordering); component tests Breadcrumbs (per-route href + label), DeviceList (debounced filter case-insensitivity, upgrade-all toast levels), DeviceCard (timeAgo boundary table — exactly 60s, 60min, 24h), Dashboard (admin vs non-admin fetchAuditEvents, slice(0,10) cap, Recent Activity gate), AgentSetupPage (typed-in label/max_uses/expires_in_hours, form reset on submit, expired-vs-active badge), AuditLog (limit:50/offset:0 payload, filter typing, Next pagination), SessionToolbar (per-state color class + Unknown default), GroupSidebar (trim newName, whitespace short-circuit, two-click confirm-delete), codec (MAX_FRAME_SIZE boundary `>` not `>=`, hex-pad `padStart(2, '0')`). 470 → 512 vitest tests | — | [enhance-audit-skills-with-structural-testing.md](plans/archive/enhance-audit-skills-with-structural-testing.md) |
| Structural Testing PR 9: Mutation testing as observability | Mutation testing runs nightly as a non-merge-blocking Rust/Go/web matrix. [`mutation-summarize.sh`](../scripts/mutation-summarize.sh) aggregates per-language reports into a canonical artifact, [`mutation-vm-push.sh`](../scripts/mutation-vm-push.sh) publishes numeric series to VictoriaMetrics, and the `opengate-mutation-trend` PromQL dashboard provides the trend surface. The workflow preserves its existing regression/Telegram rules and exits red on regression for audit visibility without joining `merge-to-main.needs[]`. | — | [pr9-mutation-testing-as-observability.md](plans/archive/pr9-mutation-testing-as-observability.md) |
| Claude Hooks PR 1: Foundation | Foundation for porting CLAUDE.md mandatory directives into deterministic Claude Code hooks. `scripts/tdd-check.sh` shared classifier (`is-source <path>` / `has-test-change`) + 24-test runner under `scripts/tests/tdd-check.test.sh` (plain bash, no bats dependency, shellcheck clean). `.claude/conventions.md` promoting 8 project-relevant memories from `~/.claude/projects/-home-ivan-opengate/memory/` into a committed file (use-make-e2e, no-silent-skip, precommit-per-pr, zero-manual-install, wiki/docs-updates-per-phase, plans-location, numbered-list editing protocol, no-backup-script scope rule). `.claude/.markers/.gitkeep` + `.gitignore` entries so PR 2 hook marker files (`precommit.head`, `refactor.head`) are local-only. Each MANDATORY block in `CLAUDE.md` gains "Enforced by `.claude/hooks/<name>.sh` (PR 2). No bypass." annotations. **Behavior unchanged** — no hook scripts, no `settings.json` hook stanzas; activation lands in PR 2. | — | [claude-hooks-port-mandatory-directives.md](plans/archive/claude-hooks-port-mandatory-directives.md) |
| Claude Hooks PR 2: Hook activation | Activates the deterministic enforcement layer for CLAUDE.md MANDATORY blocks. 6 new hooks under `.claude/hooks/`: `pretooluse-tdd-gate.sh` (primary TDD enforcement — blocks first source-file Write/Edit/MultiEdit on a branch with no test change), `pretooluse-bash-source-write-guard.sh` (catches `echo > foo.go`, `sed -i`, `tee` against source paths), `pretooluse-git-commit-guard.sh` (Co-Authored-By, --no-verify, identity, branch-not-main, behind-upstream, precommit-marker, TDD backup), `pretooluse-git-push-guard.sh` (no main, no force-push to main, behind-upstream, refactor-marker for source commits), `pretooluse-write-guard.sh` (plans-dir, ADR immutability, NOSONAR/nolint/sonar.issue.ignore/eslint-disable), `session-start-context-load.sh` (TL;DR mandatory rules + In-Progress/Planned/last-10-Completed phases + Critical/High techdebt + prior-session blocks). Shared `lib/common.sh` (JSON parsing via python3, fail-closed `trap`, blocks.log writer). `/precommit` SKILL.md appended with `git write-tree > .claude/.markers/precommit.head` final step + TDD-interaction note; `/refactor` SKILL.md appended with `git rev-parse HEAD > .claude/.markers/refactor.head` final step. `.claude/settings.json` registers all hooks across SessionStart, PreToolUse Write\|Edit\|MultiEdit, and PreToolUse Bash matchers. 48-test runner under `scripts/tests/hooks.test.sh` exercises every rule (block + allow scenarios, no-bypass verification). NO bypass mechanism of any kind — editing `.claude/settings.json` is the only way to change enforcement. Hooks fail-closed on internal error. | — | [claude-hooks-port-mandatory-directives.md](plans/archive/claude-hooks-port-mandatory-directives.md) |
| Test Coverage Phase B: Coverage Depth | 5 PRs (B1–B5) targeting Phase B structural gaps. B1: Session + terminal E2E (`web/e2e/session-terminal.spec.ts`, 4 specs) covering Start Session flow, WebSocket auth URL composition, disconnect navigation, offline error toast. B2: File Manager E2E (`web/e2e/file-manager.spec.ts`, 3 specs) with full wire-level coverage — encodes msgpack control frames in the same `[type=0x01][BE-len][msgpack]` envelope as production, so the codec → connection-store → file-store → FileManagerView pipeline runs against real bytes. B3: WSMAN unit + wire tests (`server/internal/mps/wsman/client_wire_test.go`) drove package coverage 49.7% → 95.0% via a minimal `MPSConn` interface + `net.Pipe`-based wire harness; ci.yml and sonar-project.properties exclusions for `mps/wsman/*` retired (overall Go coverage 81.2% → 82.0%). B4: Postgres-native invariants (`server/tests/integration/postgres_native_test.go`, 7 specs) pin TIMESTAMPTZ UTC normalisation, JSONB unicode/empty-slice round-trip, UUID rejection at boundary, UUID case normalisation, 32-goroutine concurrent UpsertDevice, 200× prepared-statement cache reuse, post-error pool resilience. B5: Control-stream fault injection (`server/tests/integration/control_stream_faults_test.go`, 3 specs) covers corrupted msgpack payload, partial-frame-then-close, and send-after-stream-close → device reconciles to offline; concurrent server-initiated sends deferred (techdebt: `AgentConn.sendControl` needs write mutex). | — | [tests-coverage-phase-b-coverage-depth.md](plans/archive/tests-coverage-phase-b-coverage-depth.md) |
| IaC Security Testing Pyramid (S1–S4) | 4-PR rollout layered on the new terraform foundation. S1: gitleaks secrets scanning — `.gitleaks.toml` with categorical (path/regex) allowlists for test files and example templates, `make secrets-scan` wired into `lint-deploy` + CI's `config-lint` job, `gitleaks protect --staged` added as step 6.1 of the `/precommit` skill, TDD canary at `deploy/tests/fixtures/leaked-secret.txt`. S2: Checkov (terraform + dockerfile + github_actions frameworks; secrets framework intentionally omitted — owned by gitleaks; docker_compose not a Checkov framework — owned by Conftest in S3) + Hadolint with `# hadolint ignore=DL3018` for ca-certificates/tzdata. `.checkov.baseline` pins CKV_OCI_4/5 (require VPS rebuild) and CKV_OCI_17 (design choice — stateful ingress required); ADR-015 records the Trivy/Checkov overlap and the baseline-as-suppression model. Added Dockerfile HEALTHCHECK (busybox wget) to clear CKV_DOCKER_2. S3: Conftest + Rego (OPA 0.67 Rego v0 syntax) — `policy/terraform/compute.rego` (4 Always-Free invariants), `policy/terraform/tags.rego` (env+component freeform_tags on the instance), `policy/docker_compose/images.rego` (deny `:latest`/no-tag; digest-pinning deferred), `policy/github_actions/pinning.rego` (third-party `uses:` must be 40-char SHA; allowlist for actions/github/oracle-actions/docker). 22 Rego unit tests across 4 policy bundles, all green. SHA-pin sweep replaced 14 third-party action references across all workflows + composite actions. Added `freeform_tags = { env, component, managed_by }` to compute module (next apply will flush). S4: `.github/workflows/iac-plan-preview.yml` triggers on `pull_request` touching `deploy/terraform/**` — reuses `tf-drift-reader` IAM (no new principal), runs `terraform plan -out=plan.tfplan`, parses via new `deploy/scripts/parse-tfplan.sh` (5 protected types: vcn/subnet/security_list/nsg/objectstorage_bucket), posts a sticky PR comment via `actions/github-script`, blocks merge on protected-destroy unless PR has `iac:approve-destroy` label. `make test-parse-tfplan` exercises the gate-decision matrix with 3 canned fixtures (`deploy/tests/fixtures/tfplan/{no-changes,safe-add,destroy-protected}.json`). | — | [iac-security-testing-pyramid.md](plans/archive/iac-security-testing-pyramid.md) |
| Terraform: state + decomposition + tftest + drift | 4 PRs (T1–T4). T1 moved tfstate to OCI Object Storage with bucket versioning. T2 decomposed the root into networking and compute modules with state-preserving `moved` blocks. T3 added `terraform test`, policy, and output-sensitivity invariants. T4 added nightly read-only refresh-only drift detection: canonical summaries and artifacts, Telegram notification, VictoriaMetrics publication through [`terraform-drift-vm-push.sh`](../scripts/terraform-drift-vm-push.sh), and a red audit trail on drift. No auto-remediation. | — | [terraform-test-and-remote-state.md](plans/archive/terraform-test-and-remote-state.md) |
| Markdown Guideline Modularization | CLAUDE.md (122 lines) split into a 42-line index pointing at 8 topical rule files under `.claude/rules/` (git, tdd, precommit-refactor, sonarcloud, plans-and-adrs, code, editing-and-scope, tooling) — each file ≤55 lines with consistent header (enforcement attribution + rule body). `.claude/conventions.md` dissolved into the rule files. `AGENTS.md` is a tracked symlink to `CLAUDE.md` for cross-tool compatibility. All 6 hooks' error messages re-pointed from `CLAUDE.md §<section>` to `.claude/rules/<topic>.md`; `session-start-context-load.sh` TL;DR now cites `.claude/rules/tooling.md` and the `.claude/rules/` index. 8 duplicated feedback memories deleted from `~/.claude/projects/-home-ivan-opengate/memory/` (`feedback_use_make_e2e`, `feedback_no_backup_script`, `feedback_wiki_updates`, `feedback_zero_manual_install`, `feedback_plans_location`, `feedback_numbered_lists`, `feedback_precommit_per_pr`, `feedback_no_silent_skip`); `MEMORY.md` Feedback section replaced with single-line pointer to `.claude/rules/`. 48/48 hooks tests pass unchanged. | — | [modularize-md-guidelines.md](plans/archive/modularize-md-guidelines.md) |
| Test Coverage Phase C / C1: Cross-language Protocol Safety | Bidirectional golden file tests + `.meta.json` sidecars + Go native fuzzing. **Reverse goldens (Go → Rust)**: new `server/internal/protocol/golden_reverse_test.go` writes `testdata/golden/go_<variant>.bin` under `GENERATE_GOLDEN=1` for 8 representative variants (ping/pong/heartbeat/agent_register/session_request/chat_message/restart_agent/desktop_frame); new `agent/crates/mesh-protocol/tests/reverse_golden_test.rs` decodes each via `Frame::decode` and asserts canonical struct values. Closes "Golden File Tests Are One-Directional" tech-debt (open since Phase 1). **Sidecars**: every `.bin` gets a `<variant>.meta.json` with `{variant, protocol_version=0, format=msgpack/binary/frame-only, created}`; `TestGoldenSidecarsExist` enforces coverage; 43 sidecars committed (35 forward + 8 reverse). **Go native fuzzing**: new `server/internal/protocol/codec_fuzz_test.go` with `FuzzReadFrame` + `FuzzDecodeControl`, seed corpus from the 35 forward goldens (envelope-level for ReadFrame, payload-level for DecodeControl); local 5s runs hit 555k execs/445k execs with no panics. **Makefile**: `golden` target now runs Rust forward generate → Go reverse generate → Go forward verify → Rust reverse verify in one pass. **CI**: `golden` job sets up both Go and Rust toolchains; runs `GENERATE_GOLDEN=1 go test ./internal/protocol/ -run "TestGenerateReverseGoldens\|TestGenerateForwardSidecars"` then both verifications then `git diff --exit-code -- testdata/golden/`. ADR-016 records the bidirectional + sidecar design (extends ADR-002). C2 (web QA) and C3 (backend hygiene) remain pending. Bundled with the Markdown Guideline Modularization commit. | — | [tests-coverage-phase-c-structural-hardening.md](plans/archive/tests-coverage-phase-c-structural-hardening.md) |
| Test Coverage Phase C / C3: Backend Test Hygiene + Fault Injection | 4 deliverables. **(15) `time.Sleep` → `require.Eventually`**: 17 fixed-duration sleeps across 8 test files replaced with poll-based synchronisation (`session_test.go`, `admin_test.go`, `agentapi_test.go`, `middleware_ws_test.go`, `relay_data_test.go`, `security_groups_test.go`, `relay_handler_test.go`, `handlers_restart_test.go`). Audit-log waits poll `QueryAuditLog`; relay-wired waits use the new `waitForRelayWired` helper that calls `relay.WaitForPeer` (the natural synchronisation primitive — `ActiveSessionCount` increments on the first Register, not on the pair). The 32-second `middleware_ws_test.go` sleep is now an explicit 2-second heartbeat loop over 32 s. Two cleanup sleeps now wait on `ListenAndServe` exit channels rather than blind 50 ms delays. **(NEW) `AgentConn.sendControl` write mutex**: `protocol.Codec.WriteFrame` issues a 5-byte envelope write followed by an N-byte payload write; without a mutex two concurrent server-initiated sends could interleave on the same QUIC stream and corrupt the frame seen by the agent. Added `writeMu sync.Mutex` to `AgentConn` + `writeFrame` helper used by both `sendControl` and the FramePong response in `handleControl`. Closes the [techdebt entry](techdebt.md) identified 2026-05-14. **(NEW B5 deferred case)**: new `TestControlStream_ConcurrentServerInitiatedSendsArriveDecodable` in `server/tests/integration/control_stream_faults_test.go` fires `SendRequestHardwareReport` + `SendRequestDeviceLogs` concurrently, reads two frames from the agent side, asserts both decode cleanly and each message type appears exactly once. Verified to fail reliably (~3/5 runs) without the mutex; passes 10/10 with it. **(NEW) Relay fault injection**: new `server/tests/integration/relay_faults_test.go` with 3 specs — `TestRelayFaults_BrowserClosesMidStream` (browser `CloseNow` mid-traffic; agent reads close, `ActiveSessionCount → 0`), `TestRelayFaults_AgentClosesWithBufferedTraffic` (16-message burst from agent then immediate close; browser drains, no nil writes), `TestRelayFaults_ConcurrentBidirectionalOrdering` (per-direction order preserved over 200 messages each way). **(16) `t.Parallel()` audit**: `testutil.NewTestStore` refactored to per-test schema isolation (each call creates a fresh `ogt_<uuid>` schema, runs migrations on it, drops it on `t.Cleanup`) — replaces the prior per-process shared schema + TRUNCATE-between-tests pattern that explicitly forbade `t.Parallel()`. Added `db.NewPostgresStoreWithOptions(MaxOpenConns, MaxIdleConns)` so test stores cap at 3 conns each; a `liveStoreSem` semaphore (16/process) throttles concurrent schemas. CI postgres switched from `services:` block to a manual `docker run` step with `-c max_connections=400` (services block can't override the postgres command). Added `make postgres-test-up` for local dev. **~70 top-level integration + api tests** now call `t.Parallel()`; full server suite `make test-go -race` measured **44.6 s wall (was ~145 s sequential)** = **~69% drop**, well above the plan's 30–40% target. | — | [tests-coverage-phase-c-structural-hardening.md](plans/archive/tests-coverage-phase-c-structural-hardening.md) |
| Mutation Testing PR 10: Bump floor to 85% + DeviceDetail/web survivor cleanup | Push web mutation score from the PR-8 close of **70.96% → ≥85%** by attacking surviving mutants in 16 top files. **DeviceDetail.tsx**: 103 survivors → 3 (99.18% covered) via `formatBytes` boundary battery (0 B, 1.0 KB, 1.0 MB, 1.0 GB, 1024 TB clamp, 99.0 MB at decimal pivot), AMT section coverage (offline/online status colors, model middot presence/absence, hard_reset confirm flow, instructions arrow rotate-90), Restart/Upgrade disabled-state battery, Move-to-Group filter+reset path, polling-stops-on-unmount, navigate-state pin (relayUrl + capabilities), latestManifest numeric-sort + per-OS filter. **Other top files** (~200 new tests): FileManagerView (formatSize 1024^n boundary + navigate root/up + initial-FileListRequest), DeviceList (plural/singular upgrade-all phrasing, numeric version compare, per-OS scoping, label literal), Permissions (auto-select first group, blue/gray tab class, parenthetical display_name, dropdown reset on add), AgentUpdates (cleanup plural pivot at count===1, "Cleaning..." in-flight label, max_uses 0-fallback, em-dash empty label, /max suffix gate), UserManagement (yes/no admin toggle + Delete + admin/regular row pinning), MessengerView (trim suppresses whitespace, Shift+Enter passthrough, browser-vs-agent bubble class, ignored non-Chat payloads, cleanup on unmount), SessionView (tab content swap for Desktop/Files/Chat, tab class active vs inactive, connect/disconnect lifecycle), AgentSetupPage (loading gate + admin-only fetch + Copied! pivot), ProfilePage (success/error toast + Saving... + PATCH body pin), GroupSidebar (Confirm? title + tab toggle Cancel↔+New), DeviceLogs (level color fallback, Math.min total clamp, level dropdown roster), InstallInstructions (active platform class + manual rotate + no-manifest), Dashboard (offline=total-online, audit limit:10 admin-only), LoginPage (button label + only-token vs only-user guard), Breadcrumbs (Dashboard href + separator + hostname swap guard). **Carve-outs** in `web/stryker.config.json`: `src/lib/transport/webrtc-transport.ts` and `src/features/admin/NotificationCenter.tsx` (analogous to existing PR-8 WebRTC/service-worker carve-outs in [`connection-store.ts`](../web/src/features/session/state/connection-store.ts), [`use-remote-desktop.ts`](../web/src/features/remote-desktop/use-remote-desktop.ts), [`input-handler.ts`](../web/src/features/remote-desktop/input-handler.ts) — jsdom does not simulate RTCPeerConnection / ServiceWorker / Push API). **Floor bump**: `REGRESSION_FLOOR_PCT` in `scripts/mutation-summarize.sh` and the matching Telegram-text branch raised from `70.0 → 85.0`; `.github/workflows/mutation.yml` comment + step-summary table updated; `docs/Testing.md` regression-rule line bumped to **85%**. **Vitest report consolidation**: the workflow's `GITHUB_ACTIONS=''` env override was being inherited inconsistently by Stryker's persistent vitest pool; the canonical fix lives in `web/vitest.config.ts` — when `STRYKER_MUTATOR_WORKER` is in env, `reporters: ['default']` is set explicitly, which short-circuits vitest's `if (!resolved.reporters.length) ... GITHUB_ACTIONS === 'true'` auto-load branch (see `vitest/dist/chunks/coverage.*.js`). Normal CI test jobs (`web-unit`, `web-integration`) keep the auto-loaded github-actions reporter for failure annotations. `GITHUB_STEP_SUMMARY=/dev/null` stays as a belt-and-suspenders backstop. **Rust no_coverage encoding**: `parse_rust` in `scripts/mutation-summarize.sh` now emits `no_coverage: null` (not `0`) — cargo-mutants does not distinguish "no coverage" from "missed"; the table renders `null` as `—`. **Rust score**: pre-PR 83.6% (272 K / 54 S / 3 T / 26 unviable). Of the 54 survivors, **22 live in `mesh-agent-core/src/webrtc.rs`** (peer-connection / ICE / data-channel callback wiring against a live STUN stack). Added `crates/mesh-agent-core/src/webrtc.rs` to `agent/.cargo/mutants.toml` `exclude_globs` — analogous to the existing platform-shim and the new web-side `webrtc-transport.ts` carve-outs, both gated on "needs real RTCPeerConnection / network". After the carve-out the denominator drops to 304 (-25) and the numerator drops to 272 (-3 caught) → **score 272/304 = 89.5%** (verified against the local `mutants.out/missed.txt` count). One unkillable `>` → `>=` mutant on `discover_log_files` at `files.len() == MAX_LOG_FILES`: `split_off(0)` returns the entire vec, so the two operators are behaviorally equivalent at that boundary — documented in place rather than carved out. Other surviving mutants outside webrtc.rs (Vec::with_capacity arithmetic, log-only branches, default_shell return string) are intentionally retained as latent observability signal. 191 new vitest tests; total 502 → 693. **Verified web score post-PR**: 86.33% absolute (1559 K / 197 S / 50 NC / 1 T / 863 errors), up from PR-8 close of 70.96% → +15.4pp. Phase C / Structural-Hardening plan archived to `plans/archive/`. | — | — |
| Stable dev-machine VPS access via OCI Bastion | Original Bastion rollout verified live: `terraform apply` clean (1 add bastion, 2 in-place updates, 0 destroys); bastion `ACTIVE`; Cloud Agent Bastion plugin enabled (status `STOPPED` on first apply — transitions to `RUNNING` within ~5–15 min as the agent polls). New `deploy/terraform/modules/bastion/` (oci_bastion_bastion STANDARD, original target = `opengate-public-subnet`, IAM-gated `client_cidr_block_allow_list = ["0.0.0.0/0"]`, `max_session_ttl_in_seconds = 10800`; current target is the OKE worker-node subnet per ADR-018). Networking module added the original TCP 22 public-subnet rule and introduced `locals.public_subnet_cidr` to break the would-be subnet ↔ security_list resource cycle; the active human SSH path now resolves the OKE worker node via `oke_node_pool_id`. Root `outputs.tf` keeps `bastion_id` (sensitive); retired VM instance outputs were removed during the OKE cutover. `deploy/scripts/bastion-session.sh` (pure bash + OCI CLI, shellcheck clean) caches the active session OCID at `~/.cache/opengate/bastion-session.json` with 5-min headroom on the 3h TTL; `make ssh` opens a node shell, while `make tunnel` is now a Kubernetes Grafana port-forward. New ADR-018 + decisions.md index row. docs/Infrastructure.md "Operator access via OCI Bastion" section (daily flow, per-user IAM onboarding, verification commands, failure-mode runbook). Tests: 5 new `bastion.tftest.hcl` runs + 1 new networking bastion-ingress run + 1 new compute bastion-plugin run + 1 new integration bastion-targets-subnet run = 8 new + 9 existing = 17 passing terraform-test runs total. | — | [stable-dev-machine-vps-access.md](plans/archive/stable-dev-machine-vps-access.md) |
| Path-gate `release-agent.yml` + agent-side sha256 precheck | Stops the workflow from rebuilding + publishing Rust agent binaries on every `feat:`/`fix:` merge that doesn't touch `agent/**` (14% of recent commits touched agent; 86% were triggering spurious releases). **Workflow gate:** new `check-agent-changed` job in [`.github/workflows/release-agent.yml`](../.github/workflows/release-agent.yml) at workflow entry runs [`scripts/release-agent-gate.sh`](../scripts/release-agent-gate.sh) (pure bash, validates the tag exists, finds the previous `v*` via `git describe HEAD^`, diffs `agent/**`, emits `agent_changed=true\|false` + `prev_tag=...`); `build` job gains `needs: check-agent-changed` + `if: agent_changed == 'true'`; `release` already `needs: build`, so a skipped build cleanly produces no GitHub Release (preserving `/releases/latest` continuity for `install.sh`). Also moved `permissions: contents: write` from workflow-level to the `release` job only (closes a pre-existing SonarCloud finding — `check-agent-changed` and `build` now run with read-only token). **Agent precheck:** new step 0 in [`apply_update`](../agent/crates/mesh-agent-core/src/update.rs) hashes the running binary and short-circuits with `Ok(false)` when it already matches the manifest `sha256` — belt-and-suspenders for `workflow_dispatch` paths and the future auto-publish-manifest enhancement. Bypassed when `sha256` is empty (legacy manifests) or the binary path doesn't exist. **Tests (TDD):** 6 new bash tests in [`scripts/tests/release-agent-gate.test.sh`](../scripts/tests/release-agent-gate.test.sh) (first-release, unchanged-skip, changed-build, missing-arg, unknown-tag, deep-subdir); 4 new Rust tests in `update::tests` (precheck-skips-on-match with `mockito.expect(0)`, precheck-bypassed-on-empty-hash, precheck-bypassed-on-missing-binary, proceeds-on-hash-differs). Total agent-core update tests 12 → 16, all green. **Docs:** new "Content-Hash Precheck" section in [`docs/Agent-Updates.md`](../docs/Agent-Updates.md). **Deferred (separate plan):** path-gate `build-image.yml` on `server/**`+`web/**`, determinize the agent build (`[profile.release]` + `SOURCE_DATE_EPOCH`), auto-publish manifest in CI. | — | [path-gate-agent-release.md](plans/archive/path-gate-agent-release.md) |
| Test Coverage Phase C / C2: Web QA Cross-Cutting | 4 deliverables. **(12) Playwright Firefox + WebKit nightly**: `web/playwright.config.ts` now toggles Firefox + WebKit projects via `PLAYWRIGHT_ALL_BROWSERS=1` (Chromium-only by default for PR CI; `retries: 0 → 1` for the cross-browser path since WebKit occasionally flakes in Docker). New workflow `.github/workflows/e2e-cross-browser.yml` runs at 03:00 UTC and on `workflow_dispatch`; **does NOT gate merges**. Brings up the docker compose stack, installs Playwright browsers with `--with-deps`, runs the full suite across all 3 engines, uploads the report artifact with 14-day retention. **(13) Visual regression baselines**: new `web/e2e/visual-regression.spec.ts` with 4 Chromium-only PNG baselines (login, register, device-list-empty, admin-user-management). `maxDiffPixelRatio: 0.01` tolerates font-rendering jitter. Drift requires explicit `npx playwright test e2e/visual-regression.spec.ts --update-snapshots` + PR review. Audit-log baseline intentionally excluded (timestamps + per-run event count make the screenshot inherently flaky); heavy specs (populated device list, session view, file manager, filtered device logs) deferred to a follow-up because they need fixture-driven backend state (live QUIC agent, files in a real sandbox). **(14) Accessibility gate**: added `@axe-core/playwright@4.11.3` devDep; new `web/e2e/a11y.spec.ts` runs WCAG 2.1 A/AA checks on 5 specs (login, register, device list, admin user management, admin audit log) via `AxeBuilder({page}).withTags(["wcag2a","wcag2aa","wcag21a","wcag21aa"])`. New violations block merge; pre-existing rules waived in the in-spec `WAIVED_RULES` set + the reviewer-facing `web/e2e/a11y-baseline.json` (3 rules waived for a future design pass: `color-contrast`, `link-in-text-block`, `link-in-text-block-style`). **(NEW) Web API codegen drift gate**: new step on the `Web Lint` CI job runs `npm run generate:api` and asserts `git diff --exit-code -- src/types/api.d.ts` is clean — mirrors the Go-side `oapi-codegen` drift gate so server-only OpenAPI edits cannot silently drift from the TypeScript client surface. Updated `web/tsconfig.node.json` to include `playwright.config.ts` so `process.env` typing resolves under strict mode. | — | [tests-coverage-phase-c-structural-hardening.md](plans/archive/tests-coverage-phase-c-structural-hardening.md) |
| ADR-021 #8: `auth` User repository extraction | Eighth and final per-aggregate extraction off `db.Store` (7 → 2 methods — only `Ping`/`Close` remain; ADR-021's `<30` method target now exceeded by 28). 3 new files in `server/internal/auth/`: `user.go` (`User` + `UserID` alias + `ErrUserNotFound` sentinel + 5-method `UserRepository` interface), `postgres_user.go` (`PostgresUsers` adapter), `instrumented_user.go` (`InstrumentedUsers` decorator — reuses the existing duck-typed `Observer` already defined for SecurityGroups). The `auth` module now co-owns `User` + `SecurityGroup` — the users table is JOINed by `SecurityGroup.ListMembers` and mutated by `syncIsAdmin` (Add/RemoveMember mirrors `AdminGroupID` into `users.is_admin`); both couplings stay local. `db.User` aliased to `auth.User`. `api.Server` grows a `users auth.UserRepository` field; `handlers_users.go` + `handlers_auth.go` rewire onto it (5 calls → `users.Upsert/Get/GetByEmail/List/Delete`); `auth.ErrUserNotFound` replaces `db.ErrNotFound` in user-lookup branches; `handlers_security_groups.go` `AddMember` user-existence check also rewires. The shared `db` helpers `execAndCheckAffected` / `queryListPG` / `queryOnePG` / `scanner` interface are removed — all aggregate methods that consumed them have now moved out. New `testutil.NewTestUsers(t, store)`; `SeedUser`/`SeedAdminUser` return `*auth.User` via the extracted repo. 9 api test files + 2 integration test files get the `Users` field on every `ServerConfig{}` literal. authorization/user/store_failure/session_handlers/auth_edge/securitygroup test files (10 call sites) switch from `store.*` User methods to `users.*` / `testutil.NewTestUsers`. Removed: 5 db.Store methods + 5 `PostgresStore` impls + 5 `InstrumentedStore` wrappers + `TestUserCRUD` block (~80 lines) + unused `errors`/`uuid` imports from `db/store_test.go`. Full gauntlet green. | — | [modular-monolith-evaluation.md](plans/archive/modular-monolith-evaluation.md) §9 |
| ADR-021 #7: `session` module extraction (AgentSession) | Seventh per-aggregate extraction off `db.Store` (11 → 7 methods). New `server/internal/session/` package with 4 files: `session.go` (`Session` + `ErrSessionNotFound` sentinel + 4-method `Repository` interface), `postgres.go` (`PostgresSessions` adapter), `instrumented.go` (decorator + duck-typed `Observer`), `session_test.go`. Clean leaf module — no internal imports (stdlib + uuid only). `db.AgentSession` aliased to `session.Session`. `api.Server` grows a `sessions session.Repository` field; `handlers_sessions.go` rewires onto it (Create/Get/Delete/ListActiveForDevice — the extracted method names are cleaner than the old `db.Store` names); `handlers_relay.go` `validateRelayToken` uses `s.sessions.Get`; `converters.go` `sessionToAPI` takes `*session.Session`. `main.go` `OnSessionEnd` cleanup hook uses `sessionsRepo.Delete` instead of `store.DeleteAgentSession`. `.go-arch-lint.yml` adds the `session` component (deny-all internal, deepScan off — same pattern as audit/update/auth/device/notifications). New `testutil.NewTestSessions(t, store)`. `SeedAgentSession` uses the repo and returns `*session.Session`. 8 api test files + 2 integration test files get the `Sessions` field on every `ServerConfig` literal. Removed: 4 db.Store methods + 4 `PostgresStore` impls + 4 `InstrumentedStore` wrappers + `TestAgentSessionCRUD` block (~70 lines) + the now-unused `seedUser`/`seedGroup`/`seedDevice` helpers and `device` import from `db/store_test.go`. Full gauntlet green. | — | [modular-monolith-evaluation.md](plans/archive/modular-monolith-evaluation.md) §9 |
| ADR-021 #6: `amt` repository extraction (AMTDevice) | Sixth per-aggregate extraction off `db.Store` (15 → 11 methods). 4 new files in `server/internal/amt/`: `repository.go` (4-method `Repository` interface + `ErrAMTDeviceNotFound` sentinel), `postgres.go` (`PostgresAMTDevices` adapter), `instrumented.go` (decorator + duck-typed `Observer`), `repository_test.go`. `AMTDevice` + `DeviceStatus` types deliberately remain in `db` (the amt module imports mps for its Service, and mps needs to upsert/set status on AMT device rows — moving the type into amt would create an amt → mps → amt cycle). The future amt/transport rename per the modular-monolith evaluation §4.1 is the natural place to consolidate the types. `mps.Server` now takes a narrow `AMTStateWriter` interface (Upsert + SetStatus) defined inside the `mps` package; `amt.Repository` satisfies it structurally. `mps_test.go` gains a small `pgAMTState` struct (importing `amt.PostgresAMTDevices` from `mps_test` would create a build cycle since `amt → mps`); `amt/service_test.go` converted to `package amt_test` so it can import testutil (which now imports amt). `api.Server` grows an `amtDevices amt.Repository` field; `handlers_amt.go` rewires onto it. New `testutil.NewTestAMTDevices(t, store)`; `SeedAMTDevice` uses the repo. 8 api test files + 2 integration test files get the `AMTDevices` field on every `ServerConfig` literal. Removed: 4 db.Store methods + 4 `PostgresStore` impls + 4 `InstrumentedStore` wrappers + 1 unused `uuid` import in `db` + `TestAMTDeviceCRUD` block (~75 lines) from `db/store_test.go`. Full gauntlet green. | — | [modular-monolith-evaluation.md](plans/archive/modular-monolith-evaluation.md) §9 |
| ADR-021 #5: `notifications` WebPush repository extraction | Fifth per-aggregate extraction off `db.Store` (19 → 15 methods). 3 new files in `server/internal/notifications/`: `webpush.go` (`WebPushSubscription` + sentinel `ErrSubscriptionNotFound` + 4-method `WebPushRepository` interface), `postgres_webpush.go` (`PostgresWebPush` adapter), `instrumented_webpush.go` (decorator + duck-typed `Observer`). `PushNotifier` now depends only on `WebPushRepository`, not `db.Store` — notifications becomes a clean leaf module with zero internal imports. `db.WebPushSubscription` aliased to `notifications.WebPushSubscription`. `api.Server` grows a `webPush notifications.WebPushRepository` field; `handlers_push.go` rewires onto it. New `testutil.NewTestWebPush(t, store)`. `.go-arch-lint.yml` adds the `notifications` component (deny-all internal, deepScan off — same pattern as audit/update/auth/device). 8 api test files + 2 integration test files get the new `WebPush` field on every `ServerConfig` literal. Removed: 4 db.Store methods + 4 `PostgresStore` impls + 4 `InstrumentedStore` wrappers + 4 `notifMockStore` methods (the entire 26-method mock is replaced by a 4-method `notifMockRepo`) + `TestWebPushSubscriptionCRUD` + `TestListAllWebPushSubscriptions` blocks (~120 lines) from `db/store_test.go`. Full gauntlet green. | — | [modular-monolith-evaluation.md](plans/archive/modular-monolith-evaluation.md) §9 |
| ADR-021 #4: `device` aggregate extraction (Devices+Groups+Hardware+Logs) | Fourth and largest per-aggregate extraction off `db.Store` — drops it from 37 to 19 methods, clearing the `<30` target set in ADR-021 in one PR. New `server/internal/device/` package with four Repository ports (`Repository`/`GroupRepository`/`HardwareRepository`/`LogsRepository`), their Postgres adapters (`PostgresDevices`/`PostgresGroups`/`PostgresHardware`/`PostgresLogs`), and four `Instrumented*` decorators. Owns the `DeviceID`/`GroupID` aliases, `DeviceStatus` + constants, `Device`/`Group`/`Hardware`/`NetworkInterfaceInfo`/`LogEntry`/`LogFilter` types, and sentinel errors `ErrDeviceNotFound`/`ErrGroupNotFound`/`ErrHardwareNotFound`. `db.models.go` aliases the type names (`type Device = device.Device` etc.) and the `DeviceStatus` constants to ease incremental migration of the not-yet-extracted AMT module + remaining test files. `api.Server` grows four new fields; every device/group/hardware/logs handler rewires onto them. `agentapi.NewAgentServer` is refactored to take an `AgentServerConfig` struct (the seven-arg constructor exceeded Sonar's parameter cap once the three device repos joined `deviceUpdates`); `AgentConn` follows the same pattern internally. `main.go` constructs all four `device.Instrumented*` repos against the shared connection pool and reuses `devicesRepo.ResetAllStatuses(ctx)` to clear stale online statuses on startup (previously `store.ResetAllDeviceStatuses`). New testutil helpers `NewTestDevices`/`NewTestGroups`/`NewTestHardware`/`NewTestLogs`; `SeedDevice`/`SeedGroup` reroute through them so existing callers stay untouched. 10 `ServerConfig{}` literals across api/integration tests gain the four new fields. `.go-arch-lint.yml` adds the `device` component (deny-all internal, deepScan off — same shape as audit/update/auth). Removed: 18 methods from `db.Store` + `PostgresStore` + `metrics.InstrumentedStore` + `notifications.notifMockStore`, plus `db.syncIsAdmin` already moved to `auth` in #3 stays put. The 7 device/group/hardware/logs tests in `db/store_test.go` (~520 lines) and `db/bench_test.go` (~130 lines) are removed; equivalent coverage now lives in `device/device_test.go`. Full server suite green with `-race` against Postgres. | — | [modular-monolith-evaluation.md](plans/archive/modular-monolith-evaluation.md) §9 |
| ADR-021 #3: `auth` SecurityGroup repository extraction | Third per-aggregate extraction off `db.Store` (46 → 37 methods toward the `<30` target). 3 new files in `server/internal/auth/`: `securitygroup.go` (`SecurityGroup` + `Member` view + `SecurityGroupID` alias + sentinel errors `ErrSecurityGroupNotFound`/`ErrSystemGroup`/`ErrLastAdmin`/`ErrMemberNotFound` + `AdminGroupID` + `SecurityGroupRepository` 9-method interface), `postgres_securitygroup.go` (`PostgresSecurityGroups` adapter; `syncIsAdmin` moved here — the users.is_admin mirror is owned by the auth aggregate, not orchestrated externally), `instrumented_securitygroup.go` (decorator + duck-typed `Observer`). `api.Server` grows a `securityGroups auth.SecurityGroupRepository` field; `handlers_security_groups.go` rewires onto it with a new `memberToAPI(*auth.Member) User` converter; `handlers_users.go` + `handlers_auth.go` switch their `IsUserInGroup`/`AddMember`/`RemoveMember` calls to the auth repo and the `auth.AdminGroupID` constant. New `testutil.NewTestSecurityGroups(t, store)` + `SeedAdminUser` now uses it. `.go-arch-lint.yml` adds the `auth` component (deny-all internal, deepScan off — same pattern as audit + update). 10 api test files + 1 integration test file get the new `SecurityGroups:` field on every `ServerConfig` literal. Removed: 9 db.Store methods + 4 db model types (`SecurityGroup`/`SecurityGroupID`/`SecurityGroupMember`/`AdminGroupID` + 2 sentinel errors) + 9 `metrics.InstrumentedStore` wrappers + 9 `notifications` mock methods + the entire `TestSecurityGroup*` block (260 lines, 6 tests) from `db/store_test.go`. Full server suite green with `-race`. | — | [modular-monolith-evaluation.md](plans/archive/modular-monolith-evaluation.md) §9 |
| ADR-021 #2: `update` module extraction (DeviceUpdate + Enrollment) | Second per-aggregate extraction off `db.Store` (54 → 46 methods toward the `<30` target). 4 new files in `server/internal/updater/`: `repository.go` (`DeviceUpdate` + `Status` enum + `DeviceUpdateRepository`), `enrollment.go` (`EnrollmentToken` + `EnrollmentTokenRepository`), `postgres.go` (`PostgresDeviceUpdates` + `PostgresEnrollment` adapters with their own per-package `ErrNotFound`), `instrumented.go` (decorators for both repos). `api.Server` grows `deviceUpdates`/`enrollment` fields; `handlers_updates.go` calls `s.deviceUpdates.Create/ListByVersion`; `handlers_enrollment.go` calls `s.enrollment.Create/Get/List/Delete/IncrementUseCount`. `agentapi.AgentServer` + `AgentConn` accept the `DeviceUpdateRepository` for the update-ack persistence path on conn.go:210; `NewAgentServer` and `NewAgentConn` signatures both grow the parameter. New `testutil.NewTestDeviceUpdates` + `testutil.NewTestEnrollment` helpers reuse the per-test schema. `.go-arch-lint.yml` adds the `update` component (deny-all internal, deepScan off — same pattern as audit). Removed: 8 db.Store methods + 3 db model types (`DeviceUpdate`/`EnrollmentToken`/`UpdateStatus`) + 8 `metrics.InstrumentedStore` wrappers + 8 `notifications` mock methods. Migrations table unchanged (db package still owns `enrollment_tokens` + `device_updates` schemas). Full server suite green with `-race`. | — | [modular-monolith-evaluation.md](plans/archive/modular-monolith-evaluation.md) §9 |
| ADR-021 pilot: `audit` repository extraction | First per-aggregate extraction out of `db.Store` (56 → 54 methods toward the `<30` target). New `server/internal/audit/` package owning `Event` + `Query` types, the `Repository` outbound port, the Postgres adapter (sentinel-param query preserved), and an `Instrumented` decorator. `*Server` grows an `audit audit.Repository` field; `auditLog` and `ListAuditEvents` rewire onto it. `metrics.Metrics` gains an `Observe(op, dur, ok)` method so audit's duck-typed `Observer` is satisfied without an import (audit source stays internal-import-free, deepScan disabled for audit per ADR-020 to match). New `testutil.NewTestAudit(t, store)` reuses the same per-test schema as `NewTestStore` so api/integration tests get a real audit repo without a parallel setup path. 10 api test files + 4 integration test files wire the new `Audit:` field on every `ServerConfig` literal. `.go-arch-lint.yml` adds the `audit` component (deny-all internal, `_test.go` excluded). `WriteAuditEvent`/`QueryAuditLog` + `db.AuditEvent`/`db.AuditQuery` removed from `db.Store`, postgres impl, models, store_test, and `metrics.InstrumentedStore`. Full gauntlet green incl. SonarCloud, coverage thresholds, taint, dead-code, e2e. | — | [modular-monolith-evaluation.md](plans/archive/modular-monolith-evaluation.md) §9 |
| Path-gate `build-image.yml` + digest-aware CD redeploy | Follow-on to `path-gate-agent-release` — ~83% of main-branch commits don't touch any image input. **Image-build gate:** new `check-image-changed` job in [`.github/workflows/build-image.yml`](../.github/workflows/build-image.yml) runs [`scripts/build-image-gate.sh`](../scripts/build-image-gate.sh) (pure bash, resolves `org.opencontainers.image.revision` from `:latest` via `crane config | jq`, falls open when label/registry missing or `prev_sha` rebased away, diffs `server/** web/** Dockerfile`); `build-and-push` gains `needs:` + `if: image_changed == 'true'`; new `tag-forward` job `crane copy`s `:latest → :sha-<newsha>` when build is skipped so CD's tag lookup keeps working. Cosign/SBOM/Trivy attestations inherit via the digest. Workflow-level `permissions:` removed and scoped per job (mirrors release-agent pattern). **CD digest-equality gate:** [`deploy/scripts/common.sh`](../deploy/scripts/common.sh) `redeploy()` now pulls first, compares the new image digest against the sentinel-recorded `image_digest`, and short-circuits the compose restart only when **both** the digest is unchanged AND `deploy_changed=false`. Pure infra changes (Grafana dashboards, compose tweaks) still trigger a "config-only redeploy" branch. New helpers `sentinel_file`, `read_sentinel_field`, `write_sentinel`, `inspect_digest`. Sentinel `/opt/opengate/.last-deployed[-staging]` (mode-specific) holds `image_tag`, `image_digest`, `git_sha`, `deployed_at`; written atomically via `mktemp + mv`. `deploy/scripts/deploy.sh` accepts new `--git-sha` and `--deploy-changed` flags (default `--deploy-changed=true` so older CD never silently skips). [`.github/workflows/cd.yml`](../.github/workflows/cd.yml) `deploy-staging` and `deploy-production` each ssh-read the sentinel's `git_sha`, locally compute `deploy_changed` via `git diff --quiet $LAST_SHA $GITHUB_SHA -- deploy/` (with `git cat-file -e` reachability guard for force-push history), and pass both flags through. Checkout switched to `fetch-depth: 0` so the diff can resolve historical SHAs. **Tests (TDD):** 9 new bash tests in [`scripts/tests/build-image-gate.test.sh`](../scripts/tests/build-image-gate.test.sh) (no `:latest`, agent-only unchanged, server changed, web changed, Dockerfile changed, deep server subdir, missing revision label, missing env-var usage, prev_sha unreachable) — `crane` mocked via a shim on `$PATH`, no network needed. **Docs:** new "Skip-When-Unchanged Path" section in [`docs/Continuous-Deployment.md`](../docs/Continuous-Deployment.md). | — | [path-gate-build-image.md](plans/archive/path-gate-build-image.md) |
| ADR-020 §9 trigger: `internal/mps` → `internal/amt/transport` rename | Per modular-monolith plan §4.1 + §9, the standalone `internal/mps` package (3,910 LOC: APF/CIRA + nested `wsman/`) folded into `internal/amt/transport` as the AMT module's transport layer. Pure rename — no behavior change. `git mv server/internal/mps server/internal/amt/transport`; `package mps` → `package transport` in 5 source/test files; `wsman` subpackage retained. 9 callers updated: [`server/internal/amt/service.go`](../server/internal/amt/service.go), [`server/internal/amt/service_test.go`](../server/internal/amt/service_test.go), [`server/internal/amt/transport/wsman/client.go`](../server/internal/amt/transport/wsman/client.go), [`server/internal/amt/transport/wsman/client_wire_test.go`](../server/internal/amt/transport/wsman/client_wire_test.go), [`server/internal/api/api.go`](../server/internal/api/api.go), [`server/internal/api/handlers_amt.go`](../server/internal/api/handlers_amt.go), [`server/internal/api/helpers_test.go`](../server/internal/api/helpers_test.go), [`server/tests/integration/api_test.go`](../server/tests/integration/api_test.go), [`server/cmd/meshserver/main.go`](../server/cmd/meshserver/main.go). Coverage exclusion regex in [`sonar-project.properties`](../sonar-project.properties) + [`scripts/precommit-gauntlet.sh`](../scripts/precommit-gauntlet.sh) repointed (`mps/wsman` → `amt/transport/wsman`). Doc paths updated in [`docs/Architecture.md`](../docs/Architecture.md), [`/precommit`](/.claude/skills/precommit/SKILL.md), [`/backend-audit`](/.claude/skills/backend-audit/SKILL.md), [`/infra-audit`](/.claude/skills/infra-audit/SKILL.md), [`/tests-audit`](/.claude/skills/tests-audit/SKILL.md). Plan §4.1 + §9 trigger marked rename-done; `AMTOperator` → `AMTService` port-promotion remains future work. CLI flag `-mps-listen` kept (it names the MPS protocol, not the package). | — | [modular-monolith-evaluation.md](plans/archive/modular-monolith-evaluation.md) §9 |
| ADR-021 closeout: retire `db.Store` interface | Last of the per-aggregate extractions. `db.Store` had been reduced to 2 methods (`Ping`/`Close`) — per ADR-020's earned-port rule it no longer earns its keep (1 production caller for `Ping`, only `defer store.Close()` for `Close`). The interface is deleted; consumers use the concrete `*db.PostgresStore` directly. Touched: [`server/internal/db/store.go`](../server/internal/db/store.go) (now just a package doc), [`server/internal/api/api.go`](../server/internal/api/api.go) (`Server.store` + `ServerConfig.Store` retyped), [`server/internal/testutil/testutil.go`](../server/internal/testutil/testutil.go) (14 helper signatures + inline `extractDB(t, s, name)` → `s.DB()` at call sites; helper deleted), [`server/internal/db/store_test.go`](../server/internal/db/store_test.go) (drop now-vestigial `storeFactory`/`storeFactories` indirection; the type-assertion in `TestPostgresStoreDB` is no longer needed). 9 test files + 1 integration test updated. The orphan `db.ErrNotFound` (only referenced by a doc comment in [`updater/postgres.go`](../server/internal/updater/postgres.go)) deleted with it; per-module sentinels (`auth.ErrUserNotFound`, `device.ErrDeviceNotFound`, etc.) are now the only "not found" surface. ADR-021's `<30` residual-method target met by **0** — `db.Store` has been fully retired. Full gauntlet green. | — | [modular-monolith-evaluation.md](plans/archive/modular-monolith-evaluation.md) §9 |
| ADR-020 §5.4 eslint-boundaries flip + ADR-021 §9 AMT port-promotion | Two follow-on ADR cleanups bundled. **§5.4 flip:** [`web/eslint.config.js`](../web/eslint.config.js) `boundaries/dependencies` severity warn → error (zero existing violations); marker recorded at [`.claude/.markers/arch-lint-flipped/eslint-boundaries`](../.claude/.markers/arch-lint-flipped/eslint-boundaries). [`scripts/arch-lint-flip.sh`](../scripts/arch-lint-flip.sh) gains a state-aware eslint-boundaries gate (replaces the prior hardcoded "deferred" line): `flipped` if marker present OR severity is `'error'`, `eligible` if severity is `'warn'` AND no marker, `no config` if file missing; `--apply` on eligible mutates the severity token atomically (sed via temp + mv with grep validation) and writes the marker. 9 new bash test cases in [`scripts/tests/arch-lint-flip.test.sh`](../scripts/tests/arch-lint-flip.test.sh) cover dirty/eligible/apply/idempotent/reconcile/no-config paths; full runner 18 → 27 tests, all green. Gauntlet's existing `npx eslint .` step picks up the new severity transparently — no separate marker-aware step needed (unlike depcruise which uses a snapshot baseline). **§9 AMT port-promotion:** new [`server/internal/amt/operator.go`](../server/internal/amt/operator.go) declares the `amt.Operator` inbound port (PowerAction / QueryDeviceInfo / ConnectedDeviceCount). The plan §9 row's `AMTService` name was renamed to `Operator` to avoid colliding with the existing concrete `*amt.Service` struct and to match repo convention (`audit.Repository`, `device.Repository`, `notifications.Notifier` — interfaces drop the module-prefix stutter). [`server/internal/amt/operator_test.go`](../server/internal/amt/operator_test.go) pins the contract with a compile-time `var _ Operator = (*Service)(nil)` assertion. [`server/internal/api/api.go`](../server/internal/api/api.go) drops its local `AMTOperator` interface declaration + the now-unused `google/uuid` and `amt/transport/wsman` imports; `ServerConfig.AMT` and `Server.amt` retype to `amt.Operator`. Test stub comments in [`helpers_test.go`](../server/internal/api/helpers_test.go) and [`tests/integration/api_test.go`](../server/tests/integration/api_test.go) updated; `stubAMTOperator` / `stubAMT` structs continue to satisfy the new interface via structural typing (no signature changes). `go-arch-lint check` clean. | — | [modular-monolith-evaluation.md](plans/archive/modular-monolith-evaluation.md) §9 |
| ADR-024 §9 `MouseHandler` carve-out + Code Scanning ruleset unblock | **ADR-024 pilot carve:** new [`agent/crates/mesh-agent-core/src/session/handlers/`](../agent/crates/mesh-agent-core/src/session/handlers/) module with [`mod.rs`](../agent/crates/mesh-agent-core/src/session/handlers/mod.rs) declaring the `ControlMessageHandler` marker trait and [`mouse.rs`](../agent/crates/mesh-agent-core/src/session/handlers/mouse.rs) implementing `MouseHandler` (associated functions `handle_mouse_move` + `handle_mouse_click` taking `&Permissions` + `&dyn InputInjector` explicitly — no per-session state). [`session/handler.rs`](../agent/crates/mesh-agent-core/src/session/handler.rs) `handle_control` `MouseMove`/`MouseClick` arms delegate to `MouseHandler::*`; the two `fn handle_mouse_*` methods on `SessionHandler` deleted (the body moved verbatim into MouseHandler with the permission gate restructured to early-return). 11 new unit tests in `mouse.rs` cover both permitted/denied paths, injector failure (move + button independently), u16::MAX boundary, every `MouseButton` variant, NullInput smoke, and a compile-time `assert_impl::<MouseHandler>()` against the trait marker. 3 new integration tests in [`tests/mouse_handler_test.rs`](../agent/crates/mesh-agent-core/tests/mouse_handler_test.rs) exercise the public crate surface. Existing 13 `handler.rs` inline tests covering Mouse* through the `Frame::Control` dispatch path remain green — pre-carve-out behavior preserved end-to-end. **Code Scanning ruleset unblock** (bundled because PRs #254 + #255 were stuck): `gh api -X PUT /repos/.../rulesets/13506313` dropped SonarCloud from the `code_scanning` rule's `code_scanning_tools` (CodeQL stays). Root cause: `SonarSource/sonarqube-scan-action` uploads SARIF only on push events to `dev` (verified `634d42c`, `c4ad7ad` have SonarCloud SARIF; PR merge ref `e473067` has only 3 CodeQL analyses, zero SonarCloud) — every Dependabot PR was blocked waiting for SARIF that never arrived. SonarCloud quality gate remains enforced via the `SonarCloud Analysis` required status check (which posts a regular GitHub PR check, not a Code Scanning entry). Both PRs flipped from `BLOCKED` to `CLEAN/MERGEABLE` immediately after the ruleset update. [docs/CI-Pipeline.md](../docs/CI-Pipeline.md) ruleset table appended with the change reason + date. | — | [modular-monolith-evaluation.md](plans/archive/modular-monolith-evaluation.md) §9 |
| ADR-020 §5.4 cargo-deny flip + ADR-020 Amendment 1 + Dependabot #23 fix | **§5.4 cargo-deny flip:** third of three flippable arch-lint gates (after depcruise + eslint-boundaries). [`agent/deny.toml`](../agent/deny.toml) `multiple-versions` + `wildcards` both warn → deny; marker at [`.claude/.markers/arch-lint-flipped/cargo-deny`](../.claude/.markers/arch-lint-flipped/cargo-deny). Resolved 28 pre-existing transitive-duplicate warns via a per-entry `[bans.skip]` allowlist with documented rationale (each entry pins the older version; reviewer rule: drift list down as upstreams converge — RustCrypto 0.10/0.11, getrandom 0.2/0.3/0.4, rcgen 0.13/0.14, Windows API binding cohort 0.42/0.52/0.61, etc.). Resolved 7 wildcard warns by marking all 5 workspace crates (`mesh-agent`, `mesh-agent-core`, `mesh-protocol`, `platform-linux`, `platform-windows`) `publish = false` (they ship via GitHub Releases, not crates.io) which unlocks `allow-wildcard-paths = true`. **ADR-020 Amendment 1:** new [ADR-020 Amendment 1 (cargo-deny)](../docs/adr/ADR-020-modular-monolith-full-hexagonal.md#amendments) amends ADR-020 §5.2 with the Rust HTTP-stack inventory (direct: `reqwest` + `tokio-tungstenite`; transitive via reqwest: `hyper`/`hyper-rustls`/`hyper-util`/`http`/`http-body`/`http-body-util`; via tungstenite: `tungstenite`); adds policy that net-new direct HTTP crates require an ADR amendment before merge (enforcement via code review since cargo-deny can't express "direct-only" bans); one explicit `[[bans.deny]]` for `openssl` (rustls-only policy). [`scripts/arch-lint-flip.sh`](../scripts/arch-lint-flip.sh) gains a state-aware cargo-deny gate mirroring the eslint-boundaries shape: `flipped` if marker present OR both severities at `deny`, `eligible` if both at `warn` AND no marker, `no config` if file missing; `--apply` on eligible atomically mutates both severities via paired `sed` invocations + grep validation; reconcile branch handles the case where deny.toml was edited to `deny` in the same commit. 11 new bash test cases in [`scripts/tests/arch-lint-flip.test.sh`](../scripts/tests/arch-lint-flip.test.sh); runner 27 → 38 tests, all green. **Dependabot #23 fix:** [`web/package.json`](../web/package.json) gains an `overrides.qs` entry forcing `qs >= 6.15.2`, closing CVE-2026-8723 / GHSA-q8mj-m7cp-5q26 (medium-severity DoS via `qs.stringify` TypeError on null/undefined entries in comma-format arrays). qs is pulled in only transitively via `@stryker-mutator/core` → `typed-rest-client` (dev-only mutation testing); production impact was zero but the fix is trivial. `npm ls qs` confirms 6.15.2 post-install. **Status:** 3 of 5 ADR-020 §5.4 gates flipped (depcruise + eslint-boundaries + cargo-deny); the remaining 2 (`go-arch-lint`, `cargo-modules`) were already strict from gauntlet adoption — ADR-020 §5.4 mechanism now fully exercised. | — | [ADR-020 Amendment 1 (cargo-deny)](../docs/adr/ADR-020-modular-monolith-full-hexagonal.md#amendments) |

---

## In Progress

| Phase | Summary | Version | Plan |
|-------|---------|---------|------|
| Edge-Sentinel — Phase 0 feasibility gate | Netdata-informed local anomaly detection + multi-tenant RMM telemetry; started the evidence-first Wave 0 gate that must pass before default-on behavior. **Block 1:** [`server/internal/testvm`](../server/internal/testvm/testvm.go) throwaway-VictoriaMetrics harness (mirrors `testpg`, pins the deployed `v1.114.0`, auto-starts or honors `VICTORIAMETRICS_TEST_URL`) so telemetry integration tests always run deterministically. **Block 2:** [`server/tests/vmcardinality`](../server/tests/vmcardinality/spike_test.go) ingest/cardinality spike — measured VM active-series matches the avg-only model exactly (typical host 40/agent → 20k at 500 agents, within the 50k budget); centralising all four aggregates would be ~3.5× (71k), grounding the avg-only-central decision. **Block 3:** [`server/tests/vmbackfill`](../server/tests/vmbackfill/spike_test.go) proves historical backfill through the import API preserves each sample's original timestamp (hours/days/week backdated) and never collapses toward arrival time — validating the import-not-stream-agg rule (stream aggregation buckets by arrival, so it stays for live telemetry only). WS-0/1/2/3/4 foundation is complete; the tenancy rehearsal is now automated in [`server/internal/db`](../server/internal/db/store_test.go). Remaining Phase 0 gate item: record the Edge Sentinel sampler/ML footprint bench on real ARM hardware and use it to finalize per-entity caps before enabling default-on emission. | — | `plans/edge-sentinel.md` (+ `edge-sentinel-ws-*.md`) |
