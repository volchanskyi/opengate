# Grafana admin password (change on first deploy)
GF_SECURITY_ADMIN_PASSWORD=changeme

# PostgreSQL password (must match the main stack's POSTGRES_PASSWORD)
POSTGRES_PASSWORD=changeme

# Telegram bot for critical alerts (create via @BotFather)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
