# Telegram contact point is configured manually via the Grafana UI.
# File-based provisioning of Telegram is blocked by a Grafana bug (#69950):
# numeric chat IDs are unmarshaled as JSON numbers instead of strings,
# causing a startup crash. No YAML-level workaround exists.
apiVersion: 1

contactPoints: []
