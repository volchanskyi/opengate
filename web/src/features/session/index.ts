// Public surface of this feature (ADR-022 barrel). Cross-feature consumers MUST import from here.
export { useSessionStore } from './state/session-store';
export { useConnectionStore } from './state/connection-store';
